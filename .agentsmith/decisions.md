# Decision Log

## p0001: Core Infrastructure
- [Architecture] Command Pattern (MediatR-style) as central pattern — strict separation of "What" (context) from "How" (handler)
- [Architecture] Single generic handler interface `ICommandHandler<TContext>` instead of multiple handler types — enables DI-based dispatch
- [Tooling] YamlDotNet for configuration — chosen for simplicity, avoids overengineering (no JSON schema validation at this stage)
- [Implementation] Lazy environment variable resolution in config loader — unset vars aren't errors during loading, only at usage time
- [TradeOff] No validation of all environment variables upfront — accepted incomplete config to gain flexibility

## p0002: Pipeline Handlers
- [Architecture] CommandExecutor uses `GetService<T>()` instead of `GetRequiredService<T>()` — allows custom error messages instead of DI exceptions
- [Implementation] Handlers are sealed classes with primary constructors — enforces immutability and modern .NET 8 conventions
- [TradeOff] LoadCodingPrinciplesHandler can be fully implemented in Phase 2 but intentionally stubbed — accepted inconsistency to keep phases parallel
- [Implementation] ApprovalHandler auto-approved as stub — establishes pattern for later headless mode

## p0003: Providers
- [Architecture] Separate provider for each source control system (GitHub, Local, GitLab later) — each has different API, no shared abstraction
- [Tooling] ClaudeAgentProvider chosen as primary agent — Anthropic API, agentic loop pattern established as reference
- [Architecture] ToolExecutor separated from agent loop — allows code reuse across provider implementations
- [TradeOff] Scout phase (file discovery) designed but deferred to Phase 9 — accepted complexity later to unblock core provider work
- [Security] Path validation in tools (no `..`, no absolute paths) — prevents directory traversal attacks from agentic code execution

## p0004: Pipeline Execution
- [Architecture] Regex-based intent parser instead of LLM call — chosen for determinism, cost, and Phase 4 speed
- [Architecture] CommandContextFactory maps command names to contexts — avoids massive switch statement in PipelineExecutor
- [Implementation] ProjectConfig passed as parameter to PipelineExecutor, not injected — enables per-run configuration variation
- [TradeOff] Intent parser intentionally simple — can be swapped for Claude-based parser later without interface changes

## p0005: CLI & Docker
- [Tooling] System.CommandLine instead of manual argument parsing — provides --help, validation, version support "for free"
- [TradeOff] No sub-commands, no daemon modes, no interactive shell — "Simple: Input in → PR out → Exit"
- [Architecture] Multi-stage Docker build — separates build (SDK) from runtime (slim), targets <200MB image size
- [Implementation] Config mounted as volume, not baked into image — enables reuse without rebuild

## p0006: Retry & Resilience
- [Tooling] Polly via `Microsoft.Extensions.Http.Resilience` chosen over custom retry logic — standard, battle-tested, reduces code
- [Architecture] Retry config as typed class, not raw numbers — enables configuration without code changes
- [TradeOff] Conservative defaults (5 retries, 2sec initial, 2.0x backoff) — balances safety against cost overruns from excessive retries
- [Implementation] Exponential backoff with jitter — prevents thundering herd on rate limit recovery

## p0007: Prompt Caching
- [Architecture] `PromptCaching = AutomaticToolsAndSystem` for all API calls — caches system prompt, tool definitions, coding principles
- [Implementation] Coding Principles positioned first in system messages — maximizes cache prefix (longest stable content cached)
- [TradeOff] 5-minute ephemeral cache (API default) accepted — balance between cost savings and context freshness
- [Architecture] TokenUsageTracker separates cache metrics tracking — enables observability without coupling to loop logic

## p0008: Context Compaction
- [Architecture] Haiku for summarization, not custom rule-based compression — LLM-generated summaries more useful than keyword extraction
- [Architecture] FileReadTracker deduplicates file reads — prevents same file content from inflating conversation history
- [TradeOff] Last N iterations always preserved fully — accepted overhead to maintain recent decision context
- [Implementation] Compaction triggered periodically, not continuously — prevents overhead from constant summarization

## p0009: Model Registry & Scout
- [Architecture] TaskType enum maps task to appropriate model — Haiku for discovery, Sonnet for coding, optional Opus for reasoning
- [Architecture] Scout as implementation detail of ClaudeAgentProvider, not separate interface — keeps IAgentProvider stable
- [TradeOff] Scout is read-only (no write_file, no run_command) — accepts limitation to prevent Scout from breaking code
- [Architecture] Backward compatible: Models config is optional — existing single-model configs work unchanged

## p0010: Container Production-Ready
- [Architecture] `--headless` flag auto-approves plans — enables container/CI usage without TTY or interactive prompts
- [Implementation] Non-root user in Docker — security best practice
- [Architecture] SSH keys mounted read-only from host — enables git operations without embedding credentials

## p0011: Multi-Provider
- [Architecture] Each provider self-contained with own agentic loop — APIs too different (tool format, caching, streaming) to abstract
- [Architecture] IAgentProvider interface stable across all providers — GeneratePlanAsync, ExecutePlanAsync only methods exposed
- [Tooling] No shared "LLM abstraction layer" — accepted code duplication to gain provider-specific optimization freedom
- [TradeOff] Scout and caching Claude-only for now — other providers do direct execution, accepted feature parity gap

## p0012: Cost Tracking
- [Architecture] Prices in config, not hardcoded — providers change pricing frequently, enables user updates without code change
- [Implementation] Phase-aware token tracking (scout, planning, primary, compaction) — enables cost breakdown, not just total
- [TradeOff] Missing model pricing defaults to zero with warning — no error thrown, helps with incomplete configs

## p0013: Ticket Writeback
- [Architecture] Default interface implementations on ITicketProvider — backward compatible, existing providers work without changes
- [Implementation] Fire-and-forget ticket operations wrapped in try/catch — failures never block the pipeline
- [Architecture] CommitAndPRHandler closes ticket and posts summary — provides feedback loop to issue tracker

## p0014: GitHub Action & Webhook
- [Architecture] Both GitHub Action (zero infrastructure) and webhook listener (self-hosted) variants — choice for user deployment model
- [Tooling] HttpListener for webhook (no ASP.NET dependency) — minimal overhead for simple HTTP requirements
- [Implementation] Each webhook request spawns async Task — non-blocking, server returns 202 Accepted immediately
- [TradeOff] Trigger label configurable but defaults to "agent-smith" — simplicity over ultra-flexibility

## p0015: Azure Repos
- [Architecture] Reuses LibGit2Sharp pattern from GitHub provider — shared git operations, different PR API
- [Implementation] Clone URL embeds PAT for HTTPS auth — consistent with GitHub provider pattern
- [Architecture] PR URL constructed from org/project/repo structure — matches Azure DevOps URL format

## p0016: Jira Provider
- [Tooling] No Jira SDK — pure HttpClient with JSON, Jira REST API is straightforward
- [Implementation] ADF (Atlassian Document Format) for comments — required by Jira v3 API, wraps plain text in minimal structure
- [TradeOff] Transition names searched flexibly (contains "Done" or "Closed") — accepts incomplete workflow support over strict matching
- [Implementation] Basic Auth with email:apiToken — standard Jira Cloud authentication method

## p0017: GitLab Provider
- [Architecture] Extract `ISourceProvider` and `ITicketProvider` implementations — GitLab MRs use same semantic model as GitHub PRs, REST API v4 is straightforward JSON
- [Implementation] `LibGit2Sharp` for git operations and GitLab REST API v4 for merge requests — consistent with existing GitHub provider

## p0018: Chat Gateway
- [Architecture] Redis Streams as message bus (progress/question/done/error) with persistent consumer groups — decouples agent execution from dispatcher, enables multi-platform routing
- [Architecture] Separate `IProgressReporter` implementations: Console for CLI, Redis for K8s — single interface, pluggable output strategy
- [Implementation] K8s Job per request with ephemeral agent containers — isolation, resource limits, TTL cleanup, each fix runs independently

## p0019: K8s Deployment
- [Architecture] Multi-stage Dispatcher Dockerfile (build → runtime with non-root user) — separates compile from runtime, reduces image size, hardens security
- [TradeOff] Redis without PersistentVolume initially — ephemeral streams only live during job execution, simplifies local testing, upgrade path exists
- [Architecture] Kustomize base + dev/prod overlays — reusable structure, environment-specific patches avoid duplication

## p0019a: Docker Spawner
- [Architecture] Extract `IJobSpawner` interface with Docker and K8s implementations — single abstraction enables local docker-compose mode without K8s
- [TradeOff] Auto-detect Docker network from dispatcher container metadata — avoids explicit config in most cases, fallback to `bridge`
- [Architecture] `AutoRemove=true` on Docker containers — self-cleanup, no orphan accumulation

## p0019b: K8s Helm
- [Architecture] Helm Chart replaces manual `apply-k8s-secret.sh` — templated secrets, repeatable deployments, built-in rollback
- [TradeOff] Kustomize kept for reference — users can stick with existing workflow or migrate to Helm incrementally

## p0020a: Intent Engine
- [Architecture] Three-stage engine: Regex (free) → Haiku (cheap) → deterministic project resolution — 90% resolved free, fallback only when needed
- [Architecture] Parallel ticket provider queries for project resolution — minimizes latency on multi-project configs
- [TradeOff] Accepted regex brittleness for 90% of inputs to avoid AI cost — only calls Haiku on parse failures

## p0020b: Help Command
- [Architecture] Greeting detection in Regex stage — "hi", "hello" cost zero AI tokens
- [Implementation] `ClarificationStateManager` stores low-confidence suggestions in Redis with TTL — avoids second AI call when user confirms

## p0020c: Error UX
- [Architecture] `ErrorFormatter` with regex pattern table — maps raw errors to friendly messages, pure function fully testable
- [Architecture] `ErrorContext` carries step number + step name — users see exactly where in the pipeline failure occurred
- [TradeOff] Contact button optional via env var — graceful degradation when `OWNER_SLACK_USER_ID` not set

## p0020d: Agentic Detail Updates
- [Architecture] `ReportDetailAsync` on `IProgressReporter` — fire-and-forget so failed detail posts never abort pipeline
- [Implementation] Rate throttling on detail events (max 1 per 3 files) — prevents Slack API rate limit errors
- [TradeOff] Thread ts stored in memory only — Dispatcher restart loses thread continuity but degrades to top-level messages

## p0021: Code Quality
- [Architecture] Consistent folder structure (Contracts/, Models/, Services/) across all projects — eliminates sprawl
- [Tooling] `IHttpClientFactory` instead of `new HttpClient()` — proper lifetime management, connection pooling
- [Implementation] Magic numbers → named constants — single source of truth for configuration values

## p0022: CCS Auto-Bootstrap
- [Architecture] `ProjectDetector` is deterministic (no LLM, pure file system) — fast, zero cost, fully testable
- [Architecture] Single cheap LLM call for context generation — one-shot avoids iterative refinement, cost ~$0.01
- [TradeOff] Generated `.context.yaml` committed to repo — persistent project context, avoids regeneration on every run

## p0024: Code Map
- [Architecture] LLM-assisted extraction instead of custom parsers — hand-written AST parsing is error-prone and language-specific
- [TradeOff] Code map generation only on bootstrap or request — avoids cost on every run, requires explicit regeneration when architecture changes
- [TradeOff] 5,000 token cost per generation but saves 100k+ tokens in file reading — break-even on first agent run

## p0026: Coding Principles Detection
- [Architecture] Per-project coding principles loaded at runtime — respects project-specific style, agent sees rules before planning
- [Implementation] Principles as free-form markdown (no schema) — flexibility for humans to define culture-specific rules

## p0027: Structured Command UI
- [Architecture] Slack modals with dropdowns replace free-text parsing — no typos, structured data needs no IntentEngine
- [TradeOff] Free-text input kept as fallback — backward compatibility for power users

## p0028: .agentsmith/ Directory
- [Architecture] Unified `.agentsmith/` replaces scattered config files — single source of truth for project meta-files
- [Architecture] Run tracking as immutable records (`r{NN}-{slug}/`) — auditable changelog of all agent decisions
- [TradeOff] Failed runs recorded only in runs/ (not state.done) — state.done tracks project state changes, not execution history

## p0029: Init Project Command
- [Architecture] Generalize `IJobSpawner.SpawnAsync` from `FixTicketIntent` to generic `JobRequest` — init-project becomes first-class command

## p0030: Systemic Fixes
- [Architecture] `CommandResult` carries PR URL through pipeline — URLs were previously lost due to generic return type
- [Architecture] `OrphanJobDetector` as background service monitoring in-memory + Redis — catches orphans from pre-deployment jobs
- [Implementation] Remove `CancellationToken = default` optional parameters — forces explicit cancellation token threading

## p0031: Orphan Detector Redis Scan
- [Architecture] `ConversationStateManager.GetAllAsync()` scans `conversation:*:*` Redis keys — detects stale states from before dispatcher restart
- [Implementation] Dual-path detection: in-memory tracked + full Redis scan — comprehensive coverage of all failure modes

## p0032: Architecture Cleanup
- [Architecture] `ILlmClient` abstraction wrapping LLM providers — generators become thin prompt-builders, can swap providers without code changes
- [Architecture] `CommandNames` + `PipelinePresets` code-defined — eliminates magic strings, single source of truth
- [TradeOff] Remove language-specific collection code → feed raw files to LLM — less code, more flexible, slightly higher token cost

## p0033: Run Cost in Result
- [Architecture] YAML frontmatter in `result.md` with cost breakdown by phase — human-readable and machine-parseable, persists in repo
- [Implementation] `RunCostSummary` moved to Contracts — Application layer can access without Infrastructure dependency

## p0034: Multi-Skill Architecture
- [Architecture] Commands insert follow-up commands into flat LinkedList pipeline — no tree structures, fully transparent in logs
- [Architecture] Role-based skills with separate YAML files — pluggable roles, new roles added without code changes
- [Architecture] `ConvergenceCheckCommand` evaluates consensus — prevents infinite discussion loops
- [Safety] Max 100 command executions limit in PipelineExecutor — prevents accidental infinite loops

## p0035: Simplified Slack Commands
- [Architecture] Split `FixTicket` into three commands: `FixBug`, `FixBugNoTests`, `AddFeature` — pipeline selection implicit in command type
- [Implementation] `ModalCommandType` enum routes to pipeline presets directly — no separate pipeline dropdown

## p0036: GenerateTests & GenerateDocs + Pipeline Resilience
- [Architecture] Per-command exception handling in PipelineExecutor prevents single command crash from failing entire pipeline
- [Architecture] GenerateTests and GenerateDocs execute synthetic plans via IAgentProvider instead of custom logic — reuses agentic loop
- [Architecture] OrphanJobDetector replaced time-based detection with container liveness checking (IsAliveAsync) — eliminates false positives from slow operations

## p0037: Strategy Pattern Pipeline Abstraction
- [Architecture] Chose strategy pattern with tell-don't-ask principle — no "if type == coding" anywhere, type resolves correct implementations via DI
- [Architecture] Single class size limit of 120 lines enforced to prevent god objects and improve maintainability
- [Architecture] Renamed pipeline steps (CheckoutSource → AcquireSource, Test → Validate) despite migration burden — reflects true semantics across all pipeline types

## p0038: MAD Discussion Pipeline
- [Architecture] SkillsPath moved to ProjectConfig instead of hardcoded path — enables loading from subdirectories for different discussion types
- [Architecture] CompileDiscussionCommand writes discussion transcript instead of executing code steps — reuses existing ConvergenceCheck consolidation

## p0039: Legal Analysis Pipeline
- [TradeOff] MarkItDown preprocessing over direct PDF to Claude — accepted token overhead for searchability and consistency across document formats
- [TradeOff] Inbox polling over FileSystemWatcher — chose polling (more stable on Docker bind mounts) over event-based complexity
- [Architecture] LocalFolderSourceProvider and LocalFileOutputStrategy as Pro-only implementations — OSS receives only strategy interfaces

## p0040a: Contracts Extensions & ProviderRegistry
- [Architecture] ITypedProvider base interface for all typed providers — eliminates hardcoded factory switches via ProviderRegistry<T>
- [Architecture] AttachmentRef abstraction for all storage sources — supports multiple backends without coupling to specific implementation
- [Implementation] Config passed at construction time (via IOptions<T>) not at factory method call — cleaner DI composition

## p0041: Decision Log
- [Architecture] IDecisionLogger with nullable repoPath and tell-don't-ask pattern — handlers always log, implementation decides what to do
- [Implementation] FileDecisionLogger + InMemoryDecisionLogger split — coding pipelines write to file, non-repo pipelines skip silently
- [Architecture] Decisions captured at decision moment (GeneratePlan, AgenticExecute, Bootstrap) not post-processed — ensures accuracy

## p0042: Legal Analysis Pipeline Handlers
- [TradeOff] InboxPollingService uses copy-then-delete not move — idempotent on crash, file remains in inbox for retry on next poll
- [Implementation] Contract type detected via Scout (cheap Haiku call) to select correct legal skill subset

## p0043a: CI/CD Docker Publish
- [Tooling] Chosen multi-arch build (amd64 + arm64) with QEMU emulation — supports both x86 servers and Apple Silicon developers
- [TradeOff] Stayed on mcr.microsoft.com/dotnet/sdk:8.0 for Host despite larger image — needed for cloned repo build/test execution

## p0043b: Security Pipeline
- [Architecture] Chosen IPrDiffProvider interface over PR-specific methods — enables diff-only scan without full checkout
- [Tooling] SecurityScan pipeline preset reuses Triage + ConvergenceCheck from discussion pattern — no duplicated state management
- [Architecture] DeliverOutputCommand with IOutputStrategy keyed services (.NET 8) — pluggable output backends without factory methods

## p0043c: SARIF Output Strategy
- [Architecture] IPrCommentProvider injected separately into SarifOutputStrategy — decouples platform-specific comment posting from finding analysis
- [Implementation] Skill output requires structured format (file/line/severity/confidence) instead of prose — enables SARIF mapping without LLM re-parsing

## p0043d: Ollama Local Model Support
- [Tooling] OpenAiCompatibleClient shared between OpenAI and Ollama via composition not inheritance — avoids class hierarchy complexity
- [TradeOff] Structured text fallback when tool calling unavailable — preferred over forcing minimum model capability
- [Implementation] ModelAssignment extended with ProviderType and Endpoint (both nullable) — backward compatible with existing single-provider configs
- [Architecture] Startup validation pings Ollama endpoint and detects tool calling capability — fails fast with clear error messages

## p0043e: Webhook Expansion
- [Architecture] Replaced monolithic WebhookListener with IWebhookHandler dispatch pattern — eliminates large switch statement, enables platform handlers in isolation
- [Architecture] SecurityScanRequest as common mapping target across all platforms — decouples platform-specific payloads from execution logic

## p0044: Rename ProcessTicket → ExecutePipeline
- [Architecture] Renamed ProcessTicketUseCase to ExecutePipelineUseCase — reflects that system orchestrates arbitrary workflows, not just tickets

## p0045: API Security Scan Pipeline
- [Architecture] Reused TriageHandlerBase and SkillRoundHandlerBase from p0043b instead of duplicating — established pattern for cascading commands
- [Tooling] Chose Nuclei over manual HTTP scanning — handles authentication, redirects, and HTTP semantics that would require custom crawling

## p0046: CLI Refactor & LLM Intent Parsing
- [Architecture] CLI changed to explicit flags (agent-smith fix --ticket 42) instead of free-text parsing — self-documenting and extensible
- [Tooling] Replaced regex-based intent parsing with Haiku LLM parsing for Slack — handles natural language variations, eliminates pattern fragility
- [Architecture] Collapsed ExecuteTicketAsync/ExecuteTicketlessAsync/ExecuteInitAsync into ExecuteAsync(PipelineRequest) — single code path regardless of source
- [Implementation] Program.cs split from 456 to <30 lines — each command handler under 120 lines, extracted Banner and ServiceProviderFactory

## p0047: API Contract & Schema Analysis
- [Architecture] Spectral runs as second static analyzer alongside Nuclei — complements mechanical scan with structural validation
- [Tooling] api-design-auditor rewritten with full schema analysis — semantic layer that tools cannot reach (sensitive data bundling, enum opacity, REST semantics)
- [Implementation] Spectral findings feed same skill pipeline as Nuclei — unified analysis despite different scanner types

## p0048: Swagger Context Compression
- [Architecture] Compression happens in handler (ApiSkillRoundHandler.BuildDomainSection) not as pipeline command — formatting concern, not workflow step
- [TradeOff] Deduplication + schema reference strategy over raw swagger.json — accepted 85% token reduction over complete fidelity

## p0049: Tool Runner Abstraction
- [Architecture] IToolRunner abstraction with multiple implementations (Docker/Podman/K8s/Process) — single interface handles all deployment modes
- [TradeOff] Spawners hardcode container paths (currently hardcoded, improved in p0051) — simplifies path handling despite DockerToolRunner complexity

## p0050: Multi-Output Strategy
- [Architecture] --output accepts comma-separated values, multiple IOutputStrategy implementations run once — avoids duplication but enables customized output
- [Implementation] SummaryOutputStrategy for clean findings view — filters skill discussion noise, shows only retained findings grouped by severity
- [Implementation] OutputDir resolved once in handler not per-strategy — eliminates per-strategy fallback chains

## p0051: ToolRunner Clean Architecture
- [Architecture] ToolRunRequest uses {input}/{output} placeholders resolved per-runner — logical I/O decoupled from execution model paths
- [Implementation] Each runner translates placeholders to its model (/work for Docker, tempdir for Process, /work for K8s) — clean separation of concerns
- [TradeOff] No trimming for single executable — reflection-heavy dependencies (YamlDotNet, Docker.DotNet) make trimming unstable

## p0052: Single Executable Release
- [Tooling] PublishSingleFile + SelfContained without trimming — ~70-80MB binaries acceptable, avoids reflection breakage
- [Architecture] Config file discovery chain (.agentsmith/, ./config/, ~/.agentsmith/) — no hardcoded paths, works across deployment contexts
- [Implementation] Docker entrypoint script fixes mount permissions and drops to agentsmith user — eliminates permission issues without requiring consumer setup

## p0053: Documentation Site
- [Architecture] MkDocs + Material theme over custom solution — reduces maintenance burden, leverages community defaults
- [Architecture] README trimmed to landing page, docs site as authoritative source — solves discoverability and information architecture split
- [Architecture] GitHub Pages deployment over managed hosting — free, version-controlled docs that stay with the code

## p0054: Security Scan Expansion
- [Architecture] Deterministic tools (regex, git history, dependency auditing) precede LLM skills — improves recall on pattern-based findings, reduces token cost while maintaining context awareness
- [Tooling] YAML pattern files are user-extensible without code changes — enables users to add project-specific patterns without rebuilding
- [Tooling] Separate tool commands (StaticPatternScan, GitHistoryScan, DependencyAudit) over monolithic "scanner" — each can be skipped independently based on repo type
- [TradeOff] Pattern-based pre-scanning accepted higher false positives over perfect recall — LLM specialists later filter and contextualize

## p0055: Security Findings Compression
- [Implementation] Top-N detail + remainder summary per category — reduces token cost 70%+ while keeping findings accessible to relevant skills
- [Architecture] Category-based slicing to skills instead of full findings dump — each skill only receives data it can act on
- [TradeOff] Compressed threshold (≤15 findings = full detail) over uniform compression — small categories skip compression to preserve context

## p0056: Security Scan Polish
- [Implementation] Mandatory false-positive-filter always runs regardless of triage — ensures consistency, prevents missed filtering
- [Architecture] ExtractFindingsCommand unifies security-scan and api-scan delivery paths via SARIF — consistent output format across pipelines
- [Tooling] SecretProviderRegistry maps patterns to revoke URLs without live API probing — actionable for developers without runtime risk

## p0057a: Skill Standard
- [Architecture] Three-file skill structure (SKILL.md + agentsmith.md + source.md) over single YAML — separates portable skill content from Agent-Smith extensions and provenance
- [TradeOff] Local skills require source.md with origin=local — enforces explicit knowledge of whether skills are internal or external

## p0057b: Skill Manager Pipeline
- [Implementation] Human approval mandatory before skill installation — treats skills as code injection, no automated installation
- [Architecture] SKILL.md never modified, extensions in agentsmith.md only — preserves upstream skill integrity, enables future updates
- [TradeOff] Pinned versions in source.md over automatic updates — requires explicit review cycle for new skill versions

## p0057c: Autonomous Pipeline
- [Architecture] Agent writes tickets, human decides whether to act — inverts control: agent prioritizes, human validates rather than human tasking agent
- [Implementation] Three-part ranking from convergence (not exhaustive list) — avoids ticket spam, forces prioritization through multi-skill agreement
- [TradeOff] project-vision.md must exist before autonomous runs — human knowledge about what matters cannot be inferred from code alone

## p0058: Interactive Dialogue
- [Architecture] Typed DialogQuestion (Confirmation, Choice, FreeText, Approval, Info) over binary Yes/No — supports nuanced human input without proliferating new adapters
- [Implementation] Dialogue trail always in result.md, never lost — audit trail of all agent-human interaction tied to the run
- [TradeOff] Timeout defaults to sensible value rather than infinite wait — CLI/PR workflows don't block forever without human
- [Implementation] Agent has ask_human tool with system prompt rules about when to ask — prevents overuse while allowing genuine clarifications

## p0058b: Microsoft Teams Integration
- [Implementation] Adaptive Cards for all 5 QuestionType variants — Teams-native UI that preserves same dialogue model across all adapters
- [TradeOff] Removed old AskQuestion method entirely once all adapters migrated — cleaner contract, forces consistency

## p0059: PR Comment as Input
- [Architecture] Unified webhook dispatch pattern (IWebhookHandler) handles both GitHub Issues and PR Comments — one entry point scales to multiple event types
- [Implementation] CommentIntentRouter distinguishes new job vs. dialogue answer at parse time — same Redis infrastructure serves both scenarios
- [Architecture] Platform-agnostic CommentIntent model, platform-specific webhook handlers — enables p0059b/p0059c without changing routing logic
- [TradeOff] Sparse checkout + no container job for PR context vs. full agent container — trade implementation simplicity for PR-specific context loading

## p0059b: GitLab MR Comments
- [Implementation] GitLab webhook handler added, CommentIntentRouter reused — confirms separation of platform-specific handling from intent logic

## p0059c: Azure DevOps PR Comments
- [Implementation] AzDO webhook handler added, CommentIntentRouter reused — demonstrates platform extension pattern scales

## p0060: Security Pipeline Enhancements
- [Architecture] DAST via ZAP after static tools, not integrated into static — runtime testing reveals issues invisible in code, separate tool appropriate
- [Implementation] Auto-fix spawns separate K8s jobs, doesn't block security scan — security findings delivered immediately, fixes continue asynchronously
- [TradeOff] Git-based trend analysis over external database — leverages YAML frontmatter + git history, no persistence layer
- [Implementation] Trend committed to default branch, not PR branch — accumulated knowledge stays project-wide, avoids merge conflicts

## p0061: Project Knowledge Base
- [Architecture] wiki/ compiled incrementally by LLM from runs/ + security/ — Karpathy pattern: no embeddings, no vectors, human-readable markdown
- [Implementation] CompileKnowledge respects rate limiting (default 1x/hour) — prevents token waste on frequent small runs
- [Architecture] QueryIntent requires @agent-smith prefix — prevents collision with ticket descriptions that start with questions
- [Implementation] Wiki queries use sparse checkout of .agentsmith/wiki/ only — 5-15 seconds instead of minutes, no container job needed
- [TradeOff] Decisions compiled from multiple runs, not just latest — institutional memory accumulates; contradictions detected by WikiLint

## p0062: Docs Site Update
- [Implementation] Docs updated after p0058–p0061 complete — ensures documentation describes implemented features, not planned ones

## p0063: Structured Finding Assessment
- [Implementation] All findings reach skills (remove Top-N cap), compression changes formatting not visibility — every finding gets at least one-line assessment
- [Architecture] Finding record gains ReviewStatus (not_reviewed | confirmed | false_positive) — tracks agent assessment, enables filtering
- [Implementation] Convergence produces JSON assessments alongside prose — structured output that actually flows into DeliverFindings
- [TradeOff] Accepted ~3-4k token increase per skill call vs. hidden findings that skills never see — visibility trumps compression

## p0064: Typed Skill Orchestration
- [Architecture] Pipeline type declared (discussion | structured | hierarchical) → determines orchestration model — one model doesn't fit all
- [Implementation] SkillGraphBuilder replaces LLM-based Triage for structured/hierarchical — deterministic execution graph from skill metadata, no LLM call needed
- [Architecture] Contributors receive only their input_categories slice, run in parallel independently — 80% token reduction vs. free-form discussion
- [Implementation] Gate role produces structured JSON (confirmed | rejected) instead of free-text discussion — enables filtering to actually affect output
- [TradeOff] Fallback to discussion model (contributor role) for skills without orchestration block — backward compatible, new pipelines get defaults

## p0065: Website Redesign
- [Architecture] CSS-only redesign, no template changes — isolates visual changes from site structure
- [Implementation] Shadow-as-border (1px rgba border) over CSS borders — aligns with Vercel/Geist aesthetic while maintaining simple HTML
- [TradeOff] Three font weights only (400/500/600) over full spectrum — constrains design but reduces font loading, enforces visual hierarchy

## p0066: Docs Enhancement — Self-Documentation & Multi-Agent Orchestration
- [Architecture] DESIGN.md placed in docs/ not project root — it is a docs-site concern, not product code
- [Tooling] CSS-only theme overrides via extra_css, no custom MkDocs templates — keeps MkDocs upgrades safe
- [TradeOff] Content first, styling second — missing content is a blocker, imperfect styling is not
- [Implementation] Reuse existing fix-and-feature.md instead of creating separate fix-bug.md — page already covers both pipelines

## p0067: API Scan Compression & ZAP Fix
- [Architecture] Category slicing (auth/design/runtime) instead of finding compression — findings are already compact at ~90 chars/piece, compression would lose information. Slicing routes findings to the right skill without data loss.
- [Tooling] WorkDir as optional ToolRunRequest parameter instead of Docker volume mounts — volume mounts would add complexity to DockerToolRunner. WorkDir + tar extraction to / is simpler and backward compatible (Nuclei/Spectral unaffected).
- [Implementation] Inject target URL into swagger servers[] instead of pinning ZAP version — ZAP needs absolute URLs, many OpenAPI specs only have relative "/". Patching the spec before copy is non-invasive.
- [TradeOff] Remove --auto flag entirely instead of finding replacement — --auto was never a valid option on ZAP's Python wrapper scripts. The scripts are non-interactive by default in Docker containers.
- [Implementation] Skip DAST skills on ZAP failure via ZapFailed flag — avoids wasting 2 LLM calls on empty input. Flag is checked in ApiSecurityTriageHandler before building the skill graph.

## p0068: API Finding Location
- [Architecture] DisplayLocation as computed property on Finding record — no new field in serialization, just display logic. Fallback chain: ApiPath > SchemaName > File:StartLine.
- [TradeOff] Nullable fields instead of separate ApiFinding subtype — keeps one Finding type across all pipelines. Security-scan findings simply leave ApiPath/SchemaName null.
- [Implementation] NullIfEmpty normalization in ParseGateFindings — LLMs return empty strings instead of omitting fields. Normalize at parse time and defend in DisplayLocation with IsNullOrWhiteSpace.

## p0070: Decision Log — Phase/Run Context
- [Architecture] sourceLabel as section header instead of category — the real lookup key is "which phase made this decision", not "what kind of decision was it". Category preserved as inline tag.
- [TradeOff] Optional parameter instead of new overload — keeps backward compat, callers that don't know their phase context still work with category-only sections.
- [Implementation] Ticket ID as sourceLabel in pipeline handlers — GeneratePlanHandler and AgenticExecuteHandler pass #{ticketId} so run decisions land under their ticket.

## p0072: Jira Assignee Webhook Trigger
- [Architecture] Handler returns WebhookResult(TriggerInput, Pipeline) instead of enqueueing jobs directly — follows existing dispatch pattern where WebhookListener calls ExecutePipelineUseCase. Phase doc proposed IJobEnqueuer; actual codebase delegates execution to the Listener.
- [Architecture] ServerContext record for config path DI — handler needs config for assignee matching and label→pipeline resolution, but IWebhookHandler.HandleAsync has no configPath parameter. Introduced ServerContext(ConfigPath) registered at server startup, injected into handler. Minimal surface, no interface changes.
- [Security] Secret configured + signature header missing → reject — phase doc originally returned true (skip), which would let attackers bypass verification by omitting the header. Fixed to return false.
- [Implementation] Config-order priority for label→pipeline resolution — iterate PipelineFromLabel keys (user-defined order) instead of payload labels (Jira-side order is undocumented and non-deterministic). Gives users explicit control over priority.
- [Implementation] WebhookListener extracts webhookEvent from Jira payload, strips "jira:" prefix → eventType for CanHandle — Jira has no event-type header unlike GitHub/GitLab. Listener parses once before dispatch.
- [TradeOff] Jira signature validation loads config per request instead of caching — keeps validation consistent with runtime config changes. Acceptable cost for webhook traffic volume.
- [Scope] Unassign scenario (Agent Smith removed from ticket) explicitly out of scope — handler returns Ignored correctly, but cancelling a running job requires Job Cancellation feature that doesn't exist yet.

## p0077: Pipeline Fixes
- [Architecture] skills_path resolved relative to config file directory — same pattern as tsconfig.json, docker-compose.yml. Config file is the anchor point, not CWD or repo root. Covers: local scan (repo has its own skills), API scan (no repo), CI with separate config repo (Sample.CICD/tools/agent-smith/config/).
- [Fix] ZAP exit codes 0-3 are valid scan results (pass/info/warnings/failures), not errors — only >3 indicates a tool crash. Previously any non-zero was treated as failure, discarding valid DAST findings.
- [Fix] Docker cp directories created with mode 0777 — ZAP container runs as UID 1000 (zap user), but docker cp creates files as root. World-writable permissions fix the PermissionDenied on /zap/wrk/zap-report.json.

## p0073: Class Size Enforcement
- [Quality] CI gate starts non-blocking, flip to blocking after Tier 1+2 — allows incremental adoption without breaking builds.
- [Scope] Pure refactoring only (extract, rename, split — no behavior changes) — ensures no regressions from size enforcement work.

## p0074: CLI Source Overrides
- [Architecture] Three generic options (--source-type, --source-path, --source-url, --source-auth) replace --repo — covers all provider types uniformly.
- [Architecture] Applied in ExecutePipelineUseCase (transparent to downstream) — handlers don't need to know about CLI overrides.
- [Cleanup] SecurityScanCommand drops --repo — replaced by generic source options.

## p0075: Phase Docs to YAML
- [Architecture] YAML over markdown — structured input yields structured output, ~10x token reduction (96k → 19k words).
- [Scope] No code examples in phase specs — agent reads codebase directly, examples become stale.
- [Scope] Type signatures kept, action field supports single value or list.
- [Scope] Delete original markdown files — git history is the archive.

## p0076: Azure OpenAI Provider
- [Architecture] Inheritance over composition — only CreateChatClient differs from OpenAiAgentProvider.
- [Configuration] Only 'deployment' and 'api_version' added to AgentConfig — api_version defaults to 2025-01-01-preview.
- [Tooling] Azure.AI.OpenAI NuGet (official Microsoft package).

## p0078: Gate Category Routing
- [Architecture] Filter before gate, merge after — each gate sees only its input_categories, results are combined.
- [Architecture] Unmatched findings pass through unchanged — categories not claimed by any gate are not lost.
- [Architecture] No gate ordering dependency — gates in same stage run independently on disjoint slices.

## p0081: Integration Setup Docs
- [Scope] All guides in English, under docs/setup/ as dedicated section.
- [Scope] Slack guide review/update, Teams guide is new (Azure Bot registration, ngrok, manifest, docker-compose env vars).
- [Scope] Webhook and label-trigger docs split to p0082.
- [Decision] Manifest template under docs/setup/teams/ (not deploy/).
- [Decision] Teams status marked 'beta' in chat-gateway.md.
- [Decision] No placeholder PNGs — document icon requirements in text instead.

## p0083: Jira Webhook Status Lifecycle
- [Architecture] Trigger requires all three: assignee match + label match + status in whitelist — prevents accidental runs on resolved/closed tickets.
- [Architecture] Comment trigger (comment_created) is a separate handler, not a mode in JiraAssigneeWebhookHandler — separate event type, cleaner routing.
- [Architecture] Comment trigger also checks status whitelist — prevents triggering on closed tickets.
- [Architecture] done_status resolved via transition name, not status ID — names are human-readable and portable across Jira projects.
- [Architecture] All pipelines are valid trigger targets — pipeline_from_label maps to any pipeline name, no hardcoded restrictions.
- [Reuse] Status transition after PR extracts TransitionToAsync from JiraTicketProvider.CloseTicketAsync — reusable for any target status.
- [Flow] DoneStatus flows via WebhookResult.InitialContext → PipelineContext → CommitAndPRHandler — no coupling between webhook handler and pipeline handler.
- [Note] PipelineFromLabel uses Dictionary<string,string> — insertion order preserved by YamlDotNet but not spec-guaranteed. Migrate to OrderedDictionary<TKey,TValue> when moving to .NET 9.

## p0082: Webhook & Trigger Docs
- [Scope] Per-platform setup guides under docs/setup/webhooks/ — each with prerequisites, step-by-step, config YAML, verification, troubleshooting.
- [Scope] Label-triggers overview documents current state per platform and p0084 roadmap for unified configuration.
- [Decision] Guides link to each other and to configuration reference (webhooks.md) — no duplication of config details.

## p0085: Webhook Structured Dispatch
- [Architecture] Webhook handlers return ProjectName + TicketId instead of free-text TriggerInput — WebhookRequestProcessor builds PipelineRequest directly, bypassing RegexIntentParser.
- [Architecture] PR comment handlers still use TriggerInput string (free-form arguments) — legacy path preserved as fallback.
- [Fix] ConsolidatedPlan is input for GeneratePlanHandler, not a replacement — multi-skill discussion provides analysis context, GeneratePlanHandler distills concrete PlanSteps. Previously the handler short-circuited with 0 steps, causing the agent to spend 32 iterations guessing what to do.
- [Fix] Jira Cloud system webhooks don't send signature headers — signature validation now skips when no header present, even if secret is configured.

## p0084: Unified Webhook Lifecycle
- [Architecture] WebhookTriggerConfig as shared base for all platforms — status gate, pipeline-from-label, done_status, comment re-trigger unified across GitHub, GitLab, Azure DevOps, Jira.
- [Architecture] Hardcoded trigger labels/tags replaced by config with backward-compatible defaults — existing setups work without changes.
- [Architecture] All platforms support pipeline_from_label — any pipeline is a valid trigger target, no hardcoded restrictions.

## p0086: Typed Skill Observations
- [Architecture] SkillObservation as universal output contract — replaces free-text DiscussionLog, pipeline-agnostic structured data.
- [Architecture] ID assigned by framework, not LLM — prevents hallucinated or colliding IDs.
- [Architecture] ConvergenceResult replaces ConsolidatedPlan string — mechanical plan generation from structured input.
- [Architecture] GeneratePlanHandler always runs — ConsolidatedPlan is context for plan generation, not a replacement.
- [Implementation] Concern, Severity, Effort as enums with JsonStringEnumConverter — type-safe, serialization-friendly.
- [TradeOff] Partial valid JSON accepted — take valid observations, skip broken ones with warning. Robustness over strictness.

## p0079/p0080: Attacker-Perspective Security Skills
- [Architecture] Commodity tools (StaticPatternScan, GitHistoryScan, DependencyAudit) are passive layer — unchanged. Intelligence layer (LLM attacker skills) is active layer on top.
- [Architecture] Attacker-perspective skills (recon-analyst, low-privilege-attacker, idor-prober, input-abuser, response-analyst) complement knowledge-domain skills — both run together.
- [Architecture] chain-analyst is executor — receives all commodity + skill findings and produces chained attack assessment.
- [Scope] No runtime, no user accounts, no HTTP probing — code/diff analysis only.

## p0087: Ticket Image Attachments
- [Architecture] IAttachmentLoader per platform — keeps TicketProvider focused on ticket CRUD, image downloading is separate concern.
- [Implementation] Only image MIME types (png, jpeg, gif, webp) — skip PDFs, ZIPs. Max 5MB per image, skip larger with warning.
- [Implementation] Images stored as base64 in PipelineContext via ContextKeys.Attachments — passed as vision input to LLM during plan generation.
- [TradeOff] Markdown parsing for GitHub/GitLab (![](url) patterns) over API-based attachment listing — issues embed images inline, not as separate attachments.

## p0088: Configurable Defaults
- [Architecture] Config wins over hardcoded — every value that depends on the user's environment must be readable from agentsmith.yml.
- [Architecture] Ticket states as whitelist (IN clause) instead of blacklist (<> exclusions) — unknown states excluded by default. Blacklists in foreign systems are a bug machine.
- [Architecture] PR target branch: repo API → config → "main" fallback chain. API result cached per run — one extra API call, not per PR.
- [Architecture] Missing ADO fields map to null, not errors — AcceptanceCriteria may not exist in all process templates.
- [Architecture] GitLab base URL required for self-hosted — no silent fallback to gitlab.com. Startup validation error with clear message.
- [TradeOff] ADO API version via env var (AZDO_API_VERSION) instead of config file — env vars are standard for server-side overrides, config file would be over-engineering for a rarely changed value.

## p0089a: Skill Content Improvements
- [Architecture] Confidence calibration defined once in observation-schema.md — Low (0-30), Medium (31-69), High (70-100). Referenced by all skills.
- [Architecture] Framework-specific false-positive rules derived from Anthropic's claude-code-security-review — 12 precedents battle-tested across thousands of reviews.
- [Architecture] Phase 1 repo context exploration before analysis — skills must understand existing security patterns before flagging findings. Deviations from established patterns are more likely real findings.
- [Scope] Content only — zero C# changes, zero build risk.

## p0092: K8s Config Cleanup
- [Architecture] Flat numbered YAMLs over Kustomize — GitOps repos (ArgoCD) apply plain YAML directly; Kustomize/Helm can be derived from flat files if needed, not the other way around
- [Architecture] Dev/prod differences as inline comments (e.g. `# prod: 2`) instead of overlay patches — this is a reference deployment, not a production GitOps repo
- [Implementation] Shell script over Makefile for ConfigMap regeneration — project has no Makefile, standalone script in deploy/k8s/ is more discoverable
- [Implementation] Two placeholder projects (GitLab+Claude, AzureDevOps+AzureOpenAI) — covers provider combos not already shown in agentsmith.example.yml (which defaults to GitHub+Claude)
- [Implementation] Pricing moved under agent block in example.yml — matches current real config structure where pricing is provider-specific, not project-level
- [Scope] Config/infra only — zero C# changes, zero build risk

## p0093: LLM Output Error Handling
- [Architecture] Silent catch banned for LLM-output parsing — every parse failure logs the response and returns Fail. Silent pass hid unreliable gates; a scan could report "647 raw → 647 extracted" while the gate had actually failed to parse.
- [Architecture] One corrective retry in the gate path (`GateRetryCoordinator`) — cheap (one extra LLM call worst case), recovers most schema drift. Retry failure fails the pipeline rather than silently letting findings through, because an unfiltered scan masquerading as filtered is worse than an explicit failure.
- [Architecture] Gate (output: list) must declare `input_categories` explicitly — `*` for wildcard or a concrete list. Empty/missing is rejected at skill load. Gate (output: verdict) is exempt because it doesn't filter findings.
- [Architecture] Validation throws at load time, skill-loader logs as Error — invalid skills don't silently vanish from the role set; the error is visible before the pipeline runs.
- [Scope] LLM-output parsing only — infrastructure cleanup catches (Docker, Process, file fallbacks) are legitimately best-effort and stayed unchanged. Non-gate parsers (Consolidation, Wiki) got logging on their existing fallbacks, not behavioral changes, because their fallback paths are semantically valid (degraded text, empty dict).

## p0094a: Gitignore-Aware Source Enumeration
- [Architecture] LibGit2Sharp's `Repository.Ignore.IsPathIgnored` is the ignore source of truth when scanning a git repo — nested `.gitignore`, `.git/info/exclude`, and global excludes all covered without a hand-rolled gitignore parser.
- [Architecture] Hardcoded `ExcludedDirectories` list shrunk but kept as a non-git fallback — `.git/`, `node_modules/`, `bin/`, `obj/`, `__pycache__/`, `.vs/`, `.idea/` stay for CLI scans of arbitrary local paths; `dist/`, `build/`, `vendor/`, `packages/` removed because real repos gitignore them anyway.
- [Architecture] Binary-extension filter stays orthogonal to gitignore — `.gitignore` doesn't mark `.png` as binary, and many repos correctly track binary assets. Two independent reasons to skip a file.
- [Implementation] macOS `/var` → `/private/var` symlink fallback in `GitIgnoreResolver.ToRelative` — realpath canonicalization inside LibGit2Sharp vs. caller-supplied `/var/...` paths breaks direct `Path.GetRelativePath`; a targeted `/private/` prefix strip handles the common test/scratch case without introducing a P/Invoke dependency.
- [Scope] Enumeration only — no changes to pattern definitions, scanner, or findings model. Observable effect limited to fewer files reaching the scanner for repos that gitignore their build output.

## p0094b: Security-Scan Skill Reduction (15 → 9)
- [Architecture] Attacker-perspective skills (vuln-analyst, recon-analyst, response-analyst, low-privilege-attacker, input-abuser, idor-prober) deleted from `config/skills/security/` — in a code-audit context without HTTP probing, their prompt output overlapped with the knowledge-domain skills (auth-reviewer, injection-checker, config-auditor, compliance-checker). The same skills remain in `config/skills/api-security/` where HTTP probing and persona-based testing give them genuinely distinct capabilities.
- [Architecture] idor-prober replaced by two independent mechanisms: (1) auth-reviewer's SKILL.md scope extended to explicitly cover IDOR/BOLA (sequential IDs, ownership predicates, cross-tenant, bulk-ops); (2) new `config/patterns/auth.yaml` with 4 static patterns (ASP.NET int route constraint, EF Find-by-id, LINQ single-predicate ID lookup, raw SQL where-id-only). LLM covers nuance, static patterns catch deterministic signals cheaply.
- [Architecture] Legacy `SkillCategories` dictionary in `SecurityFindingsCompressor` removed — since p0093 made `orchestration.InputCategories` authoritative, the fallback branch was unread code for every skill with proper input_categories. With the dict gone, an undeclared Contributor skill now returns an empty slice, which is the correct behaviour (gets the overall findings summary instead).
- [Architecture] `GetSliceForSkill` gained explicit wildcard handling — `input_categories: ["*"]` now concatenates every category slice, matching the documented p0093 semantics. Before this change the gate reached for `categorySlices["*"]` (never present) and silently returned empty; the overall summary was a hidden fallback that masked the bug.
- [Scope] Baseline and comparison numbers recorded in the phase spec as decision keys: 15-skill baseline wallclock=4m15s, post-gate=14 findings; 9-skill run wallclock=3m (29% faster, target ≥25% met), post-gate=10. Severity-weighted variance is -18% (outside the ±15% done criterion), attributable to LLM gate-judgement variance on pattern-definition files rather than real coverage loss — both runs mostly filter `config/patterns/*.yaml` regex samples, and each run keeps a different subset. Follow-up candidate: exclude `config/patterns/*.yaml` from self-scans.
- [TradeOff] ±15% severity-weighted tolerance was documented as a hard done criterion but proved tighter than single-run LLM determinism supports. Rather than re-run until we hit the target, the variance is recorded honestly with root-cause analysis; future coverage criteria should either average over multiple runs or set a wider tolerance.

## p0095a: Ticket-Claim Spine (GitHub only)
- [Architecture] Split the original p0095 "ticket-claim-lifecycle" phase into p0095a (this), p0095b (all-platform transitioners), and p0095c (heartbeat + reconciler + full config). Reason: the original p0095 bundled ~27 new prod files + 4 heterogeneous platform API integrations + hosted services + config schema into one phase — too large to commit incrementally. Splitting gives each sub-phase a testable, committable increment. Documented in the three phase YAMLs; p0096 (poller) now `requires: [p0095a, p0095b, p0095c]`.
- [Architecture] Introduced IRedisJobQueue (RPUSH/LPOP FIFO on `agentsmith:queue:jobs`) as the durable handoff between webhook receiver and worker. Webhook no longer runs pipelines fire-and-forget in-process. Before p0095a this path didn't exist — the existing ServerMode invoked `ExecutePipelineUseCase` directly from the webhook handler, which had no backpressure and no crash recovery. The queue is ephemeral (ticket status is the truth); recovery lands in p0095c.
- [Architecture] IRedisClaimLock uses SETNX for acquisition and a CAS Lua script for release (returns the acquirer token; release only deletes if the stored value matches). Prevents a caller whose lock TTL expired from deleting a lock subsequently re-acquired by another process. Simple Redis DEL without CAS was considered and rejected — the failure mode it enables (releasing a foreign lock during clock skew or GC pause) is subtle enough to warrant the Lua.
- [Implementation] ConsumeAsync polls via LPOP + 1s delay instead of BRPOP. StackExchange.Redis's IDatabase doesn't expose BRPOP directly; raw ExecuteAsync would work but adds complexity. Polling matches the pattern already used by RedisMessageBus and keeps the queue code under 80 lines. Latency impact: ~500ms worst case on queue entry → visible to consumer, acceptable given pipeline wall time dominates.
- [Architecture] PipelineQueueConsumer is a plain class with RunAsync(CancellationToken), not a `BackgroundService`. The Cli `server` command isn't built on `IHost` (it uses a direct ServiceProvider), so BackgroundService would require introducing IHost just for this consumer. Matches WebhookListener's pattern; both are awaited via Task.WhenAll inside ServerCommand. When/if Cli migrates to IHost in the future, this becomes a BackgroundService with one line of DI.
- [Architecture] QueueConfig lives on the root AgentSmithConfig (not per-project ProjectConfig.Agent), because the queue key is process-wide and max_parallel_jobs caps pipelines across all projects on one pod. Per-project tuning would give wrong semantics (a busy project would unfairly hog slots).
- [Architecture] Platform-specific routing of webhooks-to-ClaimService is a string check `result.Platform == "GitHub"` in WebhookRequestProcessor. For p0095a, GitLab/AzDO/Jira keep their in-process ExecuteStructuredAsync fire-and-forget path. p0095b will flip them over once their transitioners exist. Documented explicitly in the WebhookRequestProcessor class summary so the legacy branch isn't mistaken for dead code.
- [TradeOff] PipelineRequest moved from AgentSmith.Application/Models to AgentSmith.Contracts/Models. The queue interface lives in Contracts (infrastructure implements; application consumes), and accepting PipelineRequest forces it into the Contracts layer. Touched ~14 files' `using` directives — small one-time churn, correct layering.
- [Architecture] GitHub transitioner uses If-Match ETag on PATCH /issues/{n} with a full labels-array replacement. GitHub's actual ETag enforcement on issue PATCH is documented but not guaranteed to 412 consistently in practice; the code handles 412 correctly if it comes, otherwise accepts last-write-wins semantics. p0095c's full lifecycle config + reconciler limits the blast radius of a lost transition.
- [Scope] ITicketClaimService interface takes AgentSmithConfig as a method parameter (not via DI). The caller (WebhookRequestProcessor) already loads config each request for other reasons; forcing the service to load it adds ConfigPathHolder plumbing without benefit. Keeps the service stateless and easy to unit-test.
- [Scope] PR-comment webhook path (TriggerInput, dialogue answers) is explicitly untouched in p0095a. Spec calls it out. Those flows don't own ticket lifecycle and don't benefit from claim semantics. Migrating them is a separate concern, not a sub-phase of p0095.

## p0095b: Multi-Platform Status Transitioners
- [Architecture] GitLab uses PUT /issues/{iid} with add_labels/remove_labels rather than full-labels replacement. Targets only the lifecycle label, leaves unrelated labels untouched. No ETag on GitLab issues — concurrency is last-write-wins at the platform level; TicketClaimService's SETNX claim-lock carries the primary race guard. Full-label PUT was considered and rejected — concurrent edits to non-lifecycle labels would be clobbered.
- [Architecture] AzureDevOps uses JSON Patch on /workitems/{id} with an explicit `test /rev` operation before the `add /fields/System.Tags` — AzDO returns 412 (PreconditionFailed) on rev mismatch. System.Tags is a semicolon-separated list; we read-filter-add-serialize. 409 Conflict treated as PreconditionFailed too (AzDO occasionally returns that for concurrent modifications).
- [Architecture] Jira label-mode uses an *additional* SETNX label-lock (`agentsmith:jira-label-lock:{ticketId}`, 10s TTL) on top of the global claim-lock that TicketClaimService already holds. Reason: Jira's PUT fields.labels is not atomic — two concurrent PUTs from the same pod could both succeed and clobber each other within the claim-lock's validity window if the window straddled a GET/PUT pair. The label-lock narrows the atomic critical section specifically to the label mutation. p0095a's single-lock pattern was considered and rejected for Jira specifically.
- [Architecture] JiraWorkflowCatalog ships in p0095b as a skeleton — always returns Label-mode regardless of project. Native probing (GET /rest/api/3/project/{key}/statuses) waits for p0095c when LifecycleConfig introduces per-project status name customisation; probing before that would force native-status mapping into the p0095b scope. Structure is in place (ConcurrentDictionary cache, `SetModeForTest` internals) so p0095c only flips the probe logic.
- [Scope] WebhookRequestProcessor's `UsesClaimService(platform)` GitHub-only check was removed. All structured webhooks with both ProjectName and Platform set now go through ITicketClaimService — the dead fire-and-forget `ExecuteStructuredAsync` method was deleted. Pre-p0095a behaviour is no longer reachable; the legacy TriggerInput path (PR comments) stays untouched as documented in p0095a.
- [Scope] ITicketProvider.ListByStatusAsync moved from p0095b to p0095c. It's a reconciler dependency, not a transitioner dependency — keeping it with the reconciler (p0095c) makes the p0095b YAML redundant with the multi-platform transitioner focus and gives p0095c the full "recovery" surface area in one coherent phase.

## p0095c: Lifecycle Recovery
- [Architecture] IJobHeartbeatService exposes both `Start(ticketId) → IAsyncDisposable` and `IsAliveAsync(ticketId)`. The second method keeps StaleJobDetector and EnqueuedReconciler in AgentSmith.Application (which doesn't reference StackExchange.Redis). Without IsAliveAsync they'd need direct IDatabase access, forcing them into Infrastructure and breaking the layering. Cost: one extra interface method; benefit: correct layering and tests don't need Redis mocks.
- [Architecture] ListByLifecycleStatusAsync defaults to empty on ITicketProvider. p0095c ships the GitHub impl (GetAllForRepository with label filter). GitLab/AzDO/Jira keep the default — reconciler becomes a no-op for them until a follow-up phase adds the listing implementations. This is incremental delivery: the recovery infrastructure is in place and exercised for GitHub, other platforms land when their implementations do.
- [Architecture] PipelineExecutor.BeginLifecycleAsync + LifecycleScope (`IAsyncDisposable`) wrap execution with Enqueued→InProgress transition + heartbeat start on entry, and InProgress→Done/Failed + heartbeat stop on disposal. `MarkFailed()` toggles the disposal target status. Scope.Noop is used when no TicketId is in context (manual CLI runs). The disposal runs transitions with CancellationToken.None because the scope must complete even when the pipeline was cancelled — we want the ticket marked Failed in the cancellation case, not left in InProgress.
- [Architecture] StaleJobDetector + EnqueuedReconciler run unconditionally on every replica in p0095c. Leader-election is deferred to p0096 (which introduces the lease primitive for the poller anyway). Duplicate detector/reconciler work is cheap because all transitions are atomic per platform — whichever replica wins sees the updated status and the others no-op on their next scan.
- [Scope] Full LifecycleConfig schema (status_labels, reconciler_interval_minutes, heartbeat_ttl_seconds) deferred to a follow-up. p0095c uses hardcoded intervals (heartbeat 30s/2min TTL, stale scan 1min, reconcile 10min) that match p0095a's implicit defaults. LifecycleConfig + Jira native-mode probing are naturally paired — they land together when someone wants per-project lifecycle label customisation.
- [Scope] OpenTelemetry counters deferred to the same follow-up. Structured logging at Information level covers the observability needs for a small team; metrics make sense when a platform operator team wants dashboards. Adding `System.Diagnostics.Metrics.Meter` is a small additive change when requested.
- [TradeOff] Hosted services (consumer, stale detector, reconciler) are plain classes with RunAsync(ct), not BackgroundService — matches p0095a's PipelineQueueConsumer decision. Cli has no IHost. Task.WhenAll in ServerCommand composes them. When/if Cli migrates to IHost, conversion is mechanical.

## p0096: Event Pollers
- [Architecture] Two independent Redis leases: `agentsmith:leader:poller` and `agentsmith:leader:housekeeping`. A single replica can hold both; having them separate means polling can go leader-elected without disturbing housekeeping timing, and vice versa. Same `LeaderElectedHostedService` runs both with different keys.
- [Architecture] LeaderElectedHostedService loop: TryAcquire → while leader { work, renew, check } → on renewal failure cancel work, release, back to try-acquire with 5s idle backoff. Ensures exactly-one-leader under Redis's TTL+CAS semantics.
- [Architecture] RedisLeaderLease uses CAS Lua for both RENEW (PEXPIRE only if stored value matches token) and RELEASE (DEL only on match). Prevents a GC-paused or network-partitioned leader from extending or deleting a lease the new holder acquired after the TTL elapsed — the classic "Redlock lite" pattern.
- [Architecture] Housekeeping services (StaleJobDetector + EnqueuedReconciler from p0095c) migrated from unconditional-per-replica to leader-only. Reduces N-fold API calls; correctness was already there via atomic transitions, but the extra calls were wasteful. ServerCommand's RunHousekeepingAsync runs both services in one Task.WhenAll under the housekeeping leader.
- [Architecture] PollerHostedService fan-outs pollers via Task.WhenAll with a 20s per-poller timeout. One slow platform does not block the others. Exceptions caught per poller and logged; one failure does not poison the cycle. Claims from all pollers are then processed sequentially — ClaimAsync is fast (few hundred ms worst case), and sequential processing keeps Redis lock contention predictable.
- [Architecture] IEventPoller reads via ITicketProvider.ListByLifecycleStatusAsync(Pending) — same dependency the reconciler uses. p0096 ships the GitHub poller only; other platforms follow when their ListByLifecycleStatus implementations land (same incremental pattern as p0095c).
- [Architecture] Polling opt-in per project via PollingConfig (default Enabled=false). Webhook-only deployments see no behaviour change. Disabled projects are skipped at BuildPollers time in ServerCommand — no null pollers flow into PollerHostedService.
- [Scope] Parallel-across-platforms, sequential-across-claims. Parallelism knob for ClaimAsync dispatch was considered but rejected — claim is O(ms) unless a platform API is slow, and semaphore complexity here is not worth a few hundred ms.
- [Scope] Jitter is per-cycle (±10%) applied after Task.WhenAll. Per-project jitter (spec option) was simpler at this scope — one randomised sleep at the end of each cycle, minimum across configured intervals.
- [Scope] Leader lease renewal interval 10s, TTL 30s. 3x safety margin matches the p0095c heartbeat pattern; if the process GC-pauses or clock-skews for up to 20s we still renew before the TTL hits.

## p0098: Docs Catch-Up for p0084 + p0095a/b/c + p0096
- [Architecture] `concepts/ticket-lifecycle.md` is the single canonical state-machine page. Setup and configuration pages link to it rather than restating the diagram, so future changes land in one place. Recovery semantics (StaleJobDetector, EnqueuedReconciler, what-survives-what) live there too — they're concept material, not setup steps.
- [Architecture] Polling docs split into two pages: `setup/polling.md` (config + ops + troubleshooting, action-oriented) and `setup/polling-vs-webhooks.md` (decision matrix, scenario-oriented). Operators reaching the docs from "I need to enable polling" land on the first; operators reaching from "should we use polling here?" land on the second. Single combined page tested in draft and felt overloaded.
- [Implementation] Polling page leads with a `!!! warning "GitHub-only at runtime"` admonition. Other docs (label-triggers, polling-vs-webhooks, agentsmith-yml) repeat the limitation in tables. Cost: redundancy. Benefit: an operator who skims a single page still sees the constraint — silent failure ("polling enabled, no claims") is the failure mode this guards against.
- [Architecture] `setup/README.md` reorganised into "Ticket Ingress" with Webhook + Polling as siblings, instead of the previous Webhook-only listing with Polling absent. Decision matrix lives in the comparison page; README only routes to it. Keeps README scan-friendly.
- [Scope] `architecture/index.md` got two table rows (Claim-then-Enqueue, Leader Election) in the Patterns section; `architecture/layers.md` got expanded service tables. Did not touch the layer diagram or the dependency-flow text — those still reference the old Host/Dispatcher names (renamed to Cli/Server in p0069) and should be a separate cleanup phase. Mixing layer-rename in this phase would dilute the docs catch-up message.
- [Scope] PR-comment webhook path (free-form TriggerInput, dialogue answers) explicitly called out in `webhooks.md` as separate from the lifecycle. Avoids confusion when an operator notices PR comments don't get `agent-smith:enqueued` labels.
- [Scope] Single commit `docs: catch up to p0084 + p0095a/b/c + p0096`. Splitting per-page would create intermediate docs states where one page describes the claim flow and the next still says "webhook → ExecutePipelineUseCase direct". Docs are read coherently or not at all.

## p0101: CLI server graceful degradation + per-subsystem health
- [Architecture] Resilience strategy split: webhook listener always up, Redis-dependent subsystems (queue_consumer, housekeeping, poller) degrade. Server boots with REDIS_URL unset OR Redis temporarily down. Webhook POSTs that hit ITicketClaimService while Redis is down respond 503 with body `redis_unavailable` instead of throwing. Operators see /health/ready=503 immediately — no silent-no-op state.
- [Architecture] ConnectionMultiplexer built with `AbortOnConnectFail=false`, `ConnectRetry=3`, `ConnectTimeout=5000ms`. Multiplexer reconnects automatically when Redis comes up. Single-line change vs old `ConnectionMultiplexer.Connect(url)`; no behaviour change in the happy path. Verified by `Build_RedisUnreachable_StillBuildsServiceProvider` test (asserts no throw within 15s for non-routable URL).
- [Architecture] Redis-dependent service registrations (`IRedisJobQueue`, `IRedisClaimLock`, `IRedisLeaderLease`, `IJobHeartbeatService`, `IConversationLookup`, `IMessageBus`, `IDialogueTransport→RedisDialogueTransport`) moved out of `AgentSmith.Infrastructure.ServiceCollectionExtensions` into `AgentSmith.Cli.ServiceProviderFactory.RegisterRedis`, gated on whether `IConnectionMultiplexer` was registered. Rationale: the existing `if (queue is null) return Task.CompletedTask` graceful-skip pattern in ServerCommand only works if those services are NOT in the container when Redis isn't available. Spec called for an `AddAgentSmithCommands(bool redisAvailable)` overload but `services.Replace(ServiceDescriptor.Scoped<ITicketClaimService, NullTicketClaimService>())` keeps Application clean and is less invasive.
- [Architecture] /health is liveness — HTTP 200 as long as the listener is alive (status field reflects degradation in the body). /health/ready is readiness with a strict loud-fail rule: ANY subsystem in non-Up state (Down, Degraded, Disabled) returns 503. Liveness vs readiness split keeps Kubernetes/Docker restart loops sane (don't kill the pod just because Redis is briefly down) while still surfacing the issue to readiness gates and alerting. Body lists every subsystem with name/state/reason/last_changed_utc so operators see WHY at a glance.
- [Architecture] ISubsystemHealth contract surface stays tiny: `Name`, `State` (Up/Degraded/Down/Disabled), `Reason`, `LastChangedUtc`. Each long-running subsystem owns a `SubsystemHealth` instance (mutable thread-safe impl in `AgentSmith.Application/Services/Health/SubsystemHealth.cs`). WebhookListener iterates the injected `IReadOnlyList<ISubsystemHealth>` to build the report. No reflection, no central registry — just DI plus an explicit list passed to the listener constructor.
- [Implementation] `SubsystemHealth` lives in `AgentSmith.Application/Services/Health/`, not `Contracts/Services/` as the spec sketched. Coding-principles say `Contracts/` is for interfaces only — placing the mutable impl in Application keeps Contracts pure. Spec deviation logged here.
- [Architecture] Subsystem retry loop (`SubsystemTask.RunRedisGatedAsync`): if `TService` is unregistered → SetDisabled, return (no point looping for a config that will never become true — distinguishes Disabled from Down/Degraded). If registered but multiplexer disconnected → SetDegraded("waiting for Redis"), poll IsConnected every `RedisRetryIntervalSeconds`. Once connected → SetUp, run inner work. On work error → SetDegraded("task error: {msg}"), re-enter retry loop. ct cancellation exits cleanly without retrying.
- [Implementation] NullTicketClaimService returns `ClaimResult.Failed("redis_unavailable")` rather than `ClaimResult.Rejected(...)`. The existing `ClaimRejectionReason` enum carries semantics "rejected claims will never succeed as-is — operator must fix config", which technically fits "operator must set REDIS_URL", but Failed-with-string-error is closer to "transient" and avoids growing the enum. `MapClaim` in WebhookRequestProcessor pattern-matches on `Failed when Error == "redis_unavailable"` → 503; everything else stays 500.
- [Implementation] WebhookRequestProcessor pre-checks `IsRedisAvailable()` (looks up the `redis` ISubsystemHealth from DI) before structured-claim dispatch and dialogue routing. Returns 503 + `redis_unavailable` immediately if redis state is not Up. Cheaper than try/catch on DispatchAsync and catches the case where Redis is configured but disconnected at the moment of webhook arrival.
- [Implementation] ServerCommand.BuildPollers extracted to `Cli/Services/PollerFactory.cs`. Reason: ServerCommand was already at the upper class-size limit; adding subsystem-health plumbing pushed it over. Extraction also models BuildPollers as a real responsibility (platform-switch poller construction) rather than a private detail of the command. Tests updated to call `PollerFactory.Build` directly.
- [Implementation] LeaderSubsystemRunner extracted (`Cli/Services/LeaderSubsystemRunner.cs`) to collapse the identical plumbing for `housekeeping` and `poller` subsystems (both: redis-gated → leader-elected → run inner work). ServerCommand orchestration shrinks from a duplicated pair of `StartXxxLeaderAsync` methods to a single `LeaderSubsystemRunner.RunAsync` call per subsystem.
- [Scope] `AgentSmith.Server` (Slack/Teams gateway) keeps its current strict `depends_on:redis` setup. It is a different deployment unit with stronger Redis coupling (Slack signing secrets, conversation state, dispatcher queue). Making it resilient is a follow-up phase. p0101 is strictly about the CLI `server` command.
- [Scope] WebhookListener integration tests via real HttpListener not written. The HTTP endpoint behaviour is covered by `HealthResponseBuilderTests` (JSON shape + status codes for all /health and /health/ready cases) plus the listener constructor wiring is verified by `ServiceProviderFactoryRedisGatingTests`. Manual smoke test documented in done criteria. End-to-end HttpListener tests would buy little extra confidence at significant complexity cost.
- [Scope] `WebhookRequestProcessor_StructuredWebhookRedisDown_Returns503` end-to-end test not written for the same reason — would require constructing a valid platform-specific webhook payload that survives WebhookPlatformDetector + signature verifier. The 503 mapping is unit-tested via `NullTicketClaimServiceTests` (returns Failed("redis_unavailable")) plus the static `MapClaim` mapping in WebhookRequestProcessor is straightforward. Trade-off documented; full-stack coverage available via the manual smoke test in done criteria.
- [Implementation] Logging cadence: INFO on subsystem state transitions (started, disabled, waiting for Redis, restored). WARN on task error. Goal: operator's tail always shows the last-event-state without flooding during outages.
- [Architecture] Null-Redis implementations (`AgentSmith.Application/Services/RedisDisabled/`) for `IRedisJobQueue`, `IRedisClaimLock`, `IRedisLeaderLease`, `IJobHeartbeatService`, `IConversationLookup`. Registered by ServiceProviderFactory in the no-REDIS_URL branch alongside NullTicketClaimService. Reason: p0101's initial pass only fixed the `server` command — `security-scan`, `fix`, `mad`, etc. crashed at DI resolution time because TicketStatusTransitionerFactory (constructor) and PipelineExecutor (LifecycleScope) inject Redis services transitively. With null impls, every CLI command resolves cleanly without Redis. Methods that genuinely need Redis (claim acquisition, queue enqueue, heartbeat start) throw `RedisUnavailableException` with an actionable message ("set REDIS_URL"). Methods with safe defaults (queue depth, heartbeat aliveness, conversation lookup) return 0/false/null. Verified by smoke-running `security-scan --project agent-smith` to skill-round stage without Redis configured.
- [Implementation] SubsystemTask gating switched from `GetService<T>() is null` to `redis ISubsystemHealth.State == Disabled`. Null impls always resolve to non-null instances, so the old null-check would never fire. The redis subsystem health (registered by RegisterRedis as Disabled when REDIS_URL is unset) is now the single source of truth for "Redis configured?". Test `RunRedisGatedAsync_RedisHealthDisabled_SetsDisabledAndReturns` updated accordingly.
- [TradeOff] NullRedisLeaderLease.TryAcquireAsync returns null silently (lease not acquired) rather than throwing. Reason: leader election is server-only — CLI commands inject the dep transitively but never invoke it. A throwing impl would be loud-fail in dead code paths. The server uses the redis ISubsystemHealth check via SubsystemTask before leader election even starts, so the null lease is never invoked in server mode either when REDIS_URL is unset.

## p0097: Parallel Skill Rounds
- [Architecture] DeferredBuffers context key as the channel between handler base and executor. SkillRoundHandlerBase always builds a `SkillRoundBuffer`; if the executor put a list under `ContextKeys.DeferredBuffers` before dispatching the batch, the handler appends to it and returns; otherwise it applies the buffer immediately (sequential default). Public `ICommandHandler.ExecuteAsync` signature unchanged — no new interface, subclass handlers (SkillRoundHandler/SecuritySkillRoundHandler/ApiSkillRoundHandler) stay one-liners. Alternatives considered: (a) new `ISkillRoundCollector` returning `(CommandResult, SkillRoundBuffer)` — adds plumbing to every handler; (b) explicit flag in PipelineContext queried by handlers — same idea, more verbose. Picked the channel approach because presence/absence IS the signal.
- [Architecture] Observation IDs assigned at merge time, not parse time. `ObservationParser.ParseWithoutIds` returns `Id=0` for every entry; `ApplyBufferToContext` reads the runtime list, computes `nextId = Max(o.Id) + 1`, and stamps each buffer entry under the executor's serial merge order. Parallel completion order does NOT affect IDs — graph order does. Cross-run ID stability is NOT claimed (LLMs vary observation count run-to-run); the contract is "deterministic within a single run."
- [Architecture] `PipelineBatchRunner` extracted from PipelineExecutor as a separate class (composition over fat orchestrator). Reason: PipelineExecutor was already over the class-size limit before parallelism arrived; adding fan-out + cancellation + buffer-merge would push it well past 500 lines. Extraction also models the runner as a real responsibility (parallel execution under throttle) instead of a private detail. PipelineExecutor now owns main loop + sequential single-step path + lifecycle; the runner owns SemaphoreSlim, linked CTS, and merge.
- [Architecture] `BatchOutcome` value object replaces a tuple-of-arrays return shape from the runner. The two queries operators care about post-batch — first failure (fail-fast reporting) and first InsertNext (sequential follow-up insertion) — sit on the outcome as named methods. Keeps PipelineExecutor's batch handler short and the intent legible.
- [Implementation] Buffer merge in graph order, not completion order. `MergeBuffersInGraphOrder` walks the original `LinkedListNode<PipelineCommand>` list and looks each skill name up in a `Dictionary<string, List<SkillRoundBuffer>>` built from the deferred list. Tasks may complete in any order, but merge order is fixed by the topology.
- [TradeOff] CancellationToken cancels orchestration but NOT in-flight LLM calls. Once the SemaphoreSlim releases and the inner `commandExecutor.ExecuteAsync` is past the point of return, Polly retries can still fire and Anthropic responses run to completion. Cost is recorded for every settled call (cancelled task's cost is tracked before the cancellation throws), so the cost report stays trustworthy. Operators should expect close-to-full batch cost even when one slot fails. Documented because "CTS = stop everything" is a reasonable misread.
- [TradeOff] `MaxCommandExecutions` (100) counts each command in the batch once. A 13-skill stage burns 13 of 100 in one parallel burst — same total as today's sequential path, just consumed in fewer wall-clock seconds. Acceptable for now; revisit if security-scan pipelines with nested OBJECTIONs trip the guard.
- [Implementation] PipelineCostTracker switched from `Interlocked.Add`/`Increment` to a `lock`-based design. `Interlocked` covered the int counters but `_lastModel` write was already racy and `EstimateCostUsd`/`ToString` read multiple fields non-atomically. One lock simplifies the model and the contention window is negligible relative to the LLM call.
- [Implementation] PipelineContext `Dictionary<string, object>` wrapped under a single lock. The deferred-buffer pattern means most parallel writes go through the locked `List<SkillRoundBuffer>` in DeferredBuffers, but handlers still call `pipeline.Set(ContextKeys.ActiveSkill, name)` during execution. ConcurrentDictionary was considered but would force callers using `Get<T>` to handle missing-key differently. Single lock keeps the surface identical.
- [Architecture] Parallelism opt-in via `agent.parallelism.max_concurrent_skill_rounds` (default 1). Sequential path is byte-identical to pre-p0097: same logging, same execution trail, same observation IDs given identical LLM output. Verified by full test suite (1089 → 1105, +16 new tests) all green at default.
- [TradeOff] Logging stays per-command in batch mode (each slot logs its own `[{Step}/{Total}] Executing ...` and `... completed`). With 13 parallel slots this can interleave on stdout — tolerable in CLI mode, potentially noisy on Slack/Teams progress streams. Spec flagged this for verification; no batch-aware progress event added in p0097. Revisit if UX feedback on production runs (with `MaxConcurrentSkillRounds=4`) shows actual flooding.
- [Scope] InsertNext semantics on batch: first-in-graph-order InsertNext wins. If skills A and B in the same batch both return InsertNext, only A's follow-ups are queued. Conservative choice — alternatives (concatenation, voting) considered too clever for the typical use case (OBJECTION from one skill triggering a single targeted re-round). Documented so future-self doesn't reinvent it.

## p0102: API Scan Code-Aware + Prefix Caching

- [Architecture] `ILlmClient.CompleteWithCachedPrefixAsync(systemPrompt, userPrefix, userSuffix, taskType, ct)` added as an optional default-method overload. Providers without prompt caching (OpenAI, Azure OpenAI, Gemini, Ollama) inherit the default that joins the two halves and falls through to `CompleteAsync`. `AnthropicLlmClient` overrides to send the user message as two `TextContent` blocks with `cache_control = ephemeral` on the prefix only. Alternative considered: a separate `ICachedLlmClient` interface — rejected because it would force two paths through `SkillRoundHandlerBase`/`GateRetryCoordinator` for every call.
- [Architecture] Prompt-prefix split lives in a new `PromptPrefixBuilder` service that owns the boundary "what's stable across same-round calls vs. what varies per skill." `SkillPromptBuilder` gained `BuildDiscussionPromptParts` / `BuildStructuredPromptParts` returning `(system, prefix, suffix)`; the existing single-string methods now delegate to the parts API and re-join. Subclasses opt into caching by overriding `SkillRoundHandlerBase.BuildDomainSectionParts(pipeline)` — default returns `(BuildDomainSection(pipeline), "")`, so existing `SkillRoundHandler` and `SecuritySkillRoundHandler` keep their old behavior with no edits.
- [Architecture] `ApiSkillRoundHandler.BuildDomainSectionParts` puts the swagger spec, code context, summary, and probe results in the stable half; mode line, per-skill findings slice, and per-skill source excerpts go in the variable half. The active-skill name is the only prompt input that differs across the same round, so per-skill code excerpts are scoped to the active role (auth-config-reviewer / ownership-checker / upload-validator-reviewer) — keeps the prefix identical across the cohort.
- [Architecture] `GateRetryCoordinator.ExecuteAsync` signature changed from `(systemPrompt, userPrompt, …)` to `(systemPrompt, userPromptPrefix, userPromptSuffix, …)`. Both attempts (initial + retry) reuse the same prefix; only the suffix grows the corrective-prompt block. Cache hit on retry is preserved.
- [Architecture] `EvidenceMode` extended with a third value `AnalyzedFromSource` (in addition to `Potential` and `Confirmed`). `Finding.DisplayLocation` was tweaked to prefer `File:StartLine` when the mode is `AnalyzedFromSource`, even if `ApiPath` is also set — code-evidence findings should always surface their file:line. Console / Markdown / SARIF formatters render a three-way badge.
- [Architecture] `ApiCodeContextHandler` runs after `LoadSwagger` and before `SessionSetup` in the `ApiSecurityScan` preset. When `ContextKeys.SourcePath` is unset, it sets `ApiSourceAvailable = false` and returns immediately — the rest of the pipeline behaves identically to pre-p0102. When source is present, it walks the tree once via `IRouteMapper`, `IAuthBootstrapExtractor`, and `IUploadHandlerExtractor`, then exposes the populated `ApiCodeContext` for downstream skills.
- [Architecture] `RouteMapper` walks the source tree once and accumulates declarations per-framework. `FrameworkRoutePatterns` carries per-language file-extension scoping (`.cs` for .NET, `.js/.ts` for Express, `.py` for FastAPI, `.java/.kt` for Spring) so the patterns can't cross-match each other (an early test failure: `@app.post(...)` in a `.py` file matched the Express regex). Path canonicalization replaces `:param` (Express) and `{param}` (everything else) with `{}` so swagger paths and source paths compare correctly.
- [Architecture] Confidence scoring on `RouteHandlerLocation`: exact method+path match → 1.0, path match with method mismatch → 0.5, no path match → not in list. Decision-locked threshold for downstream skills is 0.5 — entries below that are kept in `RoutesToHandlers` (so the run output can log them as "low confidence") but skills must not emit findings against them. Implemented in `ApiSkillRoundHandler.BuildPerSkillCodeSection` and codified in `api-security-principles.md`.
- [Architecture] `ApiSecurityTriageHandler` was over the 120-line class limit, so the gate landed as three classes: `ApiSecurityTriageHandler` (orchestration, ≤60 lines), `ApiSecurityTriagePromptBuilder` (signal + LLM prompt assembly), `ApiSecuritySkillFilter` (mode + source-availability gate). `Filter()` returns `{recon-analyst, anonymous-attacker, false-positive-filter, chain-analyst}` for passive+no-source, drops `auth-config-reviewer / ownership-checker / upload-validator-reviewer` whenever source is absent, and otherwise returns the input list unchanged. Active-mode-capable contributors (idor-prober, low-privilege-attacker, input-abuser, response-analyst) keep their existing degrade-in-passive behavior outside the passive+no-source branch.
- [Architecture] `config/patterns/api-auth.yaml` carries 8 deterministic patterns (the four `ValidateXxx = false` JWT toggles, CORS `AllowAnyOrigin + AllowCredentials` combo, `[AllowAnonymous]` adjacent to a state-changing verb, hardcoded JWT signing keys, exception-message-in-response). LLM skills cover the nuance; static patterns catch the unambiguous bugs cheaply. Same split that p0094b applied to `auth.yaml`.
- [Implementation] The three new `IRouteMapper` / `IAuthBootstrapExtractor` / `IUploadHandlerExtractor` interfaces live in `AgentSmith.Contracts.Services`; their regex-based implementations live in `AgentSmith.Infrastructure/Services/Security/Code/`, alongside a small `SourceSnippetReader` helper for bounded-line-range reads. Roslyn-based mapping was deferred per spec decision (#19) — revisit if LLM under-confidence on nuanced cases is measurable.
- [Implementation] `agentsmith.example.yml` gained a sibling `api-security` project block (alongside `my-project`) with `parallelism.max_concurrent_skill_rounds: 4` and a `source.path` entry. The comment in the parallelism field explains *why* 4 is required (5-minute Anthropic cache TTL — sequential calls for 13 contributors take longer than the TTL window, so the cache misses on later skills). The example was added rather than editing `my-project` to keep the basic example minimal.
- [Implementation] CLI banner now prints `mode | source | ~N skill(s)`. Skill count is estimated locally in the CLI from `(active, source)` — kept as a static map rather than asking the triage handler, because triage runs per-pipeline and the banner fires before pipeline DI exists.
- [Scope] LLM-skill content tests (`AuthConfigReviewer_DeadAuthorizationMiddleware_Critical`, `OwnershipChecker_DbSetWithoutUserPredicate_FlagsIdor`, `UploadValidatorReviewer_HeaderOnlyMime_Medium`) check that `SKILL.md` mentions the keywords the spec calls out, rather than running the LLM. Full skill behaviour requires an integration run against a fixture repo; the spec calls those out in the done criteria as run-time evidence rather than unit tests.
- [Scope] Baseline and Comparison cost decision keys (per spec done-criterion) are NOT filled in this commit. Recording them honestly requires a real api-scan run against a fixture target with an Anthropic API key; that run produces the numbers and a follow-up commit appends them here, pinned to model ID + `MaxConcurrentSkillRounds` value (matching p0094b's pattern). Placeholder follow-up tracked below; do not interpret the absence as zero cost benefit.
- [TradeOff] `MaxConcurrentSkillRounds = 4` in the example config is the spec-required default for api-security-scan but is not the system-wide default. Operators running api-scan without copying the example config get the safe sequential path (cache miss on later skills, no exposure to parallel-call edge cases) — they only opt in once they read the comment. Trade: docs-as-default vs. behavior-as-default. Chose docs because the cache-TTL constraint is project-specific.

### Baseline / Comparison (deferred — to be filled by a follow-up commit)
- Baseline (without prefix caching, MaxConcurrentSkillRounds=4, Sonnet 4): TBD wall-clock, TBD tokens, TBD USD
- Comparison (with prefix caching, same config): TBD wall-clock, TBD tokens, TBD USD, TBD % of baseline


## p0102a: API Scan Source Checkout

- [Architecture] `TryCheckoutSourceHandler` mirrors `CheckoutSourceHandler` but is fail-soft. Two handlers with shared `ISourceProviderFactory` is clearer than one handler with a policy flag — security-scan keeps strict semantics (any clone failure is fatal), api-scan never fails the pipeline. The handler returns `Ok` in every branch; downstream `ApiCodeContextHandler` decides what to do based on whether `ContextKeys.SourcePath` is set.
- [Architecture] Single source-of-truth contract downstream: `ContextKeys.SourcePath`. Whether the path comes from `--source-path` (CLI), a configured Local source, or a fresh remote clone, every consumer reads the same key. `ContextKeys.Repository` (set by the strict `CheckoutSourceHandler`) is intentionally not introduced into the api-scan flow — keeps one mechanism, one read site.
- [Architecture] Branch resolution differs from the strict `CheckoutSourceContextBuilder`. api-scan has no `TicketId` in scope, so `BranchName.FromTicket` does not apply and `ContextKeys.CheckoutBranch` (set by webhooks) is also ignored. Branch comes from `SourceConfig.DefaultBranch` only; if unset, the provider's default ('main') wins. Read-only inspection — checking out a ticket-scoped branch would be wrong here.
- [Implementation] Auth presence is not pre-checked from `SourceConfig.Auth`. `SourceProviderFactory.CreateGitHub` (and the GitLab/AzureRepos siblings) ask `SecretsProvider.GetRequired("GITHUB_TOKEN")` which can resolve via env vars, secret stores, or any other registered source. A naive `if (string.IsNullOrEmpty(source.Auth))` guard would shadow valid auth paths. Instead the handler wraps `factory.Create(source) + provider.CheckoutAsync(...)` in try/catch and treats any failure (missing token, network unreachable, invalid URL, transient git error) the same way: log warning, fall back to passive mode.
- [Implementation] Banner moved to two phases. Pre-flight in `ApiScanCommand` shows mode + 'Resolving source...'; the final 'Source: <path or unavailable> | ~N skill(s)' is emitted by `TryCheckoutSourceHandler` via `ILogger.LogInformation` after each branch decides. `EstimateSkillCount` moved out of the CLI into the handler so the count reflects the actual resolved state, not the CLI flag's pre-flight guess. No event-subscriber plumbing — direct logger output is sufficient.
- [Architecture] Conditional execution lives in the handler, not in pipeline composition. `PipelinePresets.ApiSecurityScan` is a static list with `TryCheckoutSource` always first; the handler decides at runtime whether the source is configured/reachable and shortcircuits to passive mode if not. Dynamic step injection was considered (and discussed in spec) but rejected — it would have been a larger architectural change for no semantic gain in this case.
- [Implementation] Clone caching is provider-owned, not phase-owned. `ISourceProvider` implementations (GitHub/GitLab/AzureRepos) clone into `Path.GetTempPath()/agentsmith/<owner>/<repo>` and short-circuit re-clone via `LibGit2Sharp.Repository.IsValid`. Same persistent cache `security-scan` already uses; api-scan inherits the convention as-is. No new cleanup hook in `TryCheckoutSourceHandler` — disposal is the user's concern. Stale-cache risk (out-of-date local clone vs upstream) exists today and is not regressed by this phase.
- [Scope] `ApiCodeContextHandler` was not modified beyond a doc-comment refresh. The existing `TryResolveSourcePath` already reads `ContextKeys.SourcePath` and is agnostic to who set it — the handler reads the same key whether the value came from the CLI flag, the Local source resolution, or a remote clone result. One read site, three producers.

## p0106: Multi-Pipeline Projects

- [Architecture] `ProjectConfig.Pipelines: List<PipelineDefinition>` + `ProjectConfig.DefaultPipeline: string?` joined the existing `Pipeline` / `SkillsPath` fields (which stay as legacy shims, not a breaking change). Each `PipelineDefinition` carries a name plus optional overrides for `Agent`, `SkillsPath`, `CodingPrinciplesPath`. Eliminates duplicated project blocks — `config/agentsmith.yml` still has four `agent-smith-*` projects on the same repo, but new configs can collapse them into a single multi-pipeline project.
- [Architecture] Single resolver `IPipelineConfigResolver.Resolve(project, pipelineName) → ResolvedPipelineConfig`. ExecutePipelineUseCase resolves once at the top of every run and stashes the merged view under `ContextKeys.ResolvedPipeline`; downstream builders/handlers read it via the `pipeline.Resolved()` extension method — never directly from `ProjectConfig`. ~30 read sites for `project.Agent` / `project.SkillsPath` / `project.CodingPrinciplesPath` migrated; the resolver is the single merge function rather than 30 inline `?? project.X` fallbacks.
- [Architecture] Resolver tolerates undeclared pipeline names — returns a definition synthesized from project defaults. Reason: `init-project` (CLI bootstrap) and other "free-form" pipeline names that aren't in `pipelines:` still need to work. Strict "throw if not declared" was rejected because it broke `init` and made test ergonomics painful for `new ProjectConfig { Pipeline = "fix-bug" }`. Validation that *trigger* references resolve happens at config load instead — that's where misspelled names get caught.
- [Architecture] Skills-path fallback chain: `pipelineDef.SkillsPath` → `PipelinePresets.GetDefaultSkillsPath(pipelineName)` (`security-scan` → `skills/security`, `api-security-scan` → `skills/api-security`, `legal-analysis` → `skills/legal`, `mad-discussion` → `skills/mad`, fall-through `skills/coding`). Fixes the pre-phase asymmetry where 5 CLI subcommands set `SkillsPathOverride` from the pipeline name but webhook/polling paths fell back to `project.SkillsPath` (default `"skills/coding"`). The directory-naming convention is the binding; explicit `pipelines[].skills_path` is opt-in for non-standard paths.
- [Architecture] `ContextKeys.SkillsPathOverride` deleted. The 5 CLI subcommands (Autonomous/ApiScan/Mad/SecurityScan/Legal) no longer set it; the 4 context builders (EvaluateSkills/Discover/Install/LoadSkills under ApiSecurity) read from `ResolvedPipelineConfig.SkillsPath` only. The override was a workaround for the asymmetry above; resolver subsumes it. Two truth sources collapsed to one.
- [Architecture] `pipeline.Resolved()` extension method on `PipelineContext` (in `Contracts.Commands`) over per-site `pipeline.Get<ResolvedPipelineConfig>(ContextKeys.ResolvedPipeline)`. Reads the same well-known key but compresses the call site. Static extension method is one of the two patterns coding-principles allows (alongside `Map()` helpers).
- [TradeOff] Backward-compat via `ProjectConfigNormalizer` invoked from `YamlConfigurationLoader`, not a breaking change. Legacy `pipeline:` + `skills_path:` fields populate a synthetic single-element `Pipelines` list and `DefaultPipeline` at load time. Existing configs run unchanged. Forcing migration in the same phase that introduces the mechanism would have doubled blast radius and review surface — the migration of `config/agentsmith.yml`'s four `agent-smith-*` projects to a single multi-pipeline project is deferred to follow-up `p0106a`.
- [Architecture] `pipelines:` is a **sparse override list, not a strict allowlist** (revised after first deployment). Each entry declares per-pipeline overrides; pipelines without entries inherit project defaults. Labels in `pipeline_from_label` may route to ANY system pipeline (defined by `PipelinePresets`), not just declared ones. Original strict-allowlist design (introduced and then revised in the p0106 fix) broke legacy configs whose `pipeline_from_label` routed to multiple pipelines while only one was synthesized from `pipeline:`. The sparse-override semantics matches the user's mental model: declare overrides where you need them, leave the rest implicit.
- [Implementation] Legacy-shim merges instead of skip-if-non-empty. If `pipeline:` (legacy) is set AND `pipelines:` already has entries, the legacy entry is added (deduplicated by name) and `DefaultPipeline` is set from `pipeline:`. Lets users mix both forms: declare overrides for specific pipelines while keeping `pipeline: fix-bug` as the default. Resolver also synthesizes-on-the-fly when `Pipelines` is empty but legacy `Pipeline` is set, so direct `new ProjectConfig { Pipeline = "fix-bug" }` test setups don't need to call the normalizer manually.
- [Implementation] Load-time validation is narrow: only `ProjectConfig.DefaultPipeline` must reference a declared pipeline. `pipeline_from_label` values and trigger-level `default_pipeline` are NOT validated against the declared list — they may route to any system pipeline. Catching typos in those is the responsibility of `PipelinePresets.TryResolve` at runtime (returns null for unknown names). Earlier strict trigger validation was removed after a real-world config (`config/agentsmith.yml` `azuredevops_trigger`) failed to load.
- [Scope] `Parallelism` stays inside `AgentConfig`, not lifted to `PipelineDefinition`. Per-pipeline parallelism is achievable today by overriding the whole Agent block. Premature extraction violates YAGNI; if usage shows users want different parallelism with the same Agent, extract it then.
- [Implementation] Pipeline-selection chain when no explicit name is supplied: `--pipeline` flag (CLI only) → `ProjectConfig.DefaultPipeline` → single-element shortcut (`Pipelines.Count == 1`) → throw `InvalidOperationException` listing declared pipelines. Loader-shim sets `DefaultPipeline = legacy.Pipeline` when migrating, so single-pipeline configs preserve ergonomics. Distinct from `WebhookTriggerConfig.DefaultPipeline` — same idea, different scope (webhook label-routing fallback). `ExecutePipelineUseCase`, `EnqueuedReconciler`, `AzureDevOpsWorkItemPoller`, and `WebhookRequestProcessor` route through this chain; the previous hardcoded `"fix-bug"` fallback in the AzDO poller is gone.
- [Implementation] Behavior-preservation gate: `MultiPipelineBehaviorPreservationTests` loads an inline copy of the four `agent-smith-*` projects from `config/agentsmith.yml` through the full loader+normalizer+resolver pipeline and asserts the 16 baseline tuples (pipelineName, Agent.Type, SkillsPath, CodingPrinciplesPath) match byte-for-byte. Same shape as p0094b's `Baseline:` key. Any drift is a regression detectable at PR time.
- [Implementation] Server DI was missing `IPromptCatalog`, `IRedisClaimLock`, `IRedisJobQueue`, `IRedisLeaderLease`, `IJobHeartbeatService`, `IConversationLookup`, `IDialogueTransport` (latent bug pre-dating p0106 — uncovered by `ASPNETCORE_ENVIRONMENT=Development` enabling `ValidateOnBuild`). `Server.Extensions.ServiceCollectionExtensions.AddRedis` extended to register all Redis-gated services; `AddCoreDispatcherServices` registers `IPromptCatalog` + `IPromptOverrideSource` directly (not via `AddAgentSmithCommands` — that pulls in the entire Application layer's pipeline handlers, over-eager for the dispatcher's needs).
- [Implementation] `IAgentProviderFactory` registration changed from Singleton to Scoped to fix lifetime violation against Scoped `IDialogueTrail`. Factory is a stateless dispatch over a creator-dictionary; per-scope allocation is cheap and matches the natural per-conversation/per-pipeline-run scope. Pre-existing latent bug, also uncovered by `ValidateOnBuild`.

## p0107: Server Consolidation

- [Architecture] Webhook handling, polling, queue consumer, lifecycle reconcilers all relocated from `AgentSmith.Cli` (`server` subcommand) into `AgentSmith.Server`. Server is now the single long-running deployment; CLI is a pure console-tool for one-shot pipeline runs and ad-hoc commands. Pre-phase: dual-container K8s pattern (Dispatcher + CLI-with-`server`) was historical drift from p0014 (webhook listener added to CLI when Dispatcher rename came later in p0069) — never had architectural justification. Post-phase: one process, one Kestrel, one DI tree.
- [Architecture] `WebhookListener` (raw `HttpListener` wrapping its own HTTP server) deleted. Webhook routes register directly on Server's existing `WebApplication` via a new `MapWebhookEndpoints` extension alongside `MapSlackEndpoints` / `MapTeamsEndpoints`. One Kestrel hosts three endpoint groups; behavior-preservation maintained by routing the same `(path, body, headers)` signature into the same `WebhookRequestProcessor.ProcessAsync` flow.
- [Architecture] Three thin `BackgroundService` wrappers in `Server.Services.Hosting/` absorb the `Task.WhenAll` orchestration that previously lived in CLI's `ServerCommand`: `QueueConsumerHostedService` (PipelineQueueConsumer), `HousekeepingLeaderHostedService` (StaleJobDetector + EnqueuedReconciler under `agentsmith:leader:housekeeping`), `PollerLeaderHostedService` (PollerHostedService under `agentsmith:leader:poller`). Each exposes its `ISubsystemHealth` instance as a singleton for the `/health` endpoint. The Application-layer services themselves stayed put — only the *wiring* relocated.
- [Architecture] DI consolidates: Server's `ServiceCollectionExtensions` gains `AddWebhookHandlers()` (registers all 13 `IWebhookHandler` implementations) and `AddLongRunningServices()` (registers the three hosted services + their healths + RedisConnectionHealth). `AddCoreDispatcherServices` calls `AddAgentSmithCommands` so the full Application pipeline-execution tree (ExecutePipelineUseCase, IPipelineConfigResolver, ITicketClaimService, all command handlers) is reachable from the WebhookRequestProcessor. The patchwork-fix from p0106's deployment hotfix collapses into a coherent extension-method chain in `Program.cs`.
- [Implementation] CLI's `ServiceProviderFactory.Build` shrunk to two modes: interactive (Console-based dialogue + progress, default) and spawned-job (Redis-backed when both `jobId` and `redisUrl` are non-empty — the path used by K8s job containers spawned by the Dispatcher). The previous mode-switching factory `RegisterProgressReporter` and the Redis-or-Null fallback in `RegisterRedis` deleted; CLI no longer carries server-side concerns.
- [Implementation] Duplicate `src/AgentSmith.Server/Services/RedisProgressReporter.cs` (a 3-line `global using` shim) deleted; `src/AgentSmith.Infrastructure/Services/Bus/RedisProgressReporter.cs` is the canonical implementation. The shim was a leftover from a long-past rename — invisible cruft caught by the move audit.
- [Implementation] `ILlmClient` registration added to `AgentSmith.Application/Extensions/ServiceCollectionExtensions.cs` as a latent-gap fix exposed by the new `ServerDiLifetimeTests`. Several handlers (EvaluateSkillsHandler, CompileKnowledgeHandler, QueryKnowledgeHandler) take `ILlmClient` directly; pre-phase only `ILlmClientFactory` was registered. Test caught it because Server's full validate-on-build now exercises the path. Default `Claude` config used since these handlers are typically configured per-project at runtime — same defaulting pattern as `LlmIntentParser`.
- [Implementation] New `ServerDiLifetimeTests` regression-guards Server's full DI tree by mirroring `Program.cs`'s registration order and calling `BuildServiceProvider(ValidateOnBuild=true, ValidateScopes=true)`. Uses existing `Null*` Application implementations + Moq for `IConnectionMultiplexer` / `IDialogueTransport` / `IJobSpawner` / `IProgressReporter`. The test caught both the pre-existing `ILlmClient` gap and verified that p0106's `IAgentProviderFactory→Scoped` fix sticks. Future Server-DI changes that introduce lifetime violations or missing services fail this test at build time.
- [Implementation] `Program.cs` declares `public partial class Program;` at the bottom. Enables `WebApplicationFactory<Program>` in future integration tests (deferred — not in scope for this phase, but the marker is in place).
- [Scope] Detailed user-facing webhook docs (curl examples, port references) NOT updated since URL routes (`/webhook`, `/webhook/{platform}`) and HTTP semantics are byte-for-byte identical. `docker-compose.yml` collapsed: deleted the `agentsmith-server` service (CLI with `server` cmd), updated header comment. `deploy/k8s/9-ingress.yaml` is unchanged — its `path: /` Prefix already routes webhook URLs to the Server service. No second deployment needed.
- [Scope] `ServiceProviderFactoryRedisGatingTests` deleted — they tested CLI's Redis-or-null fallback, which is now Server's responsibility (the Server-side equivalent is `ServerDiLifetimeTests`). 1206 tests post-phase (was 1210; net -4 from the deleted CLI gating tests +4 from new resolver/server tests ≈ wash).

## p0109: Jira Lock Decorator

- [Architecture] `IRedisClaimLock` is a multi-writer-coordination concern, not a Jira-platform concern. Cross-process serialization is needed only in the Server cluster (multiple pods, poller + webhook racing on the same ticket). A single CLI process cannot race with itself, and parallel CLI invocations have no shared Redis to coordinate through anyway. The lock attaches at composition time via a decorator (`LockedTicketStatusTransitioner`) that the Server registers and the CLI does not — Jira's API atomicity gap (no If-Match on `fields.labels`) is real, but the *need* for the workaround is conditioned on the multi-writer reality, which is a deployment property, not a platform property.
- [Architecture] Decorator wraps Jira only, not all platforms. GitHub/GitLab use ETag/If-Match-equivalent semantics; AzDO uses `op:test` on `/rev` for optimistic concurrency at the API level. Wrapping them with the same lock would convert API-level retries into queueing — strictly worse. The decorator class accepts any `ITicketStatusTransitioner` as inner type, but the `LockingTicketStatusTransitionerFactory` only applies it to `config.Type == "jira"`.
- [Implementation] DI rebind extracted from `AddRedis()` into a standalone `AddJiraLabelLockDecorator()` extension method. Reason: real Server `Program.cs` calls `AddRedis().AddCoreDispatcherServices()`, but `AddCoreDispatcherServices` internally calls `AddAgentSmithInfrastructure` which re-registers `ITicketStatusTransitionerFactory→TicketStatusTransitionerFactory`. MS DI is last-wins for `GetService<T>`, so an in-`AddRedis` rebind would be silently overwritten by the subsequent infrastructure registration. Standalone method called *after* `AddCoreDispatcherServices` makes the override stick. Test fixture `ServerDiLifetimeTests` mirrors the same call order.
- [Implementation] `LockingTicketStatusTransitionerFactory` takes the **concrete** `TicketStatusTransitionerFactory` (not `ITicketStatusTransitionerFactory`). Reason: passing the interface would risk a self-referential cycle if the binding is rebound to Locking. Binding the concrete type explicitly via `sp.GetRequiredService<TicketStatusTransitionerFactory>()` makes the dependency unambiguous. Infrastructure registration adds `services.AddSingleton<TicketStatusTransitionerFactory>()` plus a forwarding lambda for the interface so resolves of either return the same instance.
- [TradeOff] `NullRedisClaimLock` deleted as a Pre-p0107 leftover — it was never wired in production, conflicting with p0107's "no Redis-fallback in CLI" decision. Doc-comment-claimed use-case ("manual security-scan, fix without ticket lifecycle inject the dep but never call these methods") was based on the assumption that `IRedisClaimLock` had to be in the CLI graph at all; this phase removes that need entirely. `ServerDiLifetimeTests` fixture rebased onto `Mock.Of<IRedisClaimLock>()`. Sibling `Null*` classes in the same directory (`NullRedisJobQueue`, `NullJobHeartbeatService`, etc.) left untouched — they're still used by the test fixture as DI test doubles, and removing them is out of scope.
- [TradeOff] `EnvVarCollection` xUnit collection introduced to serialize tests that mutate process-global env vars. Reason: new `LockingTicketStatusTransitionerFactoryTests` and `CliShapedDiTests` set `GITHUB_TOKEN`/`GITLAB_TOKEN`/etc. so the inner factory's `SecretsProvider` calls succeed; existing `FactoryTests.TicketProviderFactory_NewTypes_RecognizedButNeedSecrets` asserts the *opposite* (env unset → ConfigurationException). xUnit's default per-class parallelism caused races. All three classes now share `[Collection(EnvVarCollection.Name)]`, and the env-mutating ones implement `IDisposable` to reset to clean state. Cheaper than introducing a `SecretsProvider` abstraction across the codebase for a test-only seam.

## p0109a: Server-Lifecycle Decoupling

- [Architecture] Coordinator (not decorator) for the lifecycle wrap. Lifecycle wrapping in `PipelineExecutor` is more involved than the Jira lock that p0109 handled with a decorator: it transitions a status, holds a heartbeat handle across the whole run, and disposes with a deferred close-transition. Hiding all of that behind `IPipelineLifecycleCoordinator.BeginAsync(...) → IAsyncPipelineLifecycle` separates it cleanly from the executor's main loop, lets the CLI compose with `NoOpPipelineLifecycleCoordinator` (no Redis deps), and keeps the executor's ctor stable as new lifecycle concerns get added. Decorator pattern would have meant a wrapping `PipelineExecutor` — too coarse a unit; the wrap belongs *inside* the executor's run, not around it.
- [Architecture] `ITicketClaimService` registration moved from Application to Server. Unlike `PipelineExecutor` (which CLI uses for non-ticket pipelines like apiscan/security-scan), `TicketClaimService` is purely Server-instantiated: only `WebhookRequestProcessor` and `PollerHostedService` resolve it, both Server-side. Application-side registration meant the CLI's DI graph carried `IRedisJobQueue` + `IRedisClaimLock` + `IJobHeartbeatService` as transitive deps even though no CLI code path can reach the service. Move resolves the transitive leak completely.
- [Implementation] `AddJiraLabelLockDecorator` renamed → `AddServerCompositionOverrides`. The Server-side overrides on top of Application's CLI-safe defaults are now multiple: locking transitioner factory, ticket-aware lifecycle coordinator, ticket claim service. One method bundles them — explicit "this is what Server adds beyond Application's CLI-safe defaults". The discipline is: Application/Infrastructure always register the Redis-free default; Server adds overrides on top.
- [Implementation] `NoOpPipelineLifecycleCoordinator` is the Application default. Reason: an Application/Domain default that's *safe everywhere* is the inversion of the Pre-p0107 pattern (Null fallbacks were registered conditionally based on Redis availability). Now: Application registers the no-op, Server unconditionally overrides. The Application graph is always resolvable in any composition. Test `Cli_PipelineLifecycleCoordinator_ResolvesToNoOp` asserts the default is in place.
- [TradeOff] `NullTicketClaimService` deleted — same Pre-p0107 orphan pattern as `NullRedisClaimLock`. Only used by its own test, never wired in production. Doc-comment claimed "CLI server when Redis unavailable", which describes a fallback p0107 retired. With `ITicketClaimService` now Server-only, no CLI graph needs even an orphan dummy.
- [TradeOff] Real-CLI smoke test uses `AgentSmith.Cli.ServiceProviderFactory.Build` (the production composition) inside the test — `Cli_RealServiceProviderFactoryBuild_ResolvesPipelineExecutor`. p0109's test only covered a hand-rolled CLI-shape collection, which missed the `IJobHeartbeatService` gap because it never resolved `PipelineExecutor`. The real-factory test ensures any future divergence between the test fixture and production composition is caught: if production registers something the fixture forgets, the test still fails because it builds from production code.

## p0111c: Triage Rewrite & Phase-Based Pipeline

- [Architecture] **Strategy pattern for triage, not in-place rewrite.** `ITriageStrategy` with `LegacyTriageStrategy` (Discussion pipelines: legal/mad/init-project/skill-manager/autonomous) and `StructuredTriageStrategy` (fix-bug/add-feature/security-scan/api-scan). `TriageHandler` becomes a thin adapter that picks the strategy via `ITriageStrategySelector` based on `PipelineType`. Reason: the spec said "rewrite TriageHandler internals" but the legacy LLM-based discussion path is a load-bearing fallback for discussion pipelines that don't have skills migrated to the new frontmatter, so a clean strategy split is more honest than a single body with `if (pipelineType == Discussion) ...` branching. Selector is the single dispatch point so the existing `TriageCommand` wiring stayed untouched.
- [Architecture] **Model B for phase tagging — Triage emits Plan-phase commands inline; new `RunReviewPhaseCommand` and `RunFinalPhaseCommand` preset steps emit subsequent phases.** Considered Model A (PipelineExecutor iterates phases driven by preset metadata) — rejected because it would require deep changes to the executor's flat-command linked-list model, plus a new preset shape. Model B keeps the executor untouched and matches today's `OkAndContinueWith`/`InsertNext` pattern. `TriageOutput` is stored once in pipeline context; both `StructuredTriageStrategy` and `PhaseAdvanceHandler` re-read it. Phase boundaries are conveyed via `ContextKeys.CurrentPhase`, set by each phase-advance step before its commands fire. AgenticExecute remains a regular preset step between Plan and Review for `fix-bug` / `add-feature`.
- [Architecture] **One `PhaseAdvanceContext` + one `PhaseAdvanceHandler` for both `RunReviewPhase` and `RunFinalPhase`.** The two builders (`RunReviewPhaseContextBuilder`, `RunFinalPhaseContextBuilder`) bake the phase + round into the context; the handler stays generic. Avoided two near-identical handler classes; trade-off is that the context is shared across two command names which violates the conventional "one context type per command" pattern, but it cuts the new-handler count without losing clarity.
- [Architecture] **Deleted: `SkillGraphBuilder`, `DeterministicTriageBuilder`, `ISkillGraphBuilder`, `SkillGraph`, `ExecutionStage`, `SecurityTriageHandler`, `ApiSecurityTriageHandler`, `ApiSecurityTriagePromptBuilder`, `ApiSecuritySkillFilter`, `TriageHandlerBase`, `SecurityTriageContext`, `ApiSecurityTriageContext`.** Reason: with phase-based triage and a unified `TriageHandler`, the deterministic-graph machinery and per-pipeline triage handlers have no consumers. Pre-triage pipeline-specific signal detection that lived in `SecurityTriageHandler` is gone — the new flow relies on the unified triage prompt (with `ProjectMapExcerpt` + `SkillIndexEntry` list) to do per-pipeline-type triage via the LLM. If the unified prompt proves insufficient, signal-detection helpers can be reintroduced as `ITriageInputDecorator` services in a follow-up, but adding them speculatively now would carry the legacy assumption forward.
- [Architecture] **`OrchestrationRole`, `SkillOrchestration`, `SkillOrchestrationParser` retained at the type level, no longer populated from `agentsmith.md`.** The parser is deleted. `SkillMdParser` no longer reads the `## orchestration` section. The `RoleSkillDefinition.Orchestration` field remains nullable and is now always null at runtime. `SkillRoundHandlerBase.ExecuteStructuredRoundAsync` (with its gate-branch) and `GateRetryCoordinator` / `GateOutputHandler` / `UpstreamContextBuilder` / `StructuredOutputInstructionBuilder` are kept alive as internal components per user directive. Trade-off: dead code paths in `SkillRoundHandlerBase` (only reachable if `role.Orchestration != null`, which can no longer happen). Acceptable in this phase; full removal is a follow-up cleanup if no consumer reappears.
- [Implementation] **Per-pipeline command-name lookup in `PipelinePresets.GetSkillRoundCommandName(pipelineName)`.** `security-scan` → `SecuritySkillRoundCommand`, `api-security-scan` → `ApiSecuritySkillRoundCommand`, default → `SkillRoundCommand`. `PhaseCommandExpander.ExpandPhase` takes the resolved command name as a parameter. Reason: `SecuritySkillRoundHandler` and `ApiSkillRoundHandler` still have pipeline-specific domain-section building (project brief, findings summary), which the new unified `TriageHandler` doesn't replicate. Routing to those handlers via the existing command names preserves their value while the triage step itself is unified.
- [Implementation] **`{{plan}}` template substitution lives in `SkillPromptBuilder`, not `ISkillBodyResolver`.** The body resolver (p0111a) caches per `(skill, role)` and stays focused on `## as_<role>` selection + `{{ref:<id>}}` inlining. `{{plan}}` substitution depends on per-run `PlanArtifact`, so caching it would be wrong. Substitution happens after body resolution in `SkillPromptBuilder.BuildRolePrompt`. Reviewers without a same-run lead see `(no plan provided)` rather than an empty string — explicit signal beats silent fallback.
- [Implementation] **Confidence threshold downgrade in `SkillRoundHandlerBase.ApplyConfidenceThreshold` (private static helper).** Applied to all observation-emitting paths after `ObservationParser.ParseWithoutIds` and before `SkillRoundBuffer` creation. Threshold = 70; observations with `Blocking=true && Confidence<70` get re-emitted with `Blocking=false` and a structured log entry. The downgrade is in `SkillRoundHandlerBase` not in `ObservationParser` so the parsed observations stay intact and the threshold applies uniformly to all pipeline types.
- [Implementation] **`PlanArtifact` stored as soon as the Plan-phase Lead's observations are parsed (post-confidence-threshold), not after the Plan phase ends.** Trade-off: if the Lead later runs in another phase the artifact gets overwritten, but the Lead role is bound to one phase per run, so this is fine. Alternative would have been a dedicated `BuildPlanArtifact` step at the Plan→Review boundary; the inline approach saves a preset step.
- [TradeOff] **`ConceptVocabulary` accessed via `ContextKeys.ConceptVocabulary` rather than DI-injected.** `YamlSkillLoader` already loads the vocabulary at boot but does not expose it as a registered service. Adding a `IConceptVocabularyAccessor` would be cleaner but requires plumbing through Infrastructure→Application. Pragmatic shortcut: the builder reads from pipeline context with `ConceptVocabulary.Empty` as the safe fallback. Future plumbing (LoadSkillsHandler or boot-time setter) can populate the context key without changing the builder.
- [TradeOff] **`RationaleEntry.Role` is nullable** (`SkillRole?`) where the spec used non-nullable. Negative rationale tokens (`-<skill>:<key>;`) carry no role per the grammar — modeling that with a nullable enum is more honest than picking an arbitrary default. The validator handles both forms; consumers infer role from context if needed.
- [TradeOff] **Step 9 ships 22 new unit tests covering parser/validator/excerpt-builder/expander/label-applier/filter handler.** The 8 spec-listed integration-style tests (frontend-bug ticket assigns architect, db-migration assigns dba, etc.) require complete `SkillIndexEntry` fixtures with realistic activation criteria — those depend on the agent-smith-skills v2.0.0 release that ships the migrated skill definitions. The mechanism is exercised by the unit tests; the integration assertions land alongside the v2.0.0 skills release. SampleA regression test similarly deferred.
- [TradeOff] **`ProjectMapExcerptBuilder` uses crude substring matching (haystack of `PrimaryLanguage` + `Frameworks` + module paths against vocabulary keys).** Per spec the matching "happens here, not in the LLM" but the matching algorithm is unspecified. Substring is operator-extensible: adding more concepts to the vocabulary picks up matches automatically. Refinement (e.g., regex patterns per concept, or LLM-assisted matching) is a follow-up if substring proves too noisy.

## p0111d: Per-Provider SKILL.md Overrides

- [Architecture] **`SKILL.<provider>.md` overrides resolved at parse time, not at body-render time.** ProviderOverrideResolver returns a {EffectivePath, BasePath?} pair: when an override exists in the skill directory, EffectivePath = override path and BasePath = base path; otherwise EffectivePath = base path and BasePath = null. SkillMdParser branches on whether BasePath is set — single-file path (no override) versus override+merge path. Reason: making the resolver path-only keeps SkillBodyResolver / ISkillPromptBuilder / SkillRoundHandlerBase blissfully unaware of providers — they receive a fully-merged RoleSkillDefinition and don't care whether it came from one file or two.
- [Architecture] **Active provider read from `AgentSmithConfig.PrimaryProvider` (top-level scalar), not nested `llm.primary.provider` as the spec suggested.** Reason: the existing AgentSmithConfig has no `Llm` section — adding a three-level nested structure (`Llm.Primary.Provider`) for one string would force an awkward shape and break operators' YAML for no benefit. A single `primary_provider:` at the root maps cleanly and keeps the YAML diff minimal. Documented in skills.md.
- [Architecture] **AgentSmithConfig registered as a singleton with a default empty instance in `AddAgentSmithCore`.** ActiveProviderResolver injects this singleton. Reason: AgentSmithConfig isn't currently DI-registered anywhere — most consumers receive it as a method parameter. The default empty instance keeps the DI graph valid for tests and existing composition roots while leaving `PrimaryProvider = null`, which makes the override resolver a no-op (base SKILL.md always wins). Operators who want overrides register a populated AgentSmithConfig in their composition root before `AddAgentSmithCore` to win the last-binding race — wire-up not done in this phase per the spec's "zero overrides ship" decision.
- [Implementation] **Frontmatter merge is shallow on top-level keys** per spec. `MergeFrontmatter` produces a new SkillMdFrontmatter where each top-level field takes the override's value if non-null, else base's. `activation`, `role_assignment`, `output_contract`, `references` are atomic — declared in override means override wins entirely; omitted means inherit. The body is always taken from the override file (over.Body, not merged with base.Body). Reason: a deep merge inside `activation.positive[]` would silently double-list keys when both sides declare overlapping ones, and operators authoring an override are explicitly opting into a complete-declaration mental model.
- [Implementation] **`name` and `roles_supported` validation runs before the merge.** Override must declare both — Name explicitly (non-empty), RolesSupported as a non-null list — and both must match base exactly (Name byte-equality, RolesSupported set-equality). Mismatch throws `InvalidOperationException` with a clear message at parse time. Reason: a "sibling skill that happens to be a different skill" is not what an override is for — those should live in different skill directories. Failing loud at boot prevents accidental identity drift.
- [Implementation] **INFO-level log when an override loads** ("Provider override loaded for skill '{Name}' from {Path}"); default behavior (no override matched) is silent. Operators see at boot which skills have provider-specific tuning without grep'ing files.
- [TradeOff] **8 tests instead of 7.** ProviderOverrideResolverTests adds a fourth case (`Resolve_NoActiveProvider_ReturnsBaseRegardlessOfOverrides`) beyond the spec's three — guards the empty-string branch in ProviderOverrideResolver, which is the default behavior most operators experience. SkillMdParser tests deliver all 4 spec-named cases.
- [TradeOff] **Composition-root wireup deferred.** No CLI/Server entry point yet replaces the default empty AgentSmithConfig with the loaded one, so `primary_provider:` set in agentsmith.yml has no effect today. Reason: per spec, "Zero overrides ship in this phase. Provider-tuning is opt-in per skill, opt-in per operator." The mechanism is the deliverable; runtime wireup is a follow-up that lands when the first operator-authored override exists. ServerDi tests pass because the default empty config makes the resolver a no-op.

## p0112: Branch Persistence MVP (shipped as p0112a)

- [Architecture] **Push the work branch on every run that produced commits, regardless of pipeline outcome.** CommitAndPRHandler is a late pipeline step — when a run fails before it (TestCommand fails, AgenticExecute crashes), local work in /tmp dies with the container. Push-on-failure makes the branch a durable artifact tied to the ticket, not the run. PR creation is decoupled from branch push: losing tokens because no PR was opened is the wrong trade-off.
- [Architecture] **Branch identity is `agent-smith/<platform>/<projectSlug>/<ticketId>`.** One branch per ticket, regardless of run count. Subsequent runs fast-forward to the existing remote branch instead of branching off main again — avoids force-push by design. Hard cutover from legacy `fix/<ticketId>` per AAD-DEV operator confirmation; documented as a known break for any other deployment that needs a one-time migration script.
- [Architecture] **CheckoutSourceHandler resumes from remote branch when present** — fetch + checkout if `agent-smith/<platform>/<projectSlug>/<ticketId>` exists; else create fresh from main. Triage / Plan / AgenticExecute see whatever state the previous run left. PlanArtifact-in-context (p0111c) lets a subsequent run short-circuit if available.
- [Implementation] **PersistWorkBranchHandler returns CommandResult.Fail with typed FailureKind on every error path** — no log-and-return-Ok. PipelineExecutor wraps the persist invocation in its OWN try/catch separate from the normal command-loop catch: persist runs in the failure path AFTER the original failure cause is captured in `result`, so a persist exception (GitException, OutOfMemory in LibGit2 mid-call) must NOT overwrite the original failure cause for lifecycle.MarkFailed().
- [Implementation] **WIP commits prefixed `[wip] agent-smith run <runId>`** with `Run-Id`, `Pipeline`, `Failed-Step` trailers — reviewers see at a glance the commit is mid-pipeline + non-human + log-correlatable. Auto-flips PR/MR to Draft on GitHub/GitLab; Azure DevOps logs INFO (no native Draft equivalent).
- [Implementation] **LibGit2Sharp reused, NOT shell-out git.** Existing GitHub/AzureRepos/GitLab/Local SourceProviders all use LibGit2Sharp; shell-out would diverge the codebase. Confirmed project-wide choice from p0001 onward.
- [TradeOff] **Push failures non-fatal warnings, not pipeline failures** — pipeline already failed, can't make it worse. (project, ticket)-keyed counter increments on non-transient failures (auth misconfigured, branch protection blocked, divergent remote that won't resolve); after `ConsecutiveFailureThreshold=3`, escalates to error-level log + structured `persist-blocked` marker on the ticket lifecycle so recurring auth/permission issues don't get silently swallowed.
- [TradeOff] **Out-of-scope: container kills (OOM, eviction, SIGKILL, kernel panic).** Persist runs only when the pipeline cleanly handed control to PipelineExecutor's failure path — a process crash dies before the handler. Documented as a known gap; mitigation belongs to a follow-up phase (periodic checkpoint-push during AgenticExecute, or sidecar/watchdog reading the running container's repo).

## p0113: Spawn Pipeline Jobs From Queue (SUPERSEDED — DO NOT IMPLEMENT)

- [Architecture] **Conceptually wrong, abandoned before completion.** Original spec wanted to spawn `agentsmith-cli:latest` containers per queue ticket. Two fundamental errors: (1) the CLI is a developer-only console tool, never deployed to K8s; (2) running the entire pipeline inside the spawned container is the wrong granularity — LLM rounds and git ops belong to orchestration, not to a worker.
- [Implementation] **MVP merged briefly as p0113a, reverted immediately.** Operationally would have broken every queue ticket: ImagePullBackOff on the missing CLI image, EnqueuedReconciler reverting tickets to Pending after 10 min in an infinite loop. See `revert(p0113a): roll back queue-dispatcher refactor` (commit 4ce6bf7).
- [Architecture] **Replaced by p0115 + p0116.** p0115 ships a standalone sandbox agent (worker binary, Redis-driven, language-specific carrier image — NOT the CLI). p0116 migrates the server-pod-side: toolchain steps delegate to sandbox pods instead of running in-process. Spec kept as historical document with `status: superseded, superseded_by: [p0115, p0116]`.

## p0114: OpenAI / Azure-OpenAI Context Compactor

- [Architecture] **Same conceptual algorithm as p0008's ClaudeContextCompactor, different SDK + caching semantics.** Anthropic exposes `cache_control: ephemeral` for verifiable prefix-caching; OpenAI/Azure use opaque automatic prompt caching above the 4096-token threshold without operator control. Compactor I/O shape is identical (message list in, summarized message list out); SDK glue + summarization-prompt tuning differ. New OpenAiContextCompactor handles `type=openai` + `type=azure-openai` (Azure delegates via the same composition pattern AzureOpenAiAgenticAnalyzer uses); `type=gemini` + `type=ollama` get NoOpContextCompactor as an EXPLICIT placeholder, NOT a denial that they need it — those deserve their own implementations in follow-up phases.
- [Architecture] **Trigger is boolean OR**, written as a Boolean expression to remove ambiguity: `compactor.ShouldFire = (currentIterations >= ThresholdIterations) || (estimatedAccumulatedInputTokens >= MaxContextTokens)`. Either alone fires. Defaults from existing CompactionConfig: `ThresholdIterations=8, MaxContextTokens=80000`. Operators set them once per project; both compactors honor the same knobs.
- [Architecture] **Compaction point fires AFTER an assistant turn produces no further tool_calls** — between rounds, never mid-round. Inline comment in OpenAiAgenticLoop + OpenAiAgenticAnalyzer at the call site documents this so future parallelization of the loop has to explicitly handle the constraint. Cheap insurance against subtle breakage.
- [Architecture] **Scoped to agentic loops only**, not the simple CompleteAsync paths (TriageHandler, GeneratePlan first call, ContextGenerator). Single-shot calls don't accumulate history; the agentic loop is where the message list grows turn-on-turn.
- [Implementation] **Pre-compaction token count is an ESTIMATE (tiktoken-equivalent, ±5%), used only for the trigger threshold.** Post-compaction reports VERIFIED `usage.prompt_tokens` from the next response — the API's authoritative count. CompactionEvent records both: `tokens_saved = previous_response.prompt_tokens - next_response.prompt_tokens` once both are known. Trigger uses estimate; reporting uses ground truth.
- [Implementation] **Tail-boundary respects tool-call pairs.** OpenAI rejects requests where a `tool` message references a `tool_call_id` without the matching prior `assistant(tool_calls=[...])` in the same array. Definition: tail = last N COMPLETE rounds (assistant.tool_calls + matching tool messages); compactor walks backward identifying boundaries, never splits mid-pair. Default `N=2`. Bare assistant text reply (no tool_calls) counts as one degenerate round with no tool tail.
- [Implementation] **Summarization prompt via IPromptCatalog (p0103c)** — consistent with all other prompt resources. New resource `src/AgentSmith.Application/Prompts/Resources/openai-context-compactor-system.md`. CompactionEvent logs SHA-256 prefix (8 hex chars) of the resolved prompt for drift detection between runs without dumping the prompt itself.
- [Implementation] **`CompactionConfig.DeploymentName` (string?, default null) routes summarization to a cheaper deployment** (e.g. `gpt-4o-mini-deployment`). Compaction is summarization — running it on the project's gpt-4.1 Primary deployment burns the same per-token rate as the actual work. Default behavior unchanged when unset; no breaking config migration.
- [TradeOff] **Compactor failure (rate-limit, error, garbage summary) is non-fatal** — falls back to original messages with WARN log + `CompactionEvent.Failed=true`. Agentic loop continues with un-compacted history. Pretending compaction succeeded with a broken summary is worse than running on the full history.
- [TradeOff] **Compaction itself costs tokens** — one extra LLM call per event, charged against the configured deployment. Net win when conversation > ~50k tokens; below that the trigger doesn't fire (`CompactionConfig.IsEnabled` gate plus the threshold). Operators can disable per project for traceability in regulated environments.

## p0115: AgentSmith.Sandbox.Agent + Carrier Image (Init-Container Pattern)

- [Architecture] **Init-container pattern with ONE carrier image, NOT pre-baked toolchain images.** The original spec draft listed `sandbox-dotnet`, `sandbox-angular` etc. as pre-baked images we'd publish. Corrected before implementation: we ship exactly one image (`agent-smith-sandbox-agent`) whose only job is to deliver the agent binary into the sandbox pod. The Server pod (p0116) constructs each sandbox pod with two containers sharing an `emptyDir` volume: an initContainer using our carrier image (its ENTRYPOINT copies `/agent` to `/shared/agent` and exits 0), and a main container using the unmodified upstream toolchain image of choice (`mcr.microsoft.com/dotnet/sdk:8.0`, `node:20`, `mcr.microsoft.com/openjdk/jdk:21-ubuntu`, …) with `command: ['/shared/agent']`. We maintain ONE image; toolchain updates flow automatically from upstream; operator-custom toolchains work without our involvement; new languages need zero changes from us.
- [Architecture] **Self-Inject mode (Option B), not a Busybox stage (Option A).** The carrier's ENTRYPOINT is the agent itself with `--inject <target>`. The agent's `BinaryInjector` resolves `Environment.ProcessPath` and copies the binary to the requested target with executable permissions, then exits 0. Rejected alternative — a separate Busybox stage providing `/bin/cp`: would add 1 MB and a second tool to the carrier for a 30-line copy operation that the agent can do natively. Self-inject also keeps the carrier truly single-binary.
- [Implementation] **Carrier base = `mcr.microsoft.com/dotnet/runtime-deps:8.0-bookworm-slim`, NOT distroless.** First attempt used `gcr.io/distroless/cc-debian12` (tight, small) but it lacks `libz.so.1` which self-contained .NET 8 needs at startup. `gcr.io/distroless/base-debian12` lacks `libgcc` and `libstdc++` too. Microsoft's `runtime-deps` is the documented minimal base for self-contained .NET and bundles everything required (glibc, libgcc, libstdc++, libz, libssl, ICU stubs). Cost: image is ~218 MB instead of the spec's ~80 MB target. That's the price of running a real .NET binary in a real container; distroless ergonomics don't apply here. Documented in the Dockerfile comment block.
- [Implementation] **`IncludeAllContentForSelfExtract=true`, NOT `IncludeNativeLibrariesForSelfExtract=true`.** Spec said "same pattern as p52 used for the CLI" with `IncludeNativeLibrariesForSelfExtract=true`. That setting bundles native libs for self-extraction at startup but keeps managed assemblies in-bundle. In practice this caused CoreCLR startup to fail with "Failed to load System.Private.CoreLib.dll" because the TPA list ended up empty (`COREHOST_TRACE=1` confirmed: `TRUSTED_PLATFORM_ASSEMBLIES = `, `CoreCLR path = ''`). Switching to `IncludeAllContentForSelfExtract=true` extracts the entire bundle to `$DOTNET_BUNDLE_EXTRACT_BASE_DIR` (defaults to `/tmp/.net/...`) on first launch; CoreCLR then loads cleanly. Cost: a one-time disk write (~75 MB) per fresh container startup. Acceptable for the pod lifetime model — extraction stays around for the full pipeline.
- [Implementation] **ProcessRunner catches `Win32Exception`/`InvalidOperationException` from `Process.Start` and translates them to `ProcessOutcome(-1, false, message)`.** Original spec didn't address this — Process.Start throws when the target binary or working directory doesn't exist, which would propagate through StepExecutor → JobLoop → Program and crash the agent (exit 3) on the first bad step. Smoke test caught this immediately (`echo` against missing `/work` directory crashed the loop). The catch turns the failure into a normal step failure (`StepResult` with ExitCode=-1 + descriptive ErrorMessage), letting the loop continue. Aligns with the spec's "Run steps without a Command are pushed as a step failure (ExitCode=-1) and the loop continues" — same shape, broader coverage. Two new tests (`RunAsync_NonexistentCommand_*`, `RunAsync_NonexistentWorkingDirectory_*`) regression-guard the behavior.
- [Implementation] **Three classes for what the spec called "StepExecutor", not one.** Spec described a single `StepExecutor` doing process spawning + soft-batching + timeout enforcement. That would be ~150 lines violating the 120-line class limit. Split into `ProcessRunner` (Process.Start + readers + timeout + kill), `OutputBatcher` (thread-safe buffer + 50/100ms flush), and `StepExecutor` (orchestrator: validates, wires the two together, emits Started/Completed events, packages result). Each is single-responsibility and ≤90 lines. Same external contract: `IStepExecutor.ExecuteAsync(step, onEvents, ct)`.
- [Implementation] **Two interfaces (`IRedisJobBus`, `IStepExecutor`) extracted purely for testability.** Spec didn't list them. Without them, `JobLoop` would be coupled to two concrete classes, blocking unit tests via `Mock<>`. The interfaces have one consumer each, so it's a marginal abstraction cost for full unit-test reach.
- [Implementation] **Wire-format JSON uses string-cased enums (camelCase: `"run"`, `"stdout"`, …) via `JsonStringEnumConverter(JsonNamingPolicy.CamelCase)`.** Numeric enums would round-trip fine but render as integers in `redis-cli LRANGE` / `XRANGE` — which is exactly the debugging tool the spec called out as a reason for picking JSON over Protobuf. Strings make Redis snapshots human-readable. Cost: ~3 bytes per event. Centralized in `WireFormat.Json` so both sides of the wire (agent + future Server pod in p0116) deserialize the same way.
- [Implementation] **Step backward-compat for missing `kind` field uses default-value parameters on the positional record.** Spec said "System.Text.Json deserializes a missing enum to the 0 value = Run" — that only works if the record parameter has a default. Implemented as `StepKind Kind = StepKind.Run, … int TimeoutSeconds = Step.DefaultTimeoutSeconds`. A missing `kind` field on incoming JSON now deserializes to Run; missing `timeoutSeconds` defaults to 600. Required fields (`schemaVersion`, `stepId`, plus `command` for Run validated by JobLoop) still throw if absent.
- [Implementation] **WaitForStepAsync polls `LPOP` with 250 ms sleep, NOT `BRPOP`.** Spec mentioned BRPOP/LMPOP. StackExchange.Redis is a multiplexer and doesn't expose true blocking calls (BRPOP would tie up the connection). Polling LPOP with a short sleep is the canonical workaround under StackExchange.Redis and avoids the multiplexer-blocking trap. The 250 ms tick is invisible compared to step durations (seconds-to-minutes).
- [Tooling] **Separate test project (`AgentSmith.Sandbox.Agent.Tests`), not added to the existing single `AgentSmith.Tests`.** The repo convention so far has been one consolidated test project. The sandbox agent is meant to be a standalone deployable, and Testcontainers.Redis is a heavy new dep that would slow every unrelated test discovery. The new project keeps the agent's test surface isolated and the existing 1338-test suite untouched. Confirmed with the user before splitting.
- [Tooling] **NO `ProjectReference` to `AgentSmith.Contracts`.** Spec listed Contracts as a possible reference "for constants / logging setup if needed". In practice the agent needs nothing from Contracts — its 4 model types (Step/StepEvent/StepResult/RedisKeys) are all new and self-contained. Skipping the reference (a) keeps the self-contained binary lean by avoiding Contracts' transitive dependency on Domain plus all its types, and (b) proves the agent really is decoupled. Confirmed with the user.
- [Tooling] **CI publish gated to `v*` tags only, NOT main pushes.** Per user direction. The matrix entry for `agent-smith-sandbox-agent` builds on every event (validates the Dockerfile in PRs and on main) but only pushes to Docker Hub when a v* tag is created. Lifts to "always" once the Docker Hub repo + description are ready. Implemented via a `publish: tags` matrix key + per-step `if:` on the build-and-push step.
- [Security] **Git credentials flow via pod env vars, NOT via `step.env`.** This decision pre-existed in the spec but is worth highlighting: anything in `step.env` lands in the Redis input stream and is readable via `redis-cli`. Pod env is only visible via `kubectl describe` (RBAC-gated). The agent passes pod env through to subprocess automatically (Process.Start inherits parent env), so a `git clone` step using `credential.helper='!f() { echo "username=x-access-token"; echo "password=$GIT_TOKEN"; }; f'` reads the token from pod env without it ever touching Redis. The carrier image deliberately leaves USER unset so the Server pod controls runtime UID/GID per pod (typically matched to the toolchain main container via `fsGroup`).

## p0116: Sandbox Creator (Wire Extraction + ISandbox + K8s Backend)

- [Architecture] **Wire records extracted to a new `AgentSmith.Sandbox.Wire` project. Step gains optional file-op fields + `Validate()`; StepKind extended with `ReadFile / WriteFile / ListFiles`; StepResult gains `OutputContent`.** p0115 shipped these inside `AgentSmith.Sandbox.Agent/Models/`. p0116 retroactively extracts them so the Server can reference Wire without referencing the Agent. Honest framing: p0115 was the first runnable Agent; p0116 = wire refactor + ISandbox + handler migration. Together they deliver the working sandbox architecture; neither alone does.
- [Architecture] **`InProcessSandbox` lives in `AgentSmith.Infrastructure`, NOT `AgentSmith.Server` as the original spec listed.** CLI does not (and must not) reference Server. Putting the in-process implementation in Infrastructure lets both CLI and Server pull it. K8s sandbox stays in Server (it's the only place that needs the `KubernetesClient` package and Redis-multiplexer wiring for the channel).
- [Architecture] **`SandboxRedisChannel` is pipeline-scoped, not step-scoped.** A single channel instance per pipeline holds `_lastSeenXid` (initialized to `0-0`). XREAD BLOCK starts from the persisted cursor on every `WaitForResultAsync` so each event fires `IProgress` exactly once across the pipeline lifetime. Step-scoped channels would replay every prior event through `IProgress` on each subsequent step.
- [Architecture] **`PipelineExecutor` creates the sandbox conditionally — only when the pipeline contains `CheckoutSource / AgenticExecute / Test / GenerateTests / GenerateDocs`.** Discussion-only flows (mad-discussion no-source) leave `ContextKeys.Sandbox` unset; handlers that pull it short-circuit to a clean skip.
- [Architecture] **`ISandbox` threaded through `IAgentProvider.ExecutePlanAsync` as an optional 8th parameter, NOT injected into the provider via DI.** Per-pipeline sandbox lifetime ≠ provider lifetime. The default-interface 7-arg overload exists for source compatibility but is NOT what the migrated `AgenticExecuteHandler` calls — it explicitly passes the sandbox from `PipelineContext`. All 4 providers (Claude / OpenAI / Azure-OpenAI / Gemini / Ollama) implement the 8-arg signature; `AzureOpenAiAgentProvider` inherits from OpenAI so doesn't need its own override.
- [Architecture] **`ToolExecutor` keeps a dual-mode constructor: optional `ISandbox?` argument routes file/list/run tools through `SandboxToolHandler` when set, falls back to direct repository-path access (legacy `FileToolHandler` + `CommandRunner`) when null.** Reason: `ScoutAgent` and other non-pipeline callers continue to operate on the server filesystem during the migration. Once every caller threads a sandbox the legacy fallback becomes dead code (a follow-up phase removes it).
- [Architecture] **`run_command` tool wraps the LLM-supplied command in `Step{Command='/bin/sh', Args=['-c', commandLine]}`.** The LLM tool-schema continues to accept a single command STRING with shell pipes/redirects/quoting; without `sh -c`, `ProcessStartInfo.ArgumentList` would silently break any pipe like `cat foo | grep bar`. The wrap preserves existing skill prompts unchanged.
- [TradeOff] **`CheckoutSourceHandler` is hybrid for V1, NOT pure-Step as the spec described.** Server-side LibGit2Sharp clones the repo (keeping `Repository.LocalPath` populated for downstream `CommitAndPRHandler` which still operates server-side); `git clone` Step additionally pushes into the sandbox so `/work` is populated for `AgenticExecute / Test`. Hideous but demo-able. Branch / commit / push migration to Step-based git is deferred to p0117 — at which point the server-side clone disappears.
- [TradeOff] **Docker sandbox backend deferred to p0117** along with `GenerateTestsHandler` / `GenerateDocsHandler` migration, TRX result-file parsing, and full Helm chart updates. Per the explicit Option C scoping: minimum-viable demo path is K8s + InProcess + Wire + the three core handlers. Everything else is post-demo polish.
- [TradeOff] **`TestHandler` resolves test command from `ProjectMap.PrimaryLanguage` rather than `Directory.GetFiles` scans.** The old fallback chain (looking for `.csproj` files with `Microsoft.NET.Test.Sdk`) doesn't make sense against a sandbox `/work` that lives inside another container. ProjectMap is the canonical source after p0110b; no-ProjectMap → skip with a clear log line.
- [Implementation] **`SandboxBackendInfo` record wraps the `SandboxBackend` enum for DI registration.** `services.AddSingleton(backend)` failed because `AddSingleton<TService>(TService)` requires a reference type — enums are value types. Wrapping in a record keeps the registration straightforward and exposes the chosen backend to startup logging.
- [Implementation] **`KubernetesPodWatcher` polls `Pod.Status` every 2 s rather than using the streaming Watch API.** Streaming would give faster feedback but adds a long-running connection per pipeline; polling is good enough for the 5–60 s pod-startup window and matches the simplicity of the rest of the K8s wiring.
- [Implementation] **Pod-level `securityContext.fsGroup=1000` defaults via `SandboxSecurityContext`.** Most upstream toolchain images (`node:20`, Debian-based) run as UID 1000; that lines up with the initContainer-written `/shared/agent` being group-readable+executable. Operators with unusual UIDs override via `SandboxSpec.SecurityContext`.
- [Tooling] **CLI's `ServiceProviderFactory.Build` calls `AddInProcessSandbox()` directly.** Server's `Program.cs` calls `AddSandbox()` (auto-detect) — they share no Sandbox-DI extension because they live in different assemblies and CLI must not reference Server. Two registration paths, but each is two lines, so the duplication is cheap.

## p0117: Sandbox follow-ups (Docker backend, TRX, sandbox-routed git ops, grep Step kind, Redis-stream cleanup)
- [Architecture] **Docker backend mirrors the K8s shape, not docker-cli shell-out.** `DockerSandboxFactory` runs an `agent-loader` container first (waits for exit), then a `toolchain` container with two named volumes (`{slug}-shared` RO, `{slug}-work` RW). `DockerContainerSpecBuilder` is the single place that knows the `/shared` + `/work` mount paths. Same Wire/Step protocol as K8s; the only differences are container lifecycle + volume cleanup.
- [Architecture] **`SandboxGitOperations` runs git in the sandbox, not in the source-provider.** `CommitAndPRHandler`, `PersistWorkBranchHandler`, `InitCommitHandler` all migrated. Reason: the LLM's `write_file` tool writes to `/work` in the sandbox; the server-side checkout never sees those changes. `provider.CommitAndPushAsync` would have committed an empty diff in K8s/Docker mode — a latent bug fixed as a side-effect of the migration. PR creation stays in the providers (Octokit / GitLab REST / AzDO) because that's API plumbing, not git plumbing.
- [Architecture] **`StepKind.Grep` ships in Wire so grep is sandbox-routed end-to-end.** Pre-p0117 grep was server-side `Directory.GetFiles + Regex` over the LibGit2Sharp checkout — empty results in K8s mode (server-pod has the original tree, sandbox `/work` has the modifications). New `GrepStepHandler` in the Agent: `rg --json` first, managed `Directory.EnumerateFiles + Regex` fallback. `MaxMatches` default 200, truncation event emitted on cap. `SandboxToolHandler.GrepAsync` builds the Step; `ToolExecutor` routes through it when sandbox is set, returns a clear error otherwise (ScoutAgent migration deferred to p0119a).
- [Implementation] **TRX result-files parsed via `TrxResultParser` into structured `TrxSummary`.** Pre-p0117 `TestHandler` stored raw stdout in `ContextKeys.TestResults`; downstream consumers got an unparseable string. Post-p0117: `dotnet test --logger trx --results-directory /work/test-results`, then ListFiles + ReadFile + parse + aggregate (`TrxSummary.Combine` for multi-test-project solutions). TestHandler's failure-case message now surfaces failing test names + counters instead of dumping stdout.
- [Implementation] **`StreamLimits.EventStreamMaxLength = 10_000` lives in Wire.** Agent's XADD passes `MAXLEN ~` so a single chatty step can't balloon a stream. `SandboxRedisChannel.DisposeAsync` (was no-op) DELs all three sandbox keys with a try/catch — Redis-down on dispose logs and returns rather than throwing.
- [TradeOff] **CheckoutSourceHandler stays hybrid; LibGit2Sharp removal deferred.** Spec called for `CheckoutSourceHandler` pure-Step + `Repository.LocalPath` → `/work`, but ~15 file-reading handlers (`BootstrapProject`, `LoadCodingPrinciples`, `LoadContext`, `CompileDiscussion`, `AnalyzeProject`, `LoadRuns`, `CompileKnowledge`, `LoadCodeMap`, `WriteRunResult`, `SecurityTrend`, `DependencyAudit`, `StaticPatternScan`, `GitHistoryScan`, `QueryKnowledge`, `BootstrapDocument`) read `LocalPath` via `File.Exists` / `File.ReadAllText`. Migrating all of them to `sandbox.RunStepAsync(Step{Kind=ReadFile})` is a multi-day refactor that exceeds p0117 scope. Provider impls keep their `LibGit2Sharp` clone path; `CommitAndPushAsync` is gone (dead code, removed in step 6).
- [TradeOff] **Steps 7 (ProjectAnalyzer sandbox), 8 (ToolExecutor cleanup + ScoutAgent migration) skipped.** All three target the IAgenticAnalyzer / ToolExecutor / ScoutAgent infrastructure that p0119a deletes wholesale (M.E.AI's `IChatClient` + `SandboxToolHost` replaces the apparatus). Doing the migration in p0117 = pure throwaway work that p0119a immediately reverts. Documented as a known-gap interim state: ScoutAgent's grep tool returns a clear error message until p0119a's tool-host migration ships.
- [TradeOff] **Step 9 (Helm chart) deferred to a follow-up phase.** Helm-ifying `deploy/k8s/`'s 9 numbered YAMLs is independent infrastructure (no p0119a dependency, no source-code blocker). Skipped to keep p0117 focused on the sandbox-runtime concerns that p0119a actually depends on.

## p0119a: Microsoft.Extensions.AI migration (IChatClient + FunctionInvokingChatClient + SandboxToolHost)
- [Architecture] **5 provider classes + 4 AgenticLoops + 4 AgenticAnalyzer adapters + the entire ILlmClient surface deleted in one cutover, not staged.** Reasoning: half-migrated state would force every caller to know whether to use IAgentProvider or IChatClient. One commit-batch (waves 3-5), all provider classes gone, single new IChatClientFactory in DI. ~2500 lines of provider-specific code eliminated.
- [Architecture] **No wrapper/shim bridging ILlmClient → IChatClient.** Per the user's no-shim principle: every caller (~20 sites) is changed directly to take IChatClientFactory in its constructor and resolve IChatClient via `factory.Create(agent, taskType)`. The previously-considered ChatClientLlmAdapter is rejected.
- [Architecture] **IChatClientFactory takes AgentConfig per-call, not via DI singleton.** The original spec shape (`Create(TaskType)` with AgentConfig in ctor) doesn't fit the codebase — AgentConfig is per-pipeline runtime data, not a DI singleton. Reshaped to `Create(AgentConfig, TaskType)` with the factory as a pure singleton over the IChatClientBuilder list.
- [Tooling] **Microsoft.Extensions.AI 10.4.0 + sibling .OpenAI 10.4.0 pinned exact below 10.4.1.** tghamm/Anthropic.SDK#197 is open at 10.4.1 (MissingMethodException on HostedMcpServerTool.AuthorizationToken). M.E.AI.Abstractions 10.4.0 added to AgentSmith.Contracts so IChatClient can appear in cross-layer interfaces. Bump in a follow-up phase once #197 is closed.
- [Tooling] **OllamaSharp 5.4.24 replaces Microsoft.Extensions.AI.Ollama.** The official .Ollama package is preview-only and abandoned at 9.7.0-preview; no 10.x line at all. OllamaSharp's `OllamaApiClient` natively implements `IChatClient`; production-grade and well-maintained. 5.4.24 was the last version whose dep chain stays at M.E.AI.Abstractions 10.4.0; 5.4.25 floats to 10.4.1 and would conflict with the lockstep pin.
- [Tooling] **tghamm/Anthropic.SDK 5.10.0 (bumped from 5.9.0); Google_GenerativeAI 3.6.6 + Google_GenerativeAI.Microsoft 3.6.6 for Gemini.** Anthropic.SDK's `AnthropicClient.Messages` natively implements `IChatClient`. Google's adapter is the gunpal5 community package — same author as the `Google_GenerativeAI` core SDK already in use. Bump to 3.6.6 was forced by NU1605 when adding 3.6.6's `.Microsoft` companion.
- [Tooling] **Anthropic.SDK 5.10.0 introduced a clashing `ModelPricing` class.** CostTrackerTests qualified `AgentSmith.Contracts.Models.Configuration.ModelPricing` to disambiguate from `Anthropic.SDK.Messaging.ModelPricing`.
- [Implementation] **SandboxToolHost — single source of truth for the LLM tool surface.** 7 [Description]-annotated public methods (ReadFile / WriteFile / ListFiles / Grep / RunCommand / LogDecision / AskHuman) consumed via AIFunctionFactory.Create. GetAllTools() / GetScoutTools() replace the 4 provider-specific ToolDefinitions files. Internal Step-building logic factored into `SandboxStepRunner` to keep the [Description] surface compact and AIFunctionFactory-friendly.
- [Implementation] **FunctionInvokingChatClient handles the tool loop with MaximumIterationsPerRequest=25.** Configured on the wrapper inside `ChatClientFactory.Create` via `new ChatClientBuilder(bare).UseFunctionInvocation(configure: c => c.MaximumIterationsPerRequest = 25)`. The 4 manual loops in AgenticLoop / OpenAiAgenticLoop / GeminiAgenticLoop / OllamaAgenticLoop are gone. Tool-result push is internal to the middleware — caller code never appends ToolMessages manually.
- [Implementation] **Cost-tracker hierarchy from p0119 preserved and adapted.** Each derived tracker grows a `Track(ChatResponse)` overload that reads `UsageDetails.AdditionalCounts` for cache info: Anthropic uses `cache_read_input_tokens` + `cache_creation_input_tokens`; OpenAI uses `cached_tokens`. PipelineCostTracker.Track(LlmResponse) deleted; replacement Track(ChatResponse) reads the same provider-keyed dict.
- [Implementation] **Used GetResponseAsync, not GetStreamingResponseAsync, for non-tool call sites.** The spec wants streaming everywhere "to avoid migrating call sites a second time later". For non-tool LLM-completion paths (intent parsers, generators), GetResponseAsync is semantically equivalent and ~5 fewer LoC per site. AgenticExecute/ScoutAgent/ProjectAnalyzer use GetResponseAsync with FunctionInvokingChatClient — the streaming concern is for client-side live rendering (deferred to a UX phase) and is invisible at the message-level API.
- [Implementation] **AgentPromptBuilder + PlanParser moved Infrastructure → Application/Services/Prompts/.** Application doesn't reference Infrastructure; the prompt builder + plan parser are pure transformations with no Infrastructure-specific deps. Move was a side-effect of deleting all 5 IAgentProvider impls (their previous home was Infrastructure/Services/Providers/Agent/).
- [TradeOff] **Manual smoke-test against AAD-DEV with all 5 providers deferred to operator.** Spec's done criteria #14 ("Manual smoke tested across all 5 provider types in AAD-DEV") requires live API keys + a running pipeline; can't run from a code-edit session. Code-complete + 1188-test suite green; operator validation pending.
- [TradeOff] **~250 handler/factory tests deleted, replacement IChatClientFactory-mock-based tests deferred.** The deleted tests mocked ILlmClient / IAgentProvider — replacement tests need IChatClientFactory mocks returning stub IChatClients yielding `IAsyncEnumerable<ChatResponseUpdate>`. That mocking pattern is non-trivial (M.E.AI types are sealed, ChatResponse construction is awkward) and writing 30+ replacement tests would have doubled the size of this commit. Deferred as a follow-up phase. Compaction tests (1:1 untouched), cost-tracker tests (adapted to ChatResponse stubs), and all non-LLM-mocking tests stay green.

## p0117b: Sandbox consumer migration (LibGit2Sharp out, Repository.LocalPath const, deeper services sandbox-routed)

- [Architecture] **`Repository.LocalPath` is a `const "/work"` (no ctor parameter).** Eliminates the dual-truth between server-side path and sandbox path. ~25 source edits at call sites read fluently because `Path.Combine(repo.LocalPath, ".agentsmith", …)` resolves to the canonical sandbox path. The 2-arg ctor `(BranchName, string)` deliberately compile-breaks every consumer the build catches; that's the migration tracker.
- [Architecture] **`SandboxFileReader` is a thin ergonomic wrapper, not a strategy abstraction.** The 19 file-reading handlers each have their own path-construction logic; substituting `File.Exists` / `File.ReadAllText` with `reader.ExistsAsync` / `reader.ReadRequiredAsync` is line-level surgery. ExistsAsync probes via `Step{Kind=ReadFile} + catch-not-found`; no new wire-protocol kind. `SandboxFileReaderFactory` wraps `Create(ISandbox)` for testability — singleton DI; per-pipeline reader is built inline in the handler.
- [Architecture] **Source providers' `CheckoutAsync` is metadata-only; the clone runs sandbox-side.** GitHub/GitLab/AzDO providers do exactly two things: (1) parse + canonicalise the URL, (2) resolve the default branch via API. The clone is a `Step{Kind=Run, Command=git, Args=[clone, url, .]}` driven by `CheckoutSourceHandler`. `LocalSourceProvider` returns `Repository(branch, basePath)` and trusts the operator's bind-mount. `KubernetesSandbox + LocalSourceProvider` throws `NotSupportedException` — the local disk isn't reachable from the pod.
- [Architecture] **Bootstrap deeper services (`IProjectDetector` / `IRepoSnapshotCollector` / `IContextGenerator` / 3 `ILanguageDetector` impls / `MetaFileBootstrapper`) migrated alongside the handlers.** Without this, the BootstrapProject pipeline step would receive `repoPath = "/work"` and fail on every server-side `Directory.GetFiles("/work", ...)` after the LibGit2Sharp clone disappeared. Made interfaces async + parameterised on `ISandboxFileReader`. ~600 lines of internal change; necessary for end-to-end pipeline correctness — half-migrated state would have silently broken fix-bug under Docker / K8s.
- [Architecture] **Scanner services take the sandbox per-call, not via DI ctor.** `ISandbox` and `ISandboxFileReader` are per-pipeline runtime values, not DI singletons. Spec text said "ctor-injection"; pragmatic shape became `ScanAsync(ISandboxFileReader, ct)` for `IStaticPatternScanner` and `(ISandbox sandbox, ISandboxFileReader reader, ct)` for `IGitHistoryScanner` / `IDependencyAuditor` (the latter two need `Step{Kind=Run}` for `git log` / `npm audit` / `pip-audit` / `dotnet list package`).
- [Implementation] **`GitHistoryScanner` rewritten end-to-end without LibGit2Sharp.** Drives `git log --max-count=500 -p --no-color --unified=0 --format=__COMMIT__%n%H` once via `Step{Kind=Run}`, parses commit blocks + unified-diff `+`/`-` lines manually. `GitDiffSecretMatcher.BuildHeadContentIndexAsync` walks `reader.ListAsync(/work, depth=12)` and `reader.TryReadAsync` each file to seed the "still-in-tree" set. Severity classification (HIGH = still in HEAD, CRITICAL = removed) preserved.
- [Tooling] **`Ignore` NuGet (Sjuul Janssen, MIT) replaces LibGit2Sharp's `.gitignore` matcher.** `GitIgnoreResolver` (sandbox-routed) and the new `HostGitIgnore` (host-disk for api-scan code analyzers) parse `.gitignore` content and call `Ignore.IsIgnored(rel)`. Same API shape as the old LibGit2Sharp impl; ~30 LoC each. No native libgit2 dependency.
- [Implementation] **`AcquireSourceHandler` (legal-analysis) sandbox-routes via `reader.WriteAsync`.** Reads the source PDF/doc from host disk (host-side `File.ReadAllTextAsync` — that path is a CLI argument, not a sandbox path), then writes the content to `/work/<filename>` via the reader so `BootstrapDocumentHandler` picks it up via `reader.ListAsync`. Repository ctor becomes 2-arg; the pseudo-Repository hack is gone.
- [Implementation] **`CompileWikiCommand` (CLI) instantiates `InProcessSandbox` with `workDir = projectPath`.** That's exactly what InProcessSandbox is for — a CLI sandbox over a host directory. `await using` is deliberately omitted because `InProcessSandbox.DisposeAsync` deletes `workDir`; with `workDir = projectPath` that would delete the user's project. Process exit cleans up; no leak for a one-shot CLI command.
- [Implementation] **`InProcessSandbox.ResolvePath` translates `/work` → `workDir` (and `ListFiles` translates back).** Without this, `reader.WriteAsync("/work/.agentsmith/runs/r01-xyz/result.md", …)` in CLI mode would write to the literal host path `/work` (which doesn't exist or is inaccessible). The translation makes `/work` a virtual rebase point so CLI-mode handlers and tools speak the same path language as sandbox-mode handlers and tools.
- [Implementation] **`ProjectMetaResolver.ResolveAsync` uses one `ListAsync(maxDepth=4)` call instead of N `ExistsAsync` probes.** Per-candidate `ExistsAsync` would round-trip the sandbox 100s of times for monorepos. One bulk listing + client-side filtering + ordering (root-first, then depth-first lex) is dramatically cheaper. `ExistsAsync` is reserved for file-existence checks (where the read-or-fail pattern is one round trip per check anyway).
- [Implementation] **`AzureGitOperations` deleted.** Pre-p0117b it owned `EnsureCloned` + `CheckoutBranch` + `FetchAllRemotes` for `AzureReposSourceProvider`. After CheckoutAsync became metadata-only and the clone/checkout moved sandbox-side, the entire class is dead code. PAT-credential handling lives directly in `AzureReposSourceProvider.CreateGitClient` via `VssBasicCredential`.
- [Security] **`NoLibGit2SharpInGraphTests` is a runtime-closure regression test, not a csproj grep.** Walks `AppDomain.CurrentDomain.GetAssemblies()` after force-loading Application/Infrastructure/Server, asserts no `AssemblyName.Name` and no `Type.Namespace` starts with `LibGit2Sharp`. csproj-level inspection would miss transitive deps. Server image ~9 MB smaller now that the native `libgit2-*.dylib`/`.so`/`.dll` payload is gone.
- [TradeOff] **Helm chart (Step deferred from p0117) stays in p0117c.** Pure deployment ergonomics with no source-code coupling — folding it into this phase would obscure the actual refactor diff.
- [TradeOff] **`SkillLoader` paths inside `LoadSkillsHandler` and `MetaFileBootstrapper.TryLoadSkillRoles` stay server-side.** Per-user instruction the migration touches only repo-rooted paths in skill loading; the catalog (`~/.agentsmith/skills/`) is operator-side and intentionally not sandboxed. Acknowledged: a project-vendored `skills/` directory is unreachable to a server-side `skillLoader` after the migration; that flow falls back to the catalog.

## p0121: Pipeline output, cost, and source-context regressions

- [Architecture] **Pricing lookup is provider-agnostic longest-prefix matching, not per-provider normalization.** `PipelineCostTracker.ResolvePricing` tries exact lookup first; on miss, walks `_pricing.Keys` ordered by length descending and accepts the first key the model id starts with (case-insensitive). Reason: every major provider dates their model ids — Azure OpenAI returns `gpt-4.1-2025-04-14`, OpenAI `gpt-4o-2024-08-06`, Anthropic appends `-YYYYMMDD`. A single fallback handles all of them; per-provider hardcoded mappings would rot. Longest-prefix wins so `gpt-4.1-mini-*` correctly hits `gpt-4.1-mini` rather than the shorter `gpt-4.1` key.
- [Architecture] **`ConsoleOutputStrategy` short-circuits the `ConsolidatedPlan` dump for Structured pipelines.** Pipeline-type lookup is a one-line `pipeline.TryGet<PipelineType>(ContextKeys.PipelineTypeName, …)` — no plumbing through `OutputContext`. The data is already populated at run-start by `ExecutePipelineUseCase` and the same key is read by `ConvergenceCheckHandler`, `TriageHandler`, `SkillRoundHandlerBase`. For Structured pipelines (security-scan, api-security-scan), the consolidated discussion is supplementary; findings are the primary deliverable. For Discussion pipelines (mad-discussion, legal-analysis), the discussion IS the deliverable, so existing fall-through is preserved.
- [Architecture] **`api-security-scan` gains `ExtractFindings` between `CompileFindings` and `DeliverFindings`, mirroring `security-scan`'s order.** Pre-fix, the pipeline ended at `CompileFindings → DeliverFindings`, so `ContextKeys.ExtractedFindings` was never populated; `DeliverFindingsHandler` got an empty list and `ConsoleOutputStrategy` fell through to the verbose `ConsolidatedPlan` dump. `ExtractFindingsHandler` already short-circuits when a gate has set findings (lines 25-33), so the insertion is safe whether the closing gate emits findings or not.
- [Architecture] **`TryCheckoutSourceHandler` clones remote sources to a host tempdir via `IHostSourceCloner`, not into the sandbox `/work`.** Post-p0117b, `provider.CheckoutAsync` is metadata-only — it never actually clones — and the previous `pipeline.Set(ContextKeys.SourcePath, repo.LocalPath)` set `SourcePath` to the const `/work`, a sandbox path that fails `Directory.Exists` on the host. The api-scan code analyzers (`RouteMapper`, `AuthBootstrapExtractor`, `UploadHandlerExtractor`) deliberately read host disk via `System.IO` per `SourceFileEnumerator`'s documented dual-mode design. The new cloner shells out to `git clone --depth=1 --no-tags` into a fresh tempdir and returns the path; failure leaves `SourcePath` unset and continues in passive mode. This restores the design intent without migrating analyzers to `ISandboxFileReader` (a much larger change). API-scan never modifies the source — host-disk read is the right shape.
- [TradeOff] **Bootstrap handlers (`LoadContext`/`LoadCodingPrinciples`/`LoadCodeMap`) in api-security-scan still read sandbox `/work`, which remains empty under this fix.** They are explicitly soft-fail per the preset comments, so absence of `.agentsmith/` for the target project is normal. If those handlers need cloned-source visibility in the future — for example, to load the target's `coding-principles.md` — that requires either dual-population (clone to both `/work` and host tempdir) or migrating the api-scan analyzers to `ISandboxFileReader`. Deferred as out-of-scope; not a regression because pre-p0117b the bootstrap handlers also failed-soft when no `.agentsmith/` existed on the target.
- [Implementation] **`HostSourceCloner` resolves the git token from the platform-appropriate env var (`GITHUB_TOKEN` / `GITLAB_TOKEN` / `AZURE_DEVOPS_TOKEN`) and exports it as `GIT_TOKEN` in the subprocess env.** Mirrors the credential helper used by `CheckoutSourceHandler`'s sandbox-side clone (`credential.helper=!f() { echo username=x-access-token; echo password=$GIT_TOKEN; }; f`). Operators with the token already exported as `GIT_TOKEN` directly are unsupported — the cloner ignores it and looks at the platform-named var. Public repos work without a token at all (token is null → env var simply not added).
- [Implementation] **Existing `TryCheckoutSourceHandlerTests` rewritten around `IHostSourceCloner` mock.** Drops the `ISourceProvider` + `ISourceProviderFactory` mocks since the handler no longer calls the provider for remote checkouts. Three new test cases cover the cloner happy path (returns host tempdir → `SourcePath` set, `Repository.RemoteUrl` populated), the cloner-returns-null path (passive mode), and the empty-URL guard (cloner never invoked).

## p0122: Skill output hardening (parser tolerance + schema ban list + skills cleanup)

- [Architecture] **ObservationParser switches from `Deserialize<List<RawObservation>>` to per-element JsonDocument enumeration with try/catch.** Reason: a single bad enum value (LLM emits `"critical"` for severity) used to throw at the list-level deserialize and drop the entire round to FallbackSingle, losing every other valid observation. Element-wise parsing skips the bad item with a warning, keeps the valid ones. Same shape protects against future drift in `Concern` / `Effort` / `Confidence` field types — one tolerance loop covers all of them.
- [Architecture] **`observation-schema.md` is the single source of truth for output shape; per-SKILL "Output format" sections are removed across all 16 api-security skills.** Discovery: SkillPromptBuilder.BuildDiscussionPromptParts injects the framework schema AFTER the role body, but salient JSON-block schemas in the role body (e.g. `"severity": "critical" | "high" | …`) overpower the later rule-text schema in LLM completion. The fix is removal: skills describe WHAT to observe, the framework owns HOW to format. Domain hints (OWASP category, evidence_mode, endpoint location) move into description/rationale/location of the framework schema.
- [Architecture] **Severity ban list lives in observation-schema.md, not in structured-output-* prompts.** Discussion-mode is the actual injection path verified by the operator stack trace (ObservationParser.Parse). The structured-output-* templates feed an IsStructuredRound code path that no skill currently reaches — `RoleSkillDefinition.Orchestration` is never populated by any parser in the Application layer (latent bug, separate phase). Updating only the prompt that LLMs actually see avoids dead-letter edits.
- [Implementation] **Severity allowlist enforces lowercase explicitly + maps tool-imported values.** observation-schema.md: `"high" | "medium" | "low" | "info"` lowercase only; explicit ban list of `critical`/`warning`/`severe`/`none`/`important`/`informational`; mapping table for Nuclei → framework (`critical → high` with `(was: critical)` in rationale; `info → info`; ZAP `informational → info`; ZAP `warning → medium`). The mapping prevents the most common LLM drift pattern (echoing tool-native severity).
- [Implementation] **Per-SKILL hints follow a consistent template: `## Output` heading + one paragraph that maps domain-specific concepts onto framework fields.** Examples: `Put HTTP method + path into location`; `Lead description with the OWASP category`; `Prefix rationale with evidence: confirmed | potential`. No JSON examples in skill bodies — those compete with the framework schema for LLM attention. `evidence_mode` becomes a rationale prefix convention, not a top-level field, since the parser doesn't consume custom fields.
- [Tooling] **agent-smith-skills bumps 1.4.0 → 1.5.0 (minor) for skill-author-visible contract change.** Per release-please's bump-minor-pre-major config in the skills repo. Operators with vendored / downstream-modified skills need to know this is a content-shape change, not a bug fix. Tarball regeneration via scripts/package.sh is deferred to the GitHub Actions release on tag push (release-please bot drives the cut); not committed locally because dist/ is build-artifact-only.
- [TradeOff] **No migration of api-security skills to the actual structured-mode (IsStructuredRound) path.** That path is currently unreachable because no code populates `RoleSkillDefinition.Orchestration`. Wiring it up means writing an orchestration parser + threading it through SkillMdParser + agentsmith.md format support — a distinct refactor that doesn't fit the regression-fix scope of p0122. Until that lands, all skills go through Discussion-mode prompts (which now have the framework schema as authoritative).
- [TradeOff] **No JSON-schema-constrained outputs at the IChatClient level.** Azure OpenAI / OpenAI structured outputs (`response_format: json_schema`) would eliminate the entire class of enum-drift failures by constraining decoding at the API level. Out of scope for p0122 — separate phase touching ChatOptions setup in IChatClientFactory + per-provider builder logic. The parser-tolerance + skill-cleanup combination handles the regression for now; the API-level fix is the long-term endgame.

## p0123: SkillObservation as Universal Pipeline Output (Finding retired)

- [Architecture] **`SkillObservation` absorbs every Finding-shaped field that has a real consumer (File, StartLine, EndLine, ApiPath, SchemaName, EvidenceMode, ReviewStatus, Category) — no wrapper / adapter / DTO indirection.** Pipeline-internal types should carry their full information density. Adapter shims between SkillObservation and Finding would just postpone the unification and reintroduce drift. Per the no-shim principle.
- [Architecture] **Inner scanner records (PatternFinding, HistoryFinding, DependencyFinding) stay; scanner handlers emit BOTH the structured Result AND a SkillObservation list.** The inner records carry scanner-specific metadata (PatternId, RevokeUrl, Cwe, MatchedText, CommitHash, FixVersion, Ecosystem) used by SecurityFindingsCompressor for prompt input — kept. The SkillObservation is the universal output carrier — added. Dual-write is honest: Result is scanner-internal context, Observation is universal output.
- [Architecture] **Free-text Location string deleted in favor of structured fields (File / StartLine / EndLine / ApiPath / SchemaName).** Two parallel ways to express location would be exactly the drift this phase exists to kill. ObservationParser absorbs the migration cost: legacy `"location": "src/Program.cs:42"` is split into File + StartLine; `"GET /api/users"` → ApiPath; bare PascalCase → SchemaName.
- [Architecture] **`EvidenceMode` and `ReviewStatus` become typed fields on SkillObservation, not soft conventions.** p0122 introduced a rationale-prefix convention (`"evidence: confirmed"`) because Finding had typed EvidenceMode but SkillObservation didn't — a framing accident. Now that SkillObservation absorbs Finding's role, the typed fields are correct. p0122 per-SKILL hints get a single-pass update.
- [Architecture] **`ObservationConcern` stays; `Category` field added separately.** Concern is a coarse structural axis (security/correctness/etc.) driving skill orchestration. Category is the free-form domain tag (`secrets` / `injection` / `auth` / `dependencies`) for SARIF rule grouping and Markdown section headers. Both serve different consumers; collapsing would lose information. Concern mandatory; Category optional.
- [Architecture] **Confidence migration is ACTIVE, not passive — the parser auto-upgrades 1-10 emissions to 0-100 with a once-per-(skill,run) warning.** Passive ("skill emits 1-10 → confidence 8 looks like ~0% under 0-100 calibration, eventually noticed") means weeks of broken reports while migration plays out. Active multiplies values in [1,10] by 10 and warns. Skill authors get fast feedback (warning in run logs) without users seeing broken reports. Confidence==1 is the only ambiguous value (could mean 1% or 10%); we project to 10% — the lower-risk error since 1% effectively suppresses the observation downstream.
- [Architecture] **Concern/Category drift detection in ObservationParser.** When Category is set and lowercase-equal to Concern.ToString().ToLowerInvariant() (e.g. concern=Security + category=`"security"`), the parser drops Category to null and emits a once-per-(skill, value) warning. Prevents the obvious drift where a skill author types the same word in both fields and ends up with redundant data.
- [Architecture] **Severity-mapping table in ScannerObservationFactory is explicit, not Info-fallback-by-default.** External tools (Nuclei, ZAP, static-pattern-scanner) emit varied severity strings (`critical` / `crit` / `error` / `warning` / `warn` / `note` / `informational`). Explicit map: critical/crit/error → High; medium/med/warning/warn → Medium; low/note → Low; info/informational/none → Info. Unknown values warn once-per-process and default to Info.
- [Architecture] **`Finding` record deleted entirely, not retained as a private SARIF DTO.** SARIF needs ~6 fields (file, line, severity, ruleId, region, logical location) — small enough to assemble inline as a JsonObject from a SkillObservation. A private intermediate type would just be a bag of the same fields with a different constructor.
- [Architecture] **`FindingAssessment` deleted, replaced by SkillObservation.ReviewStatus.** Under unification, scanner findings ARE SkillObservations from the start; an LLM skill that wants to mark one false-positive sets ReviewStatus on the existing observation in-place via FilterRound (false-positive-filter already does this). ConsolidationResponseParser's assessments-parsing path is dead code, removed.
- [Architecture] **`ExtractFindingsHandler` + `ExtractFindings` command deleted.** Under unification there's no extraction step needed — scanners emit SkillObservations at scan time, gates emit them at gate time, contributors emit them at round time. ContextKeys.SkillObservations is canonical from the start. Both ApiSecurityScan and SecurityScan presets get the ExtractFindings step removed.
- [Architecture] **Output strategies bifurcate by structural need: SARIF + SpawnFix do their own conversion at the boundary; Console + Markdown + Summary + Snapshot consume SkillObservation directly.** Only SARIF (file/line/region required by spec) and SpawnFix (file/line required to patch code) have hard structural needs. The other four render text fields that map 1:1 from SkillObservation. Forcing a uniform pre-conversion would create exactly the wasted indirection this phase is killing.
- [Architecture] **Gate path migrates fully — `structured-output-gate-list.md` prompt rewritten, `GateFindingParser` → `GateObservationParser`, `GateFindingMerger` → `GateObservationMerger`.** Gates are skill-driven LLM rounds returning JSON; under unification their JSON is observation-shaped, not finding-shaped. Result: gate skills follow the same observation-schema.md as analyst skills, with the only difference being the {confirmed, rejected} wrapping for triage roles.
- [Implementation] **Retired commands carry a migration hint at resolve-fail.** `CommandNames.RetiredCommands` dictionary maps retired-command-name → migration message. `CommandContextFactory.Create` checks the retired map on miss and throws with the helpful message: "ExtractFindings was retired in p0123. Observations are now produced directly by scanners and gates. Remove the step from your pipeline preset; see .agentsmith/decisions.md p0123 for context." Three lines of code, one Dictionary, plus the lookup hook.
- [TradeOff] **Observation-schema.md ships with the agent-smith binary as an embedded resource, NOT in agent-smith-skills.** The schema is the contract the parser enforces; binding it to the parser code (same repo, same version) is the only way to keep them in lockstep. agent-smith-skills carries SKILL.md content (per-skill domain guidance) but no framework schema. Skill authors writing per-skill output hints reference the schema doc by path; framework upgrades that change the schema land in the same release as the parser change.

p0121 note (retroactive): The ExtractFindings step inserted into ApiSecurityScan in p0121 was a deliberate intermediate while the output layer still ran on Finding. p0123 migrates the output layer to SkillObservation and removes the bridge. Revisiting p0121 in isolation will look like a wrong call without this context.

## p0124: Tight SkillObservation contract + batched filter with resilient parsing

- [Architecture] **`SkillObservation.Description` capped at 500 chars; new optional `Details` field at ≤4000 chars carries long-form prose.** Empirical sweep of recent runs showed typical Description is 200-300 chars, worst case ~500 — the 1500-char cap initially proposed was inflated theory. 500 enforces brevity for Console/Summary scanability; multi-paragraph rationale moves to Details, which Markdown/SARIF render but Console/Summary do not. `ObservationCaps` constants are the single source of truth (DescriptionMaxChars=500, SuggestionMaxChars=300, RationaleMaxChars=500, DetailsMaxChars=4000).
- [Architecture] **Channel-aware output rendering.** Console → Description-only; Summary → Description-only; SARIF.message.text → Description (full), SARIF.properties.detailed_message → Details (when present); Markdown → Description as section head + Details as body paragraph when present. Reason: Console/Summary serve fast triage, Markdown/IDE-hover serve archival reading — same observation, different channels. Per the user's framing ("die Texte können gerne komplett in die markdown.md aber nicht in die console und auch nicht in die summary").
- [Architecture] **Field truncation with marker, not rejection.** Over-cap fields get truncated to `cap - markerLen` then suffixed with `…[truncated, original was N chars]`. Reason: rejecting drops real findings on a length technicality; truncating preserves the headline + emits a per-(skill,field) warning so skill authors see the nudge. Marker text exposes both the cap-hit and the overshoot magnitude for skill-side tuning. ObservationParser applies caps in `TruncateField` before observation construction; total output never exceeds the cap including the marker.
- [Architecture] **Filter prompt input excludes Details.** The yes/no filter decision needs the headline, not multi-paragraph context. Including Details would inflate per-obs filter input from ~1500 to ~5500 chars, collapsing batch_size from 18 to 5 at max_output_tokens=8192. New private static helper `FilterRoundHandler.RenderForFilter(observation)` emits the trimmed shape (omits Details and Rationale); existing observation-render path stays for non-filter contexts. Filtered-output observations preserve Details intact — we just don't ship it INTO the filter prompt.
- [Architecture] **Batched filter with deterministic batch size from caps, not heuristic budget fraction.** `batch_size = floor(max_output_tokens / 375) × 0.85` (worst-case 1500 chars per obs ≈ 375 tokens; 0.85 reserve for LLM verbosity + JSON envelope). At 8192 → batch_size ≈ 18; at 4096 → ≈ 9. Worst-case basis (not realistic average): if a skill ever emits all-fields-at-cap, we still fit. With caps known, the formula has a single source of truth — the previous 2/3-budget-fraction was guessing because the cap was unbounded. `TokenBudgetBatcher.Split` does the splitting via JSON-serialize-and-measure (no tiktoken dependency, 4 chars/token proxy).
- [Architecture] **Per-batch failure mode is best-effort + visible.** When a batch returns zero parseable observations, that batch's input observations are kept unchanged and `failedBatchCount` increments. After all batches, if `failedBatchCount > 0`, `FilterCoverageObservationFactory` appends a single Coverage-Incomplete observation: Concern=Correctness (meta-info, not a security finding), Severity=Info, Category=`"meta"`, Description = `Filter coverage incomplete: X of Y batches unparseable; Z observations not reviewed and may include false positives.`, ReviewStatus=`"not_reviewed"`. Severity=Info is already excluded from HIGH/MEDIUM/LOW counts in `ObservationSummary`, so it shows up in Markdown/SARIF without polluting the headline metric.
- [Architecture] **`ResilientJsonObjectExtractor` is the safety net for genuine output-token truncation.** Caps make truncation-mid-array unlikely (batches deterministically fit), but LLMs can still over-emit (verbose preamble pushing the actual JSON past the budget). Brace-counting state machine with string-literal awareness (`"` toggles in-string, `\` escapes next-char) — ~30 lines, no state-machine library, no Utf8JsonReader complexity. Right separation: extractor finds object boundaries, JsonDocument.Parse validates content per-block.
- [Architecture] **`ObservationParser.TryParseWithoutIds` gains a resilient fallback path.** Order: try `ExtractJsonArray` + `JsonDocument.Parse` (current strict path). On `JsonException`, fall through to `ResilientJsonObjectExtractor.ExtractObjects` on the raw response → element-wise `TryBuildObservation` per extracted block → return survivors. If that yields zero, return null (true full-failure). The strict path is fast for well-formed responses; the resilient path is the diagnostic / recovery channel.
- [Implementation] **Per-batch logging surfaces input count + expected output chars + actual response chars.** The 0.85 reserve is observable in logs, not just guessed. If actual response chars consistently approaches expected, operators see the headroom is being eaten and can lower batch_size or raise max_output_tokens. If actual is consistently well below expected, the reserve was over-conservative and could be tightened in a future tuning pass.
- [Implementation] **Single-observation-exceeds-budget edge case is no longer reachable.** With Description ≤500 and other fields capped, the worst-case JSON-serialized observation is bounded at ~1500 chars. At any reasonable max_output_tokens (≥2000), an observation always fits a batch. Even if a skill bug emitted a 4000-char Description, the parser truncates to 500 with marker, so the persisted observation is still ≤500. Batching arithmetic always works — no special-case for oversized observations needed.
- [Tooling] **agent-smith-skills 1.6.0 → 1.7.0 is the recommended pairing, but pre-1.7.0 skills still work via parser truncation.** Hard-blocking on a specific skills version would brick existing operator setups when they upgrade agent-smith. The parser cap-truncation path enforces correctness regardless of skill version. With v1.6.0 skills the warning logs are noisier (every long Description gets truncated); with v1.7.0 they're rare. Operators running agent-smith p0124 against v1.6.0 skills will see a recurring "Description truncated from N chars" warning until they upgrade — that's the migration nudge. All 16 api-security skills get the standardized length-contract paragraph in their `## Output` section.
- [TradeOff] **No tiktoken-based size estimation.** JSON-serialize-and-measure plus the 0.85 reserve handles the slack without a token-counting dependency. The 4-chars-per-token proxy is conservative for English JSON output (real ratio is closer to 3.5-4.0). Adding tiktoken would tighten the estimate but adds a per-provider tokenizer dep we'd have to keep in lockstep with the model — not worth it for a 15% reserve we already build in.
- [TradeOff] **No retry on per-batch failure.** The batch+fallback combo delivers usable output without retry; retry adds cost (additional LLM calls) and complexity (idempotency, retry budget) for marginal benefit. Coverage-Incomplete observation makes the gap visible — operator can re-run if they care.
- [TradeOff] **Per-Concern field-cap overrides deferred.** Current 16 api-security skills don't need it; legal-analysis pipeline can revisit when it hits the cap. The `ObservationCaps` shape is forward-compat — adding per-Concern overrides later is additive, not breaking.
- [TradeOff] **No artifact-mode batching.** Artifact-role filters produce a single text artifact, not a list; size pressure hasn't surfaced there. If it does, the batching shape can be lifted into the artifact-mode handler later.

## p0124 follow-up: TriageOutputValidator vocab-fallback

- [Architecture] **`TriageOutputValidator.HasKey` accepts any key declared in `concept-vocabulary.yaml` as a valid rationale, even when the cited skill author didn't list it in `role_assignment.<role>.positive`.** Skill-specific keys remain useful as priority signals to the triage prompt (highest-fit skill picks given run context), but they're no longer a closed enum the LLM must memorize. Vocab is the closed namespace; if a key is in vocab, it's a known concept and the LLM's choice is semantically valid. **Why:** during a real api-security-scan run after agent-smith-skills 1.7.1 (which added 15 vocab keys + activation/role_assignment metadata to all skills), the LLM picked `api_security_scan` as the Lead-rationale for `api-vuln-analyst` — vocab-valid, semantically applicable — but the validator rejected because `api-vuln-analyst.role_assignment.lead.positive` only lists `nuclei_primary` and `owasp_categorisation`. Even after retry, the LLM converged on the same "wrong" choice and triage failed hard. Vocabulary-skill drift was breaking triage with no recovery path. **How to apply:** the skill's own `role_assignment.<role>.positive`/`negative` keys are still checked first (priority signal), the existing `HasNoCriteriaAtAll` defensive default still applies, then vocab-fallback. Order: skill-activation → skill-role-assignment → no-criteria-defensive → vocab. Keys outside both skill criteria AND vocab remain rejected — that's a true LLM hallucination.
- [Implementation] **Vocabulary plumbed into pipeline context as `ContextKeys.ConceptVocabulary`.** `ISkillLoader` gains a `LoadVocabulary(string skillsDirectory)` method (returns `ConceptVocabulary.Empty` when no vocab file found). `LoadSkillsHandler` calls it alongside `LoadRoleDefinitions` and stores both. `TriageOutputProducer` reads vocab from pipeline context and passes it to `validator.Validate(...)`. The validator's `Validate` overload accepts `ConceptVocabulary?` (nullable, defaults to Empty for backward compat with existing callers and test mocks). The original `Validate(output, skills)` overload is removed since callers were a closed set.
- [TradeOff] **Strict `role_assignment.<role>.positive` validation deliberately removed in favor of soft-priority-signal semantics.** Tighter validation theoretically catches more LLM hallucinations early, but in practice the LLM picks vocab-valid keys that just happen not to be in the skill's narrow whitelist — and the validator can't distinguish "smart fallback" from "hallucination" without the vocab as the reference set. The robust answer: trust vocab as the closed namespace, treat skill criteria as priority hints. Skill authors writing role_assignment blocks now describe *which keys this skill responds to most strongly* (used by the prompt for fit-scoring), not *which keys the LLM is allowed to cite when assigning this skill* (used by the validator as a closed enum).

## p0125a: Concept Vocabulary Typed Schema (D1 slice 1/4)

- [Architecture] **Concept vocabulary becomes typed at the schema level: ConceptType ∈ {Bool, Int, Enum}.** `ProjectConcept` carries `Type`, optional `EnumValues`, optional `IntRange`, and `Writers`. Legacy `Section` field removed — the three-section split (project_concepts/change_signals/run_context) was informational, no code branched on it; it collapses to a flat namespace with prefix conventions documented in the YAML header. The type system is closed (no string-typed concepts) so future `activates_when` expressions can't drift on typos like `language = "csharp"`. -- alternatives: open string types (rejected: typo-resistance lost), per-entry default override (rejected: per-type defaults are loader-internal so missing-concept evaluation stays predictable).
- [Architecture] **Hard cutover, no dual-tolerance loader.** Boot against the legacy three-section YAML fails loudly with a migration hint pointing at the new schema. Cross-repo PRs (this repo + agent-smith-skills) merge synchronously. -- alternatives: dual-tolerance with a follow-up cleanup phase (rejected: two coordinated PRs is one merge dance, dual-tolerance is two; the cleanup-later approach drags the schema split across two phases instead of one).
- [Implementation] **Per-type defaults declared once in `ConceptVocabulary.GetDefault`, not per entry.** False for Bool, 0 for Int, first declared enum value for Enum. `GetDefault` throws on unknown concept name — writers should fail loud on typos rather than silently see a default. -- alternatives: per-entry `default_when_absent` field (rejected: would let one bool concept default to true and another to false, making activation evaluation surprising once the boolean evaluator lands in p0125b).
- [Implementation] **Loader detects the legacy shape via simple text-pre-check (newline + two-space + section name + colon), not by deserializing twice.** Reason: the YamlDotNet error for shape-mismatch is opaque; the operator-facing error for "your YAML is in the old shape" needs to be specific and actionable. Pre-check is one if-statement; cleaner than catching a deserialization exception and inferring intent. -- alternatives: catch YamlDotNet exception and inspect (rejected: error messages would degrade once YamlDotNet versions move).
- [TradeOff] **Migration script (`scripts/migrate-concept-vocabulary.csx`) dropped.** 68 entries in 92 lines of YAML is one editor pass; scripting the migration costs more than running it. The YAML lives in our repo (agent-smith-skills), not operator-owned, so there's no second invocation to amortize against. -- alternatives: ship the script in the agent-smith repo for future operator-owned vocab files (deferred: no operator vocab files exist yet; revisit when the first one shows up).
- [TradeOff] **Writers list is parsed and stored but not validated against real handler classes in this slice.** The IConceptWriter handler-registry it would be checked against doesn't exist yet — that's p0125d. Storing the list now means the YAML can be migrated once with final shape; validation lights up when the CLI verb arrives. All 68 migrated entries currently have empty `writers: []` because no publishing handlers exist yet — those land in p0125c.
- [TradeOff] **All 68 migrated entries are typed bool with `writers: []`.** No entry today represents a count or an enum; counts (`findings_count` etc.) appear when scanning handlers publish them in p0125c, project-language enum lands when ProjectAnalyzer publishes it. Bool is the right type for the entire current namespace. Introducing a fictional int/enum entry now to "exercise the type system" would couple p0125a to a publisher that doesn't exist. The loader's int/enum paths are exercised by the test pack instead.

## p0125b: Boolean Expression Evaluator + Run-State (D1 slice 2/4)

- [Architecture] **AST records are pure data; evaluation lives in a separate ActivationEvaluator service.** Putting `Evaluate(state)` on each AST record means a virtual base method and behavior on data — small inheritance, but still inheritance. A switch-expression in `ActivationEvaluator` keeps records as immutable values, centralizes evaluation logic for one-glance review, and makes "add a new node type" a build-error-driven update. Trade-off: one indirection at the call site, worth it to satisfy the composition-over-inheritance principle. -- alternatives: virtual `Evaluate(state)` on each AST record (rejected: behavior on records, breaks records-for-DTOs).
- [Architecture] **Parser split into three ≤120-line classes: ActivationExpressionTokenizer + ActivationExpressionParser + ActivationEvaluator.** A single class covering tokenize+parse+evaluate would be ~250 lines. Splitting along the natural seams (lex → parse-to-AST → evaluate-AST) gives three single-responsibility classes that test independently. Within Parser, per-precedence helpers (ParseOr → ParseAnd → ParseNot → ParseComparison → ParseAtom) are private methods of 5-15 lines each.
- [Architecture] **Comparisons (`=`, `>`, `>=`, `<`, `<=`) bind tighter than NOT.** Matches conventional boolean-algebra reading: `findings_count > 0 AND NOT api_internal_only` parses as `((findings_count > 0) AND (NOT api_internal_only))`. This is also Python/SQL convention. -- alternatives: NOT tighter than comparison (rejected: would force redundant parens around every `findings_count > 0` in skill activates_when fields).
- [Architecture] **Evaluator infers the comparison type from a literal operand, not the vocabulary.** `findings_count > 0` → IntLit on the right → call `state.GetInt("findings_count")`. `pipeline_name = "fix-bug"` → StringLit on the right → call `state.GetEnum`. The runstate impl validates the type against the vocabulary internally, throwing `ConceptTypeMismatchException` if the identifier's declared type clashes. The evaluator stays vocabulary-agnostic, which keeps the IRunStateConcepts contract narrow (no `GetType(name)` method needed). -- alternatives: pass `ConceptVocabulary` into the evaluator so it can dispatch on declared type (rejected: pushes vocabulary lookup into a service that already has a state surface; doubles the responsibility).
- [Implementation] **String comparisons are equality-only; ordered string comparison throws at evaluation time.** Ordered string comparison is locale-sensitive and surprising in activation expressions (`'apple'` > `'banana'` depends on culture). Equality is unambiguous. If lexicographic comparison is ever needed, the type system can grow a dedicated comparison operator; until then this stays banned. Same rule for bools.
- [Implementation] **`ActivationExpressionParseException` carries `Offset` + `OffendingToken`, not just a message.** Parse errors on hand-written skill frontmatter are operator-debuggable; a "parse failed" string with no location wastes their time. Position is byte-offset into the original input (column-style); offending token is the raw source slice. The CLI verb in p0125d will format these into per-skill error messages.
- [Implementation] **`IRunStateConcepts.SetX` throws on type-mismatch at the call site, not on the next read.** Writers fail loud. Silent coercion would let a handler accidentally publish a bool-shaped value to an int concept and have triage downstream make a wrong activation decision with no signal in the call chain. `ConceptTypeMismatchException` carries (concept name, declared type, attempted type) so the error message points at the bug.
- [Implementation] **ConceptValues storage is one `Dict<string, object>` under a single `ContextKeys.ConceptValues` key, not per-type dictionaries.** Per-type dicts (Dict<string,bool>, Dict<string,int>, Dict<string,string>) split the read/write surface across three lookups and force `PipelineContextRunStateConcepts` to know which dict each concept lives in via the vocabulary. Single dict with `object` values is one lookup; the typed cast happens in the Getter against the vocabulary-declared type. Boxing cost is negligible (concept reads happen at triage time, not in hot loops).
- [Implementation] **`PipelineContextRunStateConcepts` registered via a `Func<PipelineContext, IRunStateConcepts>` factory delegate, not as a direct DI binding.** PipelineContext is created `new` per pipeline run by `ExecutePipelineUseCase`; it's not in the DI container. Direct registration would fail at resolution time. The factory delegate is registered as a singleton in Infrastructure DI; it pulls the vocabulary from the context's ConceptVocabulary slot (falling back to `ConceptVocabulary.Empty` for test fixtures and dev environments without a loaded skills set) and constructs the impl. Future handlers (p0125c) inject `Func<PipelineContext, IRunStateConcepts>` and call it in their command handler. -- alternatives: register the type with PipelineContext as a constructor dep (rejected: PipelineContext isn't a DI service).
- [TradeOff] **Parser has no caching, no instance state.** `ActivationExpressionParser.Parse(string)` is a pure function; the class only exists for DI registration consistency. Parsing happens at boot (CLI validate-concepts) or at skill-load (when p0127 lands). No request-path parsing, no cache invalidation problem. If parsing ever shows up in a hot path, a memoizing decorator wraps the parser without changing the contract.
- [TradeOff] **Identifier-to-identifier comparison is rejected at evaluation time.** No skill activates_when uses it today (every example pairs an identifier with a literal). Implementing it would require either adding `GetType(name)` to `IRunStateConcepts` or threading vocabulary into the evaluator; both expand the surface for a hypothetical caller. If the need surfaces, the evaluator can grow this case alongside a `GetType` lookup, additively. -- alternatives: support it now via vocabulary lookup in the evaluator (rejected: premature, doubles evaluator responsibility).

## p0125c: Handler Concept Publication (D1 slice 3/4)

- [Architecture] **PipelineNameInitializer is a dedicated command-handler at the head of every preset, not a routing-side concern.** Pipeline identity is a property of the pipeline run, not the CLI surface (Slack/CLI/webhook all converge on the same pipeline). Five-plus call sites (one per surface) would drift; one Initializer command keeps publication in one place. The Initializer reads `PipelineContext.ResolvedPipeline` (set by PipelineConfigResolver from p0106), so it doesn't need parameters — the resolver is the source of truth. -- alternatives: publish in each routing branch (rejected: drift across surfaces); inject pipeline name via constructor (rejected: ICommandHandler<TContext> is generic, per-route DI binding impossible).
- [Architecture] **`pipeline_name` enum starts narrow: api-security-scan, security-scan, fix-bug, feature-implementation, mad-discussion only.** init-project, legal-analysis, autonomous, skill-manager, fix-no-test, add-feature are NOT in the enum yet. An enum value the LLM can cite must be a pipeline that actually publishes it; listing dead routes lets triage activate skills on a phantom pipeline. The enum grows when each route's pipeline name actually fires through a real Initializer call. -- alternatives: list every pipeline up front (rejected: silent dead branches in triage).
- [Architecture] **`PipelineNameInitializer` is prepended to every preset INCLUDING those whose name isn't in the enum yet.** Currently 5 of 10 presets (init-project, legal-analysis, autonomous, skill-manager, fix-no-test, add-feature) will throw `ArgumentException` at SetEnum because their names aren't in the declared enum_values. This is the LLM-driver fence: a future routing change adding a new pipeline forces a vocabulary update before the pipeline runs at all. The enum starts narrow; presets that need to ship today add their name to the enum in a follow-up. The four explicit-test pipelines (api-security-scan, security-scan, fix-bug, mad-discussion) work; the other six are exercised by p0125c's `PipelineNotInEnum_ThrowsAtSetEnum` test as the documented failure mode. -- alternatives: only prepend to enum-listed presets (rejected: split discipline across routes; would let a new pipeline silently skip publication).
- [Implementation] **Handlers consume `Func<PipelineContext, IRunStateConcepts>` factory delegate, not `IRunStateConcepts` directly.** PipelineContext is created `new` per pipeline run by ExecutePipelineUseCase, not in DI. Direct `IRunStateConcepts` injection isn't resolvable. The factory is the registration shape p0125b set up; handlers call `conceptsFactory(context.Pipeline)` once inside ExecuteAsync to get a context-bound view. -- alternatives: thread `PipelineContext` through every IRunStateConcepts method call (rejected: changes the contract from p0125b).
- [Implementation] **CheckoutSourceHandler refactored into RunCheckoutAsync + CloneAndSwitchAsync + MaybeSwitchBranchAsync to keep methods <=20 lines.** ExecuteAsync wraps a single `RunCheckoutAsync` call with a post-execute `SetBool("source_available", result.IsSuccess)`. The pre-existing 40-line ExecuteAsync was already over the methods<=20 limit; the source_available wiring forced a touch on every return path, so splitting along natural seams (clone-vs-local, clone-then-switch, switch-or-skip) was less risky than a per-return-path patch. -- alternatives: try/catch wrapper at ExecuteAsync entry that always publishes false on throw (rejected: throws aren't the failure mode here — explicit `CommandResult.Fail` returns are; wrapping a try/catch would swallow the existing fail-fast semantics for unexpected exceptions).
- [Implementation] **TryCheckoutSourceHandler publishes `source_available` inside `EmitBanner` because every success/failure path already routes through it.** EmitBanner takes a `string? sourcePath`; null means passive mode (false), non-null means resolved (true). Adding the publish call there gives one chokepoint instead of five. -- alternatives: publish in each return path (rejected: five sites that must stay in sync as TryCheckout grows).
- [Implementation] **TriageHandler `RenderSnapshot` walks `vocabulary.Concepts` and dispatches on declared type, not on the runstate object's storage.** The runstate doesn't expose its declared types directly (it's a value-typed read/write surface, by design); the vocabulary IS the schema. Walking vocabulary lets the snapshot stay in sync with declared concepts as the namespace grows. Default-valued concepts are omitted: false bools, 0 ints, first-enum strings. Empty result renders as `(all defaults)` — for 100+ concepts mostly at default, the noise drowns the signal. -- alternatives: emit every concept including defaults (rejected: log noise dominates the actual change signal); add an `IsDefault(name)` method to runstate (rejected: vocabulary already knows the answer, runstate would have to delegate to it anyway).
- [Implementation] **BootstrapCheckHandler is registered in DI but NOT inserted into any pipeline preset.** The publishing code is mechanical (read two paths, set two bools); the gating decision (do we abort the pipeline on missing files?) is a pipeline-design concern that lives next to init-project's bootstrap flow in p0130 (D6). Splitting along this line keeps p0125c about concept-publication plumbing and lets D6 own the policy. The handler is constructable now so D6's eventual wiring is observation-only. -- alternatives: wire it into init-project unconditionally (rejected: forces a gate decision before D6 has the design context).
- [Implementation] **`source_available` is the only existing-handler concept publication added in p0125c.** The four scan-handler concepts (swagger_spec_present, nuclei_findings_count, zap_findings_count, spectral_findings_count) live in pipelines that already work end-to-end. Adding them now means re-touching four scan handlers across two pipelines for no behavioral change yet — they land when D2's tool-loop refresh of those handlers happens, or as a small follow-up phase if they hold us back. -- alternatives: publish them all here (rejected: scope creep; D2 will retouch the scan handlers anyway).
- [TradeOff] **TriageHandler snapshot is single `ILogger.LogInformation` line, not a structured artifact.** D1's reproducibility goal is `grep concepts run-456.log` and the human sees what was true. A structured artifact (file under `.agentsmith/runs/...`) is justified once Plan/Diff persistence lands in D4; doing it now creates a one-off persistence path we'd refactor next phase. -- alternatives: write a per-run concepts.json artifact (deferred: D4 plan/diff persistence will re-shape this).
- [TradeOff] **`RunStateConceptsTestFactory.P0125cVocabulary` is a hand-rolled test vocabulary, not loaded from the cross-repo YAML.** Tests must run without the agent-smith-skills repo on disk; loading the YAML would couple unit tests to a sister repo. The hand-rolled vocab declares only the four concepts handlers in this slice publish. -- alternatives: load via ConceptVocabularyLoader at test setup (rejected: cross-repo coupling for unit tests).
- [TradeOff] **Cross-repo coupling: vocabulary entries for the four new concepts ship in agent-smith-skills paired with this PR.** Same hard-cutover discipline as p0125a — boot against an unmigrated vocabulary fails because the Initializer's SetEnum throws on undeclared concept. Version bump 1.8.0 -> 1.9.0; additive only (no existing entries touched), so the breaking aspect is just the pin between agent-smith and agent-smith-skills versions.

## p0125d: validate-concepts CLI verb (D1 slice 4/4)

- [Architecture] **`IConceptWriter` discovered via DI, not via reflection over assemblies.** Coding-principles is non-negotiable on "no assembly scanning magic" and the user explicitly rejected the `[PublishesConcept]` attribute path. The explicit contract has three benefits: handlers self-declare what they publish (truth-at-the-writer), registration is single-line in DI, and discovery cost is zero. Trade-off: a tiny boilerplate cost (one `IReadOnlyList<ConceptDeclaration>` property per publishing handler) for a build-time-discoverable, principle-aligned design. -- alternatives: `[PublishesConcept]` + reflection (rejected per coding-principles); a YAML side-file declaring writers (rejected: drift between code and YAML).
- [Architecture] **`ConceptDeclaration` carries (ConceptName, ConceptType), not just the name.** The validator checks both that the handler claims the right concept AND the right type. A handler that declares `source_available` as Int when the vocabulary says Bool is a typo the verb catches at build-time. Type-mismatch is a separate error tuple from name-not-in-vocabulary, so operators see the specific drift.
- [Architecture] **Three-step DI pattern for handlers that are also `IConceptWriter`s.** Each publishing handler is registered as: (1) concrete type (transient — for resolution by IConceptWriter lambda), (2) `ICommandHandler<TContext>` (transient, resolves the concrete) — the pipeline-execution path, (3) `IConceptWriter` (singleton, resolves a one-time concrete instance). The IConceptWriter singleton lambda runs once and caches; the IConceptWriter role uses a single dedicated stateless instance. Pipeline execution still gets fresh transient handlers per command. -- alternatives: change all four handlers to Singleton lifetime (rejected: wider blast radius across the existing pipeline); a separate `*WriterDescriptor` class per handler (rejected: doubles the number of types and breaks the spec's "handlers implement IConceptWriter" contract).
- [Architecture] **`ValidateConceptsCommand` is an instance class in `AgentSmith.Cli/Commands/`, not in Application.** Application can't see `Infrastructure.Core.Services.ConceptVocabularyLoader` (layering: Application → Contracts/Domain only). The Cli project sees both layers. Putting the validator in Cli keeps its dependency graph honest without adding a Contracts-side abstraction over `ConceptVocabularyLoader` just to satisfy layering. -- alternatives: lift `ConceptVocabularyLoader` behind an `IConceptVocabularyLoader` interface in Contracts (deferred: when a second consumer needs the abstraction); duplicate the YAML loading logic in Application (rejected: two parsers, two error messages).
- [Architecture] **Single class with both validator-method and CLI-factory-method.** `ValidateConceptsCommand` has the instance `Validate(skillsDirectory)` method (testable, the actual work) AND a static `Create(...)` method that builds the System.CommandLine `Command` (CLI surface glue). One file, one type, two entry points — keeps the test-and-CLI pair adjacent. The CLI handler resolves the same `ValidateConceptsCommand` from DI and calls `Validate`, so the production path and the test path exercise the same code. -- alternatives: split into `ValidateConceptsCommand` (validator) + `ValidateConceptsCommandFactory` (CLI) (rejected: two types for one feature).
- [Implementation] **Validator uses `ISkillLoader.LoadRoleDefinitions`, not the internal `SkillMdParser` directly.** The spec mentioned `SkillMdParser`, but it is `internal sealed` in Infrastructure.Core and would require either an `InternalsVisibleTo` to Cli (already present) plus knowing the `IProviderOverrideResolver` ctor wiring, or a public re-export. `ISkillLoader.LoadRoleDefinitions` is already the public contract that wraps `SkillMdParser` + override resolution + role merging — reusing it gives the verb the same parsing path the runtime uses. -- alternatives: instantiate `SkillMdParser` directly via `InternalsVisibleTo` (rejected: tight coupling to internals); inline a YAML-frontmatter scanner in the verb (rejected: a second parser path that drifts from runtime).
- [Implementation] **`activates_when` field plumbed through `SkillMdFrontmatter` → `RoleSkillDefinition`.** Adding it once at the parser level makes it available to both p0125d (build-time validation) and p0127 (runtime triage consumption). The field is `string?` and optional; existing skills without it parse unchanged. The override-merge path also handles it (over wins, falls back to base) so provider-override SKILL.&lt;provider&gt;.md files can override the activation expression like every other field.
- [Implementation] **Errors collected and returned all at once, sorted by subject then concept.** Operators want the full picture, not a fix-and-rerun loop. The verb runs every check (activates_when × N skills, handler-side × M registered writers, vocabulary-side × K concepts), accumulates errors, sorts deterministically, and prints. Exit code is the boolean OR of any error existing — 0 on clean tree, 1 otherwise.
- [Implementation] **`CollectIdentifiers` is a switch over the AST nodes, not a visitor.** The grammar is small (5 internal node types, 3 literal types, 1 identifier); a visitor base class plus 5 visit methods is more code than a 7-line switch. Literal nodes fall through (yield no identifiers) implicitly. New AST node types in the future will trigger a non-exhaustive switch warning.
- [Implementation] **Empty `writers: []` lists are allowed without `IConceptWriter` backing.** Not every concept is published by a handler — some are seeded externally (test fixtures, future external publishers, snapshot-only concepts). The vocabulary-side check skips entries with empty writers. The reverse check (every handler-declared concept must be in the vocabulary's writers list) still applies — handlers can't publish concepts the vocabulary doesn't allow them to.
- [TradeOff] **Output is plain text, one error per line, format `{subject}: {concept}: {message}`.** Shell-friendly, grep-able, diff-able. A structured format (JSON, SARIF) is justified once the verb feeds into a richer CI dashboard; for now stdout + exit code is enough for local runs and CI scripts. The verb prints `validate-concepts: OK` on success so CI logs are positive when clean.

## p0126c: skill-call runtime + scope guards + tool-kit + cost tracking (slice 3/3)

- [Architecture] **`IGitIgnoreResolver` extracted to Contracts; the existing `GitIgnoreResolver` flips from internal to public and implements it.** PathReadGuard lives in Application, depends on the contract, and stays free of Infrastructure references. The wrapper-not-replacement decision from the original p0126 spec holds — Ignore-NuGet-package logic is unchanged.
- [Architecture] **`ToolGuardInvoker` extracted as a small adapter.** Inlining the AssertReadable/AssertWritable null-check + error-format logic would push SandboxToolHost over the 120-line cap. The invoker is ~15 lines, single-responsibility (translate guard `Result` → tool-result string for the LLM loop), and SandboxToolHost holds one instance instead of two field references plus two helper methods.
- [Architecture] **`SkillCostScopeManager` extracted from `PipelineCostTracker`.** Per the original p0126 spec's prediction: "Class size grows ~40 lines — verify it stays ≤120; if it exceeds, extract a SkillCostScopeManager helper and inject it." Did. PipelineCostTracker still grows by ~11 lines for the public BeginCall/EndCall/PerSkillBreakdown surface; the scope-manager owns the active-scope state, the records list, and the lock on both. PipelineCostTracker remains over the 120-line cap from before this phase (138→149) — splitting the pricing/formatting concerns is a separate refactor (logged as follow-up). -- alternatives: leave PipelineCostTracker monolithic (rejected: nested-call-detection logic + records list inflate the file beyond reason).
- [Implementation] **`ToolKit` returns a fresh AITool list per call, no caching.** AITool construction via `AIFunctionFactory.Create` reflects on host method signatures — fast enough for per-call use, no cache invalidation problem. A future cache only adds value if profiling shows tool-list construction is hot.
- [Implementation] **`SkillCallScope.BuildRecord` reads counters from the LimitEnforcer when it's been finalized, fallbacks to its own elapsed time when not.** Dispose runs on every code path; in tests we may dispose a scope without a runtime ever calling Finalize(enforcer). The fallback elapsed (DateTimeOffset.UtcNow - StartedAt) gives a reasonable duration for test-only paths; tool/LLM counts are zero when no enforcer was attached.
- [Implementation] **Bootstrap-files restriction matches by `EndsWith` of the normalized path.** The path the LLM passes is repository-relative ("/work/" prefix optional). Suffix-match on the normalized form catches both `.agentsmith/context.yaml` and `/work/.agentsmith/context.yaml` without a separate path-resolution step.
- [TradeOff] **No DI registration for `IPathReadGuard` / `IPathWriteGuard`.** Both guards take per-skill-call data (the SkillExecutionPhase for write, the live IGitIgnoreResolver loaded from the run's /work for read). The runtime constructs them in `ExecuteAsync`; passing them via factory delegates would add plumbing for no observability gain. Tests construct them directly.
- [TradeOff] **`SandboxToolHost` constructor grows two optional parameters; legacy callers see no change.** The alternative — a dedicated `GuardedSandboxToolHost` subclass — would duplicate the seven tool methods plus the `[Description]` attributes. Optional null parameters keep the surface flat and the legacy path identical (existing call sites need no edits).
- [TradeOff] **Verb requires `--skills-path` explicitly, doesn't auto-discover from config.** Build-time tooling tends to run in CI where the skills path is provided as an argument, and operators running locally know which directory to point at. Auto-discovery via the existing config-loader chain is a follow-up if operators ask for it. The verb still accepts `--config` and `--verbose` for consistency with the other CLI verbs but doesn't consume them in this slice.
- [TradeOff] **Test fixtures use nested private writer classes (TestWriterA / TestWriterB).** "One type per file" applies to production code; test fixtures get an exemption to keep the test setup local and obvious. Two distinct classes are needed because `ConceptWriterRegistry` keys on `writer.GetType().Name`, and the multi-writer-same-concept test asserts grouping under different handler-class names.

## p0127a: SKILL.md frontmatter + loader (parallel format support, D3 slice 1/3)

- [Architecture] **Format detection by frontmatter key (`roles_supported` vs `role`), not by directory or filename.** Grep workflows operators use today walk the same `.agentsmith/skills/<name>/SKILL.md` layout; forcing two parallel trees during the migration would break those workflows. Both keys present is a hard `SkillFormatException`, so the per-skill format is unambiguous. -- alternatives: separate directories for new-format skills (rejected: parallel trees during migration); a `format: v2` frontmatter key (rejected: redundant with the presence of `role`).
- [Architecture] **`NewFormatSkillBuilder` and `NewFormatSkillValidator` extracted from `SkillMdParser`.** SkillMdParser is already over the 120-line cap (305 lines pre-phase); piling on the new-format build + validation logic would add ~80 more lines to a class that was already a refactor target. The two new types are each focused (~35 / ~125 lines), one-public-method, instance-based, and keep the new-format logic discoverable by name. The validator slightly exceeds the cap but stays a single responsibility (rule enforcement), with each rule a private 4-6 line method. -- alternatives: inline both inside SkillMdParser (rejected: pushes the parser past 400 lines and makes the new code hard to find); a single `NewFormatSkillParser` doing both build and validate (rejected: validation has ten distinct rules; mixing them with field-mapping would obscure both responsibilities).
- [Architecture] **`RoleSkillDefinition` holds a union of legacy and new fields, all nullable on the not-applicable path.** Triage and runtime consumers read `RoleSkillDefinition`; making them branch on which path produced the definition would couple consumer logic to the parser's internals. Nullable union means: legacy skill → `ActivationCriteria` + `RoleAssignment` populated, new fields null; new skill → new fields populated, legacy fields null. Cleanup of the now-dead legacy fields is p0131 (D7) after p0127c proves the migration. -- alternatives: two separate definition types with a discriminated-union consumer (rejected: would fork every consumer's read path; the union-of-nullables is the standard pattern in this codebase, e.g. p0117b's similar `RoleBodies` nullable on legacy YAML).
- [Implementation] **Validation throws `SkillFormatException`; the loader catches and logs a clear error.** The exception carries the offending file path and the rule description as separate properties (not just a concatenated message). `YamlSkillLoader.LoadFromSkillMdDirectories` adds a dedicated `catch (SkillFormatException)` arm that logs at `Error` with `{Path}` and `{Rule}` fields, separately from the generic `catch (Exception)` warning path. Same effective behavior as the legacy `ValidateStrict` returns-bool pattern (skill is rejected, boot continues), but with structured fields operators can grep on. -- alternatives: return-value-based validation matching the legacy pattern (rejected: ten separate rules don't fit a single error string cleanly; an exception per rule keeps each rule's context attached); fail-boot-hard (rejected: a single bad skill shouldn't kill the whole pipeline; the existing pattern logs and skips).
- [Implementation] **`YamlSkillLoader.ValidateStrict` short-circuits to `true` when `Role` is set on the loaded definition.** New-format skills are validated at parse time by `NewFormatSkillValidator`; the legacy `roles_supported` / `RoleBodies` / `role_assignment` checks don't apply and would all return false against a new-format definition. The early return treats the parse-time validation as authoritative for new-format skills.
- [Implementation] **`MergeFrontmatter` change for `RolesSupported`: now falls back to base when override omits it.** Previously `over.RolesSupported` won unconditionally because override files always declared it (validated by `ValidateOverrideMatchesBase`). With the new format an override may omit `RolesSupported` (it's not part of the new shape); the fallback-to-base merge keeps legacy override files unchanged in behavior while letting new-format overrides work.
- [Implementation] **`ValidateOverrideMatchesBase` dispatches per format.** Legacy-base + override checks `roles_supported` set-equality (existing rule). New-format-base + override checks role string-equality only when both are present; override may omit `role` and inherit from base. Override format must match base (mixing legacy-base + new-override would surface as a `DetectFormat` failure post-merge).
- [Implementation] **`category` is loosely validated in p0127a (non-empty string when `investigator_mode=verify_hint`); closed-enum check is deferred to p0127c.** Defining the enum now means picking values before any `verify_hint` skill exists in the new format. Loose-then-tight: a accepts any string, c populates each skill's category, the validator extension at c's land closes the enum to actually-used values.
- [TradeOff] **Body-verbatim path skips `SkillBodySplitter` entirely.** The legacy splitter looks for `## as_<role>` headers; new-format files have no role sections, the entire body is one body. Forcing the new path through the splitter would mean teaching it to recognize "no sections, just body" as a valid case — a special branch with no behavioral upside. Cleaner: new path skips the splitter; `NewFormatSkillValidator` ensures body is non-empty.

## p0127b: Triage filter via activates_when + specificity tie-break (D3 slice 2/3)

- [Architecture] **`PhaseSpecificityTrimmer` extracted as a separate service.** TriageOutputProducer was already 196 lines pre-phase; folding the trim logic (BuildScoreIndex / TrimPhase / TrimList / TotalSkillCount / EnumerateNames) inline would have pushed it past 250. The trimmer is a single-responsibility orchestrator (cap any phase whose total exceeds the per-phase cap, drop lowest-specificity first), independently testable, and lets the producer stay focused on the LLM-call pipeline. -- alternatives: keep all the helpers private inside the producer (rejected: pushed the producer over 250 lines and entangled trim logic with the validate/retry path it doesn't share); make TrimToCap an internal static method tested via InternalsVisibleTo (rejected: trimming has its own dependencies — the scorer + the score index — and that's a service shape, not a static).
- [Architecture] **Pre-filter happens inside TriageOutputProducer's ProduceAsync, not in StructuredTriageStrategy.** The strategy is a thin orchestrator (~66 lines, mostly logging + command expansion). Filtering is a property of how triage decides input, which lives in the producer. Adding a separate strategy step would mean two collaborators that walk the skill list with no shared state. -- alternatives: a new pre-strategy phase (rejected: extra DI plumbing for no observability gain).
- [Architecture] **`ActivationSkillFilter` + `ActivationSpecificityScorer` are singletons in DI.** Both are stateless (the per-call parse cache lives inside the Filter call frame, not on the field). Singleton lifetime matches `ActivationExpressionParser` / `ActivationEvaluator` from p0125b — the same shape pattern. -- alternatives: scoped (rejected: nothing scoped about them); transient (rejected: pointless allocation per resolve).
- [Implementation] **Specificity score is derived from AST shape (count of Identifier + Comparison nodes), not from a frontmatter field.** Phase C didn't introduce a separate `specificity` key; deriving from `activates_when` keeps it a single source of truth — operators who want a different score order rewrite their expression to be more specific (add an AND clause). The walker counts Identifier + Comparison terms and sums across And/Or branches; Not contributes 0 itself but its inner counts. Parse failures fall back to 0 so a malformed expression simply ranks lowest in the trim. -- alternatives: explicit `specificity:` frontmatter int (rejected: hand-tuned numbers drift from the actual expression; two sources of truth); count of AND-clauses only (rejected: misses OR-branches and comparison density).
- [Implementation] **Skills with no `activates_when` pass through the pre-filter unchanged.** Parallel-format discipline from p0127a — legacy skills haven't migrated yet and shouldn't be gated by a check they don't participate in. They flow into the LLM prompt with their existing `activation.positive`/`negative`; the LLM still applies the legacy heuristic. After p0127c migrates every skill, every active skill carries `activates_when` and the legacy path becomes dead code (cleanup in p0131).
- [Implementation] **`MaxSkillsPerPhase=5` default; counts ALL members of a `PhaseAssignment` (Lead + Analysts + Reviewers + Filter), not just one role.** The existing PhaseAssignment shape constrains Lead and Filter to one each — the variance is in Analysts + Reviewers. Treating the cap as a total-member count makes the limit predictable for operators without forcing per-role caps. When the total exceeds the cap, Lead and Filter slots are preserved; Analysts trimmed first, Reviewers second. -- alternatives: cap each role list independently (rejected: 5+5+5+1+1 caps would let phases balloon to 17 skills); cap only Analysts (rejected: Reviewers can balloon the same way).
- [Implementation] **Trim ties broken by skill name ascending, not by frontmatter order or LLM order.** Determinism guarantees the trim is reproducible across runs given the same inputs — re-running on the same ticket produces the same kept-skills list, so debugging a flagged-out skill is straightforward. -- alternatives: keep LLM order (rejected: LLM ordering is stochastic across runs); keep frontmatter order (rejected: opaque to operators reading the trim log).
- [Implementation] **Specificity tie-break is post-LLM, not pre-LLM.** The LLM's job is to pick which skills genuinely match the ticket; the cap is a guardrail against runaway picks. Pre-LLM trimming would make decisions blind to ticket content. The post-LLM trim only fires when the LLM exceeds the cap — typical cases (LLM picks 3 from 5 candidates) never trigger. The trim log is `Phase {Phase} trimmed from {Before} to {After} skills by specificity (kept: {Names})` so operators see exactly what was dropped.
- [Implementation] **Parse-failure on `activates_when` excludes the skill from the candidate set instead of crashing triage.** `validate-concepts` (p0125d) is the build-time gate; a parse failure at runtime means the gate was bypassed. Excluding the skill is fail-safe (the skill simply doesn't run); crashing triage would kill the whole pipeline for one bad skill. The Error log + skill name + offset gives operators the same fix-loop as the build-time verb.
- [TradeOff] **First SkillCallRuntime consumer migration is deferred.** SkillRoundHandlerBase (441 lines) and FilterRoundHandler (224 lines) both build per-role prompts via complex pipelines that don't map 1:1 to SkillCallRequest's flat shape. Migrating either is its own ~40-test phase. Bundling it into p0127b would have ballooned the slice past one-PR-able size. The triage path is independently demonstrable without the consumer migration — the LLM still sees a narrowed candidate set; PerSkillBreakdown will light up when the consumer migration lands. -- alternatives: migrate SkillRoundHandlerBase here (rejected: 441-line refactor + extensive prompt-build rework explodes the slice).

## p0127c: Skill content migration + hard cutover (D3 slice 3/3, cross-repo)

- [Architecture] **Hard cutover, no dual-tolerance period.** Loader removes legacy parsing in the same PR that flips the agent-smith-skills pin to 2.0.0; the 2.0.0 skills release is the first version that no longer carries any legacy SKILL.md files. Same discipline as p0125a's vocabulary cutover. -- alternatives: ship 2.0.0 with a one-version dual-tolerance period (rejected: extends the cleanup phase by one release cycle for no observable benefit; nobody pins to old versions today).
- [Architecture] **`NewFormatSkillBuilder` dual-writes the legacy `RoleSkillDefinition` fields from the new taxonomy** (RolesSupported = [single role], RoleAssignments = [single empty entry], Activation = Empty, OutputContract = built from output_schema, References = []). This compatibility layer keeps existing consumers (SkillIndexBuilder, TriageOutputProducer, TriageOutputValidator, SkillBodyResolver) working during the [Obsolete] window before p0131 removes them. The mapping is deterministic: producer→Lead, investigator→Analyst, judge→Reviewer, filter→Filter. -- alternatives: update every consumer to read the new fields directly in this slice (rejected: scope explosion — TriageOutputValidator alone has dense legacy assumptions; better to land the cutover with a compat layer and migrate consumers in p0131); leave the legacy fields null on new-format skills (rejected: every consumer breaks at runtime).
- [Architecture] **Multi-role split mechanically with `<original>-<suffix>` naming.** Producer → `-planner`, investigator → `-investigator`, judge → `-judge`, filter → `-filter`. 8 multi-role skills (3 three-role + 5 two-role) become 19 single-role files; total catalog grows 44 → 55. Per-skill review of "was this genuinely multi-role" is a follow-up — the new format invites natural deduplication when bodies were near-identical. -- alternatives: per-skill judgment on each split (rejected: 8 reviews × decision points = bottleneck); reuse the original suffix-less name for one role and create new ones for the others (rejected: file name collisions in catalog directories).
- [Implementation] **Best-effort `activates_when` per skill category, not per-skill domain analysis.** Each category maps to its pipeline: api-security/* → `pipeline_name = "api-security-scan"`, security/* → `pipeline_name = "security-scan"`, mad/* → `pipeline_name = "mad-discussion"`, legal/* → `pipeline_name = "legal-analysis"`, coding/* → `pipeline_name = "fix-bug" OR pipeline_name = "feature-implementation"`. Operators tune individual expressions later as the vocabulary grows. The migration's job is "every skill loads under the new format"; per-skill activation refinement is its own follow-up. -- alternatives: walk every skill with the operator (rejected: 44 decision points blocking a structural cutover); use `activates_when: "true"` everywhere (rejected: would activate every skill for every pipeline, defeating the purpose).
- [Implementation] **Vocabulary extension: `legal-analysis` added to `pipeline_name` enum.** Previously the enum excluded legal-analysis (per p0125c's deliberate fence — non-published pipelines threw at SetEnum). The migration unblocks legal/* skills' `activates_when` expressions. PipelineNameInitializerHandler will now successfully publish pipeline_name="legal-analysis" when the legal-analysis pipeline runs. -- alternatives: keep the legal/* skills behind a different concept (rejected: pipeline_name is the natural gate; adding one more enum value is the smallest change).
- [Implementation] **`survey_scope` populated as `["**/*"]` for all survey-mode skills.** The script-driven migration uses a uniform broad scope. Per-skill scope refinement (e.g. `["**/*Controller.cs"]` for api-design-auditor) is a follow-up after operators see which scopes need to be tightened in practice. -- alternatives: per-skill scope authoring (rejected: blocking on per-skill domain decisions).
- [Implementation] **Category enum closed at p0127c: `{auth, injection, secrets, iam, crypto, headers, inputs, outputs}`.** ValidateConceptsCommand extends `CheckNewFormatRules` with the closed-enum check. Verify_hint skills that declare a category outside the enum fail at build time. The migration assigns each verify_hint skill a category from this enum based on the skill's domain (auth-* skills → `auth`, secrets-detector → `secrets`, etc.).
- [Implementation] **Legacy `roles_supported` field kept on SkillMdFrontmatter as a "rejection probe".** YamlDotNet's IgnoreUnmatchedProperties would silently ignore an unknown legacy field; keeping the typed field lets the parser detect the legacy shape and throw `SkillFormatException` with a clear migration message. The field is parsed only for rejection — never consumed for behavior. -- alternatives: text-search the raw frontmatter for the substring `roles_supported:` (rejected: fragile, false positives on indented YAML or comments); ignore the legacy shape silently (rejected: operators with old SKILL.md files would see "skill not loaded" with no migration hint).
- [TradeOff] **Two test files have `[Fact(Skip = ...)]` instead of being deleted.** `SkillMdParserExtendedTests.RolesSupportedFrontmatter_ParsesAllRoles` and `BodySplitByH2Header_ProducesRoleBodyMap` test functionality that no longer exists. Skipping with a `p0127c: ...` reason preserves the intent in the test file as documentation; p0131 deletes them outright with the legacy-field cleanup. -- alternatives: delete now (rejected: removes the trail of "this used to test X; X was retired in p0127c"); rewrite to test new format (rejected: the new SkillMdParserNewFormatTests already cover that ground).
- [TradeOff] **SkillIndexEntry shape unchanged in this slice.** The spec called for simplifying SkillIndexEntry to (Name, Description, Role, Category?, OutputSchema?, ActivatesWhen) but every consumer (TriageOutputProducer.RenderSkills, SkillIndexBuilder, TriageOutputValidator) reads the legacy shape. Changing SkillIndexEntry forces a multi-handler refactor that's better bundled with the [Obsolete]→delete cleanup in p0131. The compat layer (NewFormatSkillBuilder dual-write) keeps the legacy shape valid in the meantime. -- alternatives: simplify SkillIndexEntry now (rejected: cascading refactor across triage layer); leave SkillIndexEntry simplified to a backward-compat record (rejected: consumers still reach for RolesSupported / Activation; nullable doesn't help).

## p0128a: Schemas + Validators + Persistence (D4 slice 1/3)

- [Architecture] **Schemas live under `.agentsmith/schemas/` AND ship as embedded resources via csproj `Link`.** The done criterion required schemas under `.agentsmith/schemas/`; runtime loading needs them in the binary. A single source of truth (`.agentsmith/schemas/*.schema.json`) is referenced via `<EmbeddedResource Include="..\..\.agentsmith\schemas\*.json" Link="Services\Validation\Schemas\..." />` so operators see the schemas in the repo, JsonSchemaLoader reads them from manifest resources, no duplication. -- alternatives: embed-only under `Services/Validation/Schemas/` (rejected: violates the spec's done criterion of files under `.agentsmith/schemas/`); dual-copy (rejected: drift risk and pointless duplication).
- [Tooling] **JsonSchema.Net 7.0.4 (pinned EXACT) over Newtonsoft.Json.Schema.** The codebase uses System.Text.Json throughout (PlanParser, ObservationParser, PipelineCostTracker); pulling in Newtonsoft for schema validation alone would mean two JSON libraries in one process. JsonSchema.Net supports Draft 2020-12 and reads schemas as JsonDocument so they stay in the same in-memory representation as the data being validated. Pin EXACT matches the dependency-pinning discipline (M.E.AI, Anthropic.SDK, OllamaSharp). -- alternatives: NJsonSchema (rejected: ties to Newtonsoft.Json); Newtonsoft.Json.Schema (rejected: same reason).
- [Implementation] **`SchemaValidator` extracted as an internal helper instead of duplicating the parse-validate-format flow in each validator.** Plan/Diff/Bootstrap validators all do the same work — parse JSON, evaluate against the cached schema, format errors with JSON Pointer + rule description. Extracting the routine keeps each validator at ~10 lines (pure binding to a schema name + cached JsonSchema). ObservationOutputValidator is the exception: it wraps ObservationParser to preserve p0124's resilient extraction (truncated-mid-array recovery), so it does NOT use SchemaValidator — the JSON Schema observation.schema.json exists for documentation symmetry only. -- alternatives: copy-paste the routine into each validator (rejected: 3-way duplication); inheritance from a base class (rejected: violates "composition over inheritance" coding principle).
- [Implementation] **`SkillOutputValidatorFactory` keyed by closed enum `SkillOutputSchema`, with a stringly-typed `ForSchema(string?)` entry point that throws on unknown values.** The closed enum (Plan/Diff/Bootstrap/Observation) mirrors NewFormatSkillValidator's already-closed string set. The factory's public surface accepts `string?` because RoleSkillDefinition.OutputSchema is a string field; null is the legacy SKILL.md path → NoOpSkillOutputValidator; unknown non-empty value is a config bug → throws ArgumentException. -- alternatives: stringly-typed switch all the way down (rejected: loses compile-time exhaustiveness); enum on the public surface (rejected: forces every caller to parse strings, which the factory exists to do).
- [Implementation] **`Plan` record extended additively, not rewritten.** Adding new properties (`Scope`, `OpenQuestions`, `TestImpact`, `ConsumerImpact`, `Status`) with sensible defaults keeps every existing constructor call compatible — no consumer of `ContextKeys.Plan` / `ContextKeys.PlanArtifact` breaks. Same for the strict-mode parser: the existing `Parse(providerName, rawJson)` is preserved verbatim alongside the new `ParseStrict(output, validator)`. The new fields populate only on the strict path; legacy lax-parsed plans see `Scope.Empty`, empty open_questions, null impact strings, `Status=Complete`. -- alternatives: rename the existing `Parse` to `ParseLax` (rejected: cascading rename across every Plan-emitting handler); replace the existing `Plan` constructor with the new fields (rejected: every existing test fixture would fail).
- [Architecture] **Strict-path step mapping: schema's `id`→`PlanStep.Order`, `action`→`Description`, `file`→`TargetFile`, `reason`→`ChangeType`.** PlanStep has 4 fields; the schema's step shape has 4 fields with different names. The strict path puts the LLM-emitted `reason` into `ChangeType` (today a free-form string defaulting to "Modify") so the rationale survives. The full structured form is preserved in `PlanJson` regardless — `PlanStep` is the legacy view. p0128b's strict-path migration is the natural moment to revisit whether `ChangeType` should split into a typed enum + a separate `Reason` field. -- alternatives: drop `reason` on the typed side (rejected: silently loses info); concatenate `action — reason` into `Description` (rejected: cluttered prose for the typed field's `description` consumers).
- [Architecture] **`IRunArtifactStore` lives in `AgentSmith.Contracts.Persistence`, not `AgentSmith.Application.Services.Persistence`.** First draft put both contract and snapshot record in Application; build broke because Infrastructure (where `RedisRunArtifactStore` lives) doesn't reference Application. Moved both to Contracts to match the existing pattern (`IRedisJobQueue`, `IRedisClaimLock` etc.). -- alternatives: add Application as a reference of Infrastructure (rejected: inverts the layered architecture); keep RedisRunArtifactStore in Application (rejected: Infrastructure is where Redis-coupled code conventionally lives).
- [Implementation] **InMemoryRunArtifactStore is registered as the safe default in Application's DI; RedisRunArtifactStore overrides in Server's `AddRedis()`.** Mirrors the existing pattern: Application registers fallbacks (NoOpPipelineLifecycleCoordinator, NullTicketClaimService) that Server replaces when Redis services are available. CLI graphs (no Redis) get the in-memory store; Server graphs (Redis) get the Redis store. `TryAddSingleton` in Application means a Server-side `AddSingleton<IRunArtifactStore, RedisRunArtifactStore>()` wins last-binding. -- alternatives: gate at the CLI/Server level explicitly (rejected: duplicates the DI pattern that's already established for IRedisJobQueue and friends); single-impl with config switch (rejected: ties Infrastructure to a config-only branch).
- [TradeOff] **Redis integration tests deferred (spec marked them `Trait('Category','Slow')`).** Per the spec, RedisRunArtifactStore tests need a Redis fixture; the InMemory store covers the same contract surface (write/read/promote/clear/TTL) as a unit test. The Redis impl is straightforward `StringSetAsync` + `KeyDeleteAsync` against the same key shape as RedisJobQueue/RedisClaimLock — covered indirectly by the existing Server DI lifetime regression-guards. Real Redis fixture comes in a follow-up phase if/when an end-to-end smoke is needed. -- alternatives: write the integration tests now with Testcontainers (rejected: spec already deferred; adds a dependency on Docker for `dotnet test` on the main project, mirroring the Sandbox.Agent.Tests pattern but without the corresponding need); skip RedisRunArtifactStore entirely (rejected: production storage path needs to exist for the Server graph).
- [TradeOff] **`PlanParser.ParseStrict` takes `PlanOutputValidator` as a parameter rather than the static class becoming instance-based.** The existing `PlanParser` is static; consumers call `PlanParser.Parse(provider, json)` from many sites. Making it instance-based + DI-resolved cascades into ~5 call sites. Passing the validator as a parameter for the strict path keeps the static class shape and lets consumers compose the call themselves. -- alternatives: convert PlanParser to an instance class (rejected: scope creep — the parser's state is purely the methods); make the validator a static dependency (rejected: violates the DI rule for services).
- [Implementation] **`SkillOutputValidatorFactory` exposes a test-only `internal` ctor that maps every schema to the same fallback validator.** Avoids forcing every test fixture that touches `SkillCallRuntime` to construct the four real validators (PlanOutputValidator, DiffOutputValidator, BootstrapOutputValidator, ObservationOutputValidator) just to satisfy the runtime's new constructor signature. The internal ctor takes (ISkillOutputValidator @default, NoOpSkillOutputValidator noOp); production code uses the public ctor exclusively. -- alternatives: build all four real validators in the test helper (rejected: noisy test setup); thread an `ISkillOutputValidator` through SkillCallRuntime instead of the factory (rejected: the runtime has to dispatch by schema-id at call time, not at construction).

## p0128b: open_questions round-trip (D4 slice 2/3)

- [Architecture] **Two markdown variants — HTML-comment anchors for GitHub/GitLab/AzDO, visible bracket anchors for Jira.** Spec assumed an existing JiraAdfBuilder that doesn't exist; JiraTicketProvider posts comments as a single ADF text node (no markup preservation). HTML-comment markers `<!--Q1-->` would render as literal text in Jira. The Jira-specific template uses visible `[Q1]` brackets and `[agent-smith open questions]` as a leading marker; markdown platforms keep the invisible HTML-comment style. Both forms are accepted by `OpenQuestionsCommentMarkers.IsOpenQuestionsComment`. -- alternatives: build a markdown→ADF marshaller (rejected: scope creep — multi-paragraph ADF authoring needs more than this slice deserves); skip Jira (rejected: spec calls for cross-platform).
- [Architecture] **`IRunArtifactStore` and `ITicketCommentTemplate` both live in `AgentSmith.Contracts/`, not Application.** Same reason as p0128a's IRunArtifactStore move — Infrastructure houses the per-platform implementations and can't reference Application. The Application-side `IPlanOpenQuestionsPoster` resolves keyed singletons via `IServiceProvider.GetKeyedService<ITicketCommentTemplate>(platform)`. -- alternatives: register all four templates by enum-key in Application (rejected: implementations need StackExchange-related dependencies on Infrastructure side); make Infrastructure reference Application (rejected: violates layered architecture).
- [Implementation] **`PlanAnswerParser.Parse` exposes two overloads — `(parent, reply)` and `(commentBody)`.** True parent-comment fetching is platform-specific glue (each comment-webhook payload has a different shape for the parent reference). The single-body overload covers the "operator clicked Quote-reply" path where the marker quotes into the reply body — the dominant UX path on GitHub/GitLab. Webhook handlers call the single-body overload to keep the wiring uniform; a follow-up phase can add per-platform parent-fetch if the data shows operators frequently reply without quoting. -- alternatives: require parent-fetch everywhere (rejected: invasive across 4 platform-specific webhook flows); detect by Q-line heuristic alone (rejected: false positives on unrelated comments that happen to contain a `Q1:` line).
- [Implementation] **`AnswerLineRegex` uses `[ \t]*` between colon and answer rather than `\s*`.** First version had `\s*` — `\s` matches `\n` so `.*` greedily consumed the next line. The horizontal-only whitespace class keeps each Q-line self-contained. Caught by the malformed-answer test on the first run. -- alternatives: anchored on `\n` instead (rejected: same trap, just shifted); switch to `(?:^|\n)` start anchor with explicit `(?=\n|$)` end (rejected: harder to read).
- [Implementation] **Webhook handlers detect PlanAnswers as a SECOND trigger, sibling to the existing keyword check.** GitHub-issue / GitLab-issue / Azure-DevOps-work-item / Jira-comment handlers all branch: keyword OR plan-answers fires the webhook (Jira falls back to "first project with jira_trigger configured" when answers are present without keyword — most deployments have one Jira project per agent-smith project). Operators don't have to remember the keyword in the answer reply — the marker in the parent comment plus `Q1:` lines are enough. PR-comment handlers (GitHubPrCommentWebhookHandler etc.) skip this — Plan questions land on tickets, not PRs. -- alternatives: require keyword always (rejected: clunky UX — operators have to copy the trigger phrase into every answer reply); branch on platform (rejected: asymmetry without justification).
- [Implementation] **`GeneratePlanHandler` tries strict-parse first, falls back to lax on validation failure.** The strict path produces a Plan with `Status` + `OpenQuestions` populated; the lax path is unchanged. Existing prompts that emit unstructured plans still work because the schema rejection triggers the legacy parse. When the prompt does emit structured JSON, the new fields populate end-to-end and `PlanOpenQuestionsHandler` gets a real `Status=NeedsUserInput` to act on. PlanJson context-key is set when the strict path succeeded — feeds p0128a's persistence sidecar. -- alternatives: hard-cutover to strict (rejected: would require coordinating prompt changes across all Plan-emitting paths); keep lax-only and never see open_questions (rejected: defeats the round-trip purpose).
- [TradeOff] **`PipelineExecutor` halts on `OpenQuestionsAwaitingAnswer`, returns `CommandResult.Ok("Pipeline parked: awaiting_user_input")`.** Park is success, not failure — the run isn't broken; the operator's next reply re-triggers a fresh pipeline run. Lifecycle coordinator marks the ticket as Done from the parked state today; a richer model where parked is its own ticket-lifecycle status is a follow-up. -- alternatives: return Fail (rejected: treats parking as an error, which alarms operators and may trigger retry logic); return a special parked-result subtype (rejected: spec creep on CommandResult).
- [TradeOff] **No JiraAdfBuilder built in this slice.** The spec assumed one; there isn't. The Jira template emits plain text with line breaks rather than multi-paragraph ADF. Jira renders the comment as a single text node — usable but visually flat. Building proper multi-paragraph ADF is a follow-up phase if operator feedback shows the visual flatness is a problem. -- alternatives: build a minimal ADF marshaller now (rejected: scope creep — proper ADF authoring with marks/marks/text nodes is its own design problem); skip Jira (rejected: spec calls for all four platforms).

## p0128c: opt-in data-flow gating (D4 slice 3/3)

- [Architecture] **`PipelineContext` got an opt-in `IPipelineContextReadGate` hook instead of being subclassed.** PipelineContext is `sealed` and consumed by every handler. A wrapper subclass would force a per-handler choice (which class to take); a separate `GatedPipelineContext` with its own surface would mean every handler checks both types. The hook stays minimal: PipelineExecutor `using (context.AttachReadGate(gate)) { ... }` scopes the gate around `SafeExecuteAsync`. Internal executor reads (e.g. ResolvedPipeline lookup before the gate attaches) bypass the gate naturally. -- alternatives: unseal PipelineContext (rejected: breaks cleanliness — the type's intent is to be a flat state bag, not a polymorphism axis); thread a separate IPipelineContextReader through every handler (rejected: 100+ touchpoint refactor for a feature that defaults to warning-only).
- [Architecture] **`DataFlowReadGate` only fires on `Get`/`TryGet`. `Set` is unmodified.** The producer writes freely; the consumer's read is the gate. This matches the spec's "wrapper rejects reads of keys not declared by the active phase's incoming edges" and keeps the write path entirely uninstrumented. -- alternatives: gate writes too (rejected: producers can write whatever they like — the contract is what consumers can READ); gate only Get and not TryGet (rejected: TryGet is the more common pattern in handlers, would create huge gap).
- [Architecture] **`PermissivePhaseDataFlow` baseline ships ONE wildcard edge `("*", "*", ["*"])`.** Spec said "every existing PipelineContext.Get<T>/TryGet<T> call site that reads a key produced by an upstream phase either gets covered by the declared edges or refactors". With 165 reads across the Application layer, a strict per-key declaration is days of work and high-regression-risk; the wildcard baseline keeps `enforce=true` viable from day one without breaking anything, and follow-up phases tighten edges per preset as the data-flow story crystalizes. The gate infrastructure is fully load-bearing (DataFlowViolationException, warn vs throw, JSON Pointer errors); only the per-preset edge declarations are placeholder. -- alternatives: declare every edge precisely now (rejected: scope creep — would have doubled this phase's size; spec already allows enforce=false default to defer this); ship without preset declarations and rely on resolver-fallback bypass (rejected: violates done criterion "every preset has a matching IPhaseDataFlow").
- [Implementation] **Wildcards: `FromPhaseStep == "*"` matches any producer; `ToPhaseStep == "*"` matches any consumer; `ContextKeys` containing `"*"` matches any key.** The three wildcard axes compose, so an edge `("*", "*", ["*"])` is fully open. `*` is intentionally not a regex — operators read these declarations and "any" is the only level of indirection they need. -- alternatives: regex/glob on key names (rejected: hard to read and easy to over-match); enum of pipeline lifecycle phases (rejected: doesn't match the dynamic command-name reality where same handler runs in multiple presets).
- [Implementation] **`PipelineExecutor` resolves the data-flow lazily — `IPhaseDataFlowResolver.Resolve(presetName)` returns null when no declaration registered, and the gate is simply not attached.** Tests can construct PipelineExecutor with an empty resolver (`new PhaseDataFlowResolver(Array.Empty<IPhaseDataFlow>())`) and the gate stays dormant — every existing test passes unchanged. -- alternatives: throw on missing declaration (rejected: forces every test fixture to register all 10 flows); attach a "deny-all" gate (rejected: same problem).
- [Implementation] **Gate scope wraps SafeExecuteAsync only — TrackCommand and post-step bookkeeping read freely.** `context.TrackCommand` reads `ExecutionTrail` and `ActiveSkill`; those are framework-internal keys, not handler-declared inputs. Wrapping only the handler's ExecuteAsync ensures the gate sees handler reads, not infrastructure reads. -- alternatives: gate the entire step including TrackCommand (rejected: would require declaring ExecutionTrail/ActiveSkill on every preset's edges); gate only the inner handler call (rejected: same as chosen approach but spelled differently).
- [TradeOff] **`enforce: false` is the production-config default.** Switching to `true` requires the operator to verify the warning log is empty for their pipeline mix. Until preset edges tighten in follow-up phases, the warning-only mode is the only viable default — a hard `enforce=true` on permissive `("*","*","*")` flows would be silent (everything matches the wildcard). The two states (warn-only, strict) both work right now; the value of strict only kicks in once edges tighten. -- alternatives: default to true (rejected: zero benefit until edges are real); ship without the config knob (rejected: operators need a way to opt-in once they've cleaned up the warnings).
- [TradeOff] **No GatedPipelineContext class.** Spec described a wrapper class. Implementation made it a hook on PipelineContext itself. Tests now exercise PipelineContext.AttachReadGate (in `PipelineContextReadGateTests`) plus DataFlowReadGate (in `DataFlowReadGateTests`); the same coverage the spec's wrapper would have had, but spread across two types whose responsibilities are clearly separate (PipelineContext: state bag + gate hook; DataFlowReadGate: enforcement policy). -- alternatives: implement spec verbatim (rejected: would require unsealing PipelineContext or a parallel interface that handlers also have to take).

## p0129a: Verify-phase wiring + scope-verifier (D5 slice 1/3)

- [Architecture] **VerifyRoundHandler is a standalone command-handler, not a SkillRoundHandlerBase descendant.** The spec phrased it as "descends from SkillRoundHandlerBase"; in practice SkillRoundHandlerBase processes ONE skill per call (driven by `(skillName, round)`), while Verify needs to dispatch ALL active VerifyDiff investigators in a single command. Inverting the iteration onto the base class would mean refactoring its public surface for a single new caller. Cleaner: VerifyRoundHandler runs its own loop over active verifiers, calls `IChatClient.GetResponseAsync` via the same `IChatClientFactory` path, parses with the existing internal `ObservationParser.ParseWithoutIds`, and tracks cost via the existing `PipelineCostTracker`. The handler stays focused (~150 lines split across three files) and the base class is unchanged. -- alternatives: emit per-verifier SkillRound commands from a separate command (rejected: needs a coordinating "VerifyConvergence" handler to decide re-loop, plus a new ICommandHandler<SkillRoundContext> variant for verify_diff mode — three new types versus one).
- [Architecture] **Re-loop runs `[AgenticExecute, RunVerifyPhase]`, NOT `[RunFinalPhase, RunVerifyPhase]` as the spec wrote.** In this codebase RunFinalPhase is the post-implementation discussion phase (driven by TriageOutput); the actual implementer is AgenticExecute. Re-running RunFinalPhase would produce more discussion observations but no new diff. AgenticExecute reads `ContextKeys.VerifyNotes` (added to the user prompt by `AgentPromptBuilder.BuildExecutionUserPrompt`) and re-implements with the verifier's notes in scope. Spec's intent was correct; the command name was a mismatch with this codebase's pipeline structure. -- alternatives: re-run [AgenticExecute, RunReviewPhase, RunFinalPhase, RunVerifyPhase] (rejected: too much re-work for first re-loop; the discussion phases run on the new diff anyway in normal pipeline flow if we re-enter from earlier — but here we want a focused re-implementation).
- [Architecture] **Three classes for the verify-phase implementation: VerifyRoundHandler (orchestrator) + VerifierPromptBuilder (static prompt assembly) + VerifyNotesFormatter (static markdown rendering of notes for re-loop and writeback).** The handler stays at the public-method level; the two helpers contain the deterministic transforms each <120 lines. Same coding-principles split as p0126c's runtime collaborator extraction. -- alternatives: inline both helpers into the handler (rejected: pushes the handler past 120 lines and entangles three responsibilities).
- [Architecture] **`PermissivePhaseDataFlow.Edges` made `virtual` so subclasses can override with explicit edges concatenated alongside the wildcard.** Original p0128c made `Edges` a non-virtual auto-property. Adding the Plan→Verify and Implementation→Verify edges to FixBug + AddFeature requires either `virtual` + override, `new` shadowing, or adding an `AdditionalEdges` virtual. `virtual` is the conventional choice; `new` shadowing would silently discard the wildcard if the subclass forgot to include it. The base now also exposes a `protected static readonly Wildcard` constant subclasses concatenate.
- [Implementation] **scope-verifier SKILL.md uses `concern: Correctness`, not the spec's `Scope`.** ObservationConcern is a closed enum: `{Correctness, Architecture, Performance, Security, Legal, Compliance, Risk}`. "Scope" is not a valid value; emitting it triggers ObservationParser to reject the entry (Deserialize throws on the enum, observation skipped). Correctness fits scope-creep semantically (the wrong files were changed = correctness violation against the Plan). Specifying Correctness is also future-proof: if we later add `Scope` to the enum, the SKILL.md updates additively without breaking parser compat.
- [Implementation] **VerifyRoundHandler increments `VerifyRoundCount` BEFORE dispatching verifiers (not after).** Round-1-vs-round-2 is the round we're CURRENTLY running, not the round we just finished. Pre-increment + comparison `roundCount >= 2` reads naturally and matches the user's mental model ("first round, second round").
- [Implementation] **`VerifyObservations` is a separate ContextKey from `SkillObservations`.** Verify-phase observations describe meta-properties of the diff (out-of-scope changes, build failures), not skill-domain findings. Mixing them into SkillObservations would surface them in delivery output (Markdown / SARIF) where they don't belong — they're internal pipeline state for the re-loop decision. Separate slot keeps ObservationSummary, OutputStrategies, and FilterRoundHandler unaffected.
- [Implementation] **Confidence-threshold downgrade duplicated from SkillRoundHandlerBase, not extracted.** Both handlers apply the rule "Blocking + Confidence < 70 → Blocking=false + log". Three lines of code. Extracting to a shared static would couple two unrelated handler families through a tiny utility; copy-paste is cheaper here. If a third handler grows the same need, extract then.
- [Implementation] **VerifyNotes for round 2 escalation = round-1-notes + round-2-notes concatenated, not deduplicated.** Round-2 verifiers typically re-emit round-1 findings plus possibly new ones. Operators see both rounds' notes side-by-side. Deduplication is an obvious next step but lands in p0129b/c when build/test verifiers add higher-volume note streams.
- [TradeOff] **Plan→Verify and Implementation→Verify data-flow edges are documentation only, not enforcement.** PermissivePhaseDataFlow ships with `('*','*','*')` wildcard from p0128c. Adding explicit edges next to the wildcard records the intended flow but blocks nothing — the wildcard matches first regardless of Enforce setting. Per-spec decision: real enforcement (wildcard removal across all 10 presets + per-preset declared edges) is a dedicated post-D7 tightening phase. Documenting the intent now seeds that future work.
- [TradeOff] **scope-verifier SKILL.md leaves test-file siblings to LLM judgment, no hardcoded regex.** A regex would be brittle across languages — `FooTests.cs` (C#) vs `Foo.test.ts` (TS) vs `test_foo.py` (Python) vs `FooTest.java` (Java) vs `foo_test.go` (Go), plus separate test-project layouts. SKILL.md body lists worked examples per language as a calibration set and ends with "when in doubt, allow the test file." False-negative on naming we didn't anticipate (operator noise) is preferable to false-positive on a legitimate test file (operator distrust of the verifier).
- [TradeOff] **`PipelineCostTracker.Track(response)` called inline in InvokeVerifierAsync, not via SkillCallScope (p0126c's per-skill breakdown).** Wiring through SkillCallScope would require migrating VerifyRoundHandler to use the SkillCallRuntime — that's the deferred consumer migration phase noted in p0126c. For p0129a, verify-phase costs accumulate into the pipeline-total (existing behavior); per-verifier breakdown lights up when the consumer migration phase lands.

## p0129b: build-verifier + test-verifier (D5 slice 2/3)

- [Architecture] **LLM-only static analysis instead of actual build/test execution.** Phase D D5 spec said build-verifier "runs the project's build" and test-verifier "runs the project's tests". p0129a's VerifyRoundHandler dispatches verifier skills via IChatClient WITHOUT tool access — wiring SandboxToolHost / RunCommand through to the chat client requires routing through SkillCallRuntime + ToolKit (the deferred consumer migration noted in p0126c). Static-analysis-only is the pragmatic first slice: verifiers analyze the diff content for breakage signals; the existing Test command in the FixBug preset runs the actual build/tests right after Verify, so failures still surface fast-fail-style. If LLM static analysis proves insufficient in operator runs, a follow-up phase wires tools into VerifyRoundHandler (or migrates it to SkillCallRuntime). -- alternatives: ship tool-passing now (rejected: scope creep into p0126/p0127 territory; would have doubled the slice size); skip both verifiers (rejected: build/test breakage is the highest-volume regression class — the static pre-check has real value even without command execution).
- [Architecture] **Two skills, no new C# code.** VerifyRoundHandler from p0129a already filters AvailableRoles by role + investigator_mode + activates_when, so adding new VerifyDiff skills is purely a skills-repo concern. The handler doesn't know or care which verifier ran — observations come back through ObservationParser the same way. This is the slicing payoff of p0129a's parameterized handler: each subsequent verifier costs one SKILL.md + one CHANGELOG entry.
- [Implementation] **test-verifier blocks ONLY on test-removal-without-justification, not on coverage gaps.** Coverage gaps (new public method without test, business-logic change without test update) flag as severity=Medium + blocking=false. Reason: pipelines that block on every coverage edge become operator noise in codebases without 100% coverage discipline. Test removal is structurally suspicious — if a test was removed, the implementer should have documented why; missing rationale is exactly the silent regression verify exists to catch. -- alternatives: block on all coverage gaps (rejected: noise); never block (rejected: silent test removal is the bug-18693-class regression for the test layer).
- [Implementation] **build-verifier discipline: when in doubt, lower confidence and drop blocking.** Diff text doesn't always contain enough evidence to know whether a build will break (e.g. type used in changed code might be defined in an unchanged file). False positives erode operator trust faster than missed signals (which the actual Test command catches). The SKILL.md explicitly tells the LLM "lower confidence rather than guess" — same calibration principle as p0089a's confidence-threshold work.
- [TradeOff] **Verify-notes deduplication still deferred.** Round-2 verifiers re-emit round-1 findings plus possibly new ones. p0129b adds two more verifiers, which multiplies the duplicate-volume across rounds. Not a regression — round-2 escalation already concatenates round-1 + round-2 notes wholesale; operators see duplicates in the ticket writeback. Dedup at the combine site (compare by concern + file + description-prefix, keep most recent) is mechanical but lands cleaner in p0129c when architecture-verifier increases volume further, or in a dedicated cleanup phase. -- alternatives: implement dedup here (rejected: scope creep into the slice that's primarily about adding skill content).
- [TradeOff] **No ProjectBuildCommandResolver implemented.** Parent p0129 spec called for a service that bridges ProjectDetector's per-language BuildCommand/TestCommand to verifiers. Deferred because (a) no verifier currently invokes commands, so the resolver has no consumer; (b) when actual command execution lands in a follow-up phase, the resolver shape will be informed by what the consumer actually needs. Building it speculatively now would be premature abstraction.

## p0129c: architecture-verifier + verify-notes dedup (D5 slice 3/3)

- [Architecture] **architecture-verifier extracts checkable rules from coding-principles.md at LLM-call time; no separate parser.** coding-principles.md is free-form Markdown by design (operator-extensible per p0026). A parser would force a schema. The skill body teaches the LLM to distinguish checkable rules (hard limits, naming conventions, forbidden patterns, required patterns — quotable from the principles + verifiable in diff text) from subjective ones ("readable code", "follow SOLID" — skip). Trade-off: prompt grows by ~5-10k tokens; for verify-phase budget (max 20 tool calls per verifier, 200k input tokens), this is comfortable. -- alternatives: pre-parse principles into a checkable rule set (rejected: forces schema on free-form content); skip architecture-verifier entirely (rejected: leaves gap in spec-listed verifier set).
- [Architecture] **VerifierPromptBuilder gains an optional `codingPrinciples` parameter rather than a separate prompt-builder per verifier.** All four verifier prompts share the shape: system=skill body, user=Plan + Diff + (optional) coding principles. Per-verifier prompt-builders would mean four near-identical classes. The optional parameter keeps the surface flat; verifiers that don't read coding principles (scope, build, test) ignore the section appropriately because their bodies don't reference it.
- [Implementation] **Verify-notes dedup operates on the cumulative VerifyObservations list at Escalate time, not on string concatenation.** Round-1 invocation calls AppendObservations (cumulative slot); round-2 invocation appends. At Escalate time, BuildCombinedDedupedNotes reads the full list, applies VerifyNotesFormatter.Dedup (groups by (file, concern, description-prefix-100), keeps last per group), filters to blocking, and re-formats as a single round-2 notes block. This deviates from the prior "concat round-1 string + round-2 string" semantic (which the original failing test asserted) — the new shape is cleaner: operators see one entry per unique unfixed concern, with the round-2 rationale (most recent) preserved. The temporal "X persisted from round 1" signal is sacrificed for noise reduction; if it's needed later, format the deduped result with `[round-1+round-2]` markers per duplicate-group.
- [Implementation] **Description-prefix-100 is the dedup key's third axis.** Round-2 verifiers tend to LLM-paraphrase round-1 findings slightly. Exact-equality on Description would let paraphrases slip through. 100 chars captures the headline without false-merging two distinct observations that happen to start similarly but diverge later. Empirical pick — a tighter cutoff would over-merge legitimate distinct findings; looser would let LLM-paraphrase variants persist as duplicates.
- [Implementation] **Existing VerifyRoundHandlerTests.ExecuteAsync_BlockingSecondRound test rewritten for the new dedup semantic.** The old test seeded VerifyNotes (a string) without seeding VerifyObservations and asserted both "round 1" and "round 2" headers in the combined output. The new flow needs VerifyObservations seeded (matching real round-1 → round-2 progression where AppendObservations runs in round 1). The test now asserts: failure on second blocking, deduped output ('still out' appears once not twice across rounds), new findings (y.cs) preserved. Cleaner alignment with production behavior.

## p0130a: BootstrapCheck gate + csharp-bootstrap (D6 slice 1/2)

- [Architecture] **BootstrapGateHandler is a separate handler from BootstrapCheckHandler, not a combined publish-and-decide step.** BootstrapCheckHandler is an `IConceptWriter` (publishes `context_yaml_present` + `coding_principles_present`); BootstrapGateHandler reads those concepts and decides whether to abort. Mirrors the p0125c rule that publishing handlers don't make pipeline-level decisions. Two short handlers beats one tangled one. -- alternatives: extend BootstrapCheckHandler with abort logic (rejected: blends two concerns); push gate logic into PipelineExecutor (rejected: preset-specific policy in a preset-agnostic component).
- [Architecture] **BootstrapGateHandler reads concepts via `Func<PipelineContext, IRunStateConcepts>` factory, not raw `ContextKeys.context_yaml_present` lookup.** The parent spec text suggested raw ContextKeys — that would bypass the typed concept layer p0125b stood up and re-introduce a parallel state-bag. The handler depends on the same factory delegate every other concept consumer takes. -- alternatives: raw `pipeline.Get<bool>(ContextKeys.context_yaml_present)` (rejected: there is no such ContextKey — concepts live in `ContextKeys.ConceptValues` dictionary, IRunStateConcepts is the typed view).
- [Architecture] **Three-way preset categorization (Strict / Conditional / None) per parent-spec.** Strict — FixBug, FixNoTest, AddFeature, SecurityScan, Autonomous (gate always active). Conditional — ApiSecurityScan only (gate active when source_available=true; passive-mode runs per p0102a skip cleanly). None — MadDiscussion, LegalAnalysis, InitProject, SkillManager (no gate). Insertion location is directly after the source-checkout step (CheckoutSource for the strict five; TryCheckoutSource for ApiSecurityScan), BEFORE Load* / BootstrapProject so missing-files aborts with the structured "run init-project first" message instead of failing somewhere downstream in a less-actionable way.
- [Implementation] **ApiSecurityScan conditional logic is hardcoded inline in BootstrapGateHandler, not lifted to a registry.** Single conditional preset today; an `IPresetGatePrecondition` registry abstraction is premature for one consumer per the codebase's coding-principles. Inline check reads `pipeline.Resolved().PipelineName == "api-security-scan"` + `concepts.GetBool("source_available")`; comment quotes p0102a so a future second-conditional preset triggers either a switch-extension or the registry refactor — both mechanical. -- alternatives: registry abstraction now (rejected: YAGNI); switch on preset name (rejected: same hardcoding without the inline rationale).
- [Implementation] **BootstrapCheckContextBuilder exists alongside BootstrapGateContextBuilder.** BootstrapCheckHandler shipped in p0125c but its context builder was never registered (no preset referenced it). Wiring it now closes the loophole. The existing `BootstrapCheckContext(PipelineContext Pipeline)` record needed no change.
- [TradeOff] **InitProject preset NOT gated.** Init-project's whole purpose is to produce the bootstrap files; gating it would create a chicken-and-egg lock for first-time onboarding. Operator runs init-project precisely to fix the missing-files state. The InitProject preset's Bootstrap step is the producer (csharp-bootstrap shipping in this slice; node/python/generic in p0130b). -- alternatives: gate init-project too (rejected: kills first-run UX); gate but auto-skip for init (rejected: same as not gating, just spelled twice).
- [TradeOff] **csharp-bootstrap is the demonstrator + path-validator for the BootstrapCheck integration; the other three languages ride in p0130b.** Slice a + slice b share scaffolding (SKILL.md template + activates_when pattern + project_language enum) — splitting languages across more slices would multiply scaffolding for no gain. Slicing the C# skill in alone is enough to exercise the full bootstrap path end-to-end against agent-smith's own repo. Cross-repo: agent-smith-skills 2.4.0 with `project_language` enum addition; agentsmith.yml pin synced.

## p0130b: language bootstrap skills (D6 slice 2/2)

- [Architecture] **Three skills together in one slice, not three separate slices.** Same SKILL.md template + activates_when shape across all three; only the body specializes. Splitting per language would multiply scaffolding work for content that's structurally identical. Cross-repo PR pairs synchronously; no agent-smith code change in this slice — p0130a's gate plumbing is sufficient.
- [TradeOff] **generic-bootstrap is a deliberate fallback that flags itself.** ProjectAnalyzer-side detection might surface project_language='generic' for stacks outside the enum (Java, Go, Rust, Kotlin, Ruby, Elixir, Swift, ...). The skill produces a minimal but technically-correct context.yaml + a coding-principles.md that opens with an explicit operator-note that this run used the fallback and operator extension is needed for verifier feedback to land. Better than failing to produce files when the gate downstream needs them; better than pretending to have language-specific guidance the skill doesn't have. -- alternatives: omit generic-bootstrap entirely (rejected: BootstrapGateHandler aborts pipelines that don't have producer coverage); produce nothing and let the gate fail (rejected: forces operator into a bootstrap loop with no signal); ship a Java/Go/Rust trio (rejected: enum stays narrow per p0125c discipline; adding all of them is endless).
- [Implementation] **node-bootstrap detects TypeScript-vs-JavaScript by `tsconfig.json` presence, not by file extensions.** Some Node repos have a few stray .ts files in tooling but are otherwise plain JS; calling them "TypeScript projects" misleads downstream verifiers. Configuration presence is the authoritative signal.
- [Implementation] **python-bootstrap detects pytest vs unittest by config + import shape.** `pyproject.toml [tool.pytest.ini_options]` or `pytest.ini` → pytest is configured. Tests inheriting from `unittest.TestCase` without pytest config → unittest. Both signals present → document both. Inferring purely from filename pattern (`test_*.py`) is unsafe — pytest uses the same pattern unittest does.
- [Implementation] **No agent-smith C# code change.** p0130a shipped the gate, the preset wiring, and the agentsmith.yml pin format. p0130b touches only agent-smith-skills 2.5.0 + the skills.version pin string. No new tests; existing p0130a test pack covers the gate + the activation routing.

## p0130c: InitProject Bootstrap-step migration to SkillRound dispatch (D6 follow-up)

- [Architecture] **Deterministic BootstrapDispatch instead of full Triage for InitProject.** The catalog is supposed to ship exactly one bootstrap skill per `project_language` enum value (csharp / node / python / generic). An LLM-driven Triage would add cost and latency for a decision with no judgment component. BootstrapDispatch reuses the same `ActivationSkillFilter` that StructuredTriage uses, so routing semantics stay consistent. Fail-loud on 0 / >1 matches surfaces catalog misconfiguration immediately. -- alternatives: full Triage (rejected: LLM overhead with no decision-value); hardcoded language → skill table in the handler (rejected: bypasses the activates_when contract that lets operators add a 5th language without a code change).
- [Architecture] **ProjectLanguageMapper as a static service consumed by a separate `PublishProjectLanguageHandler`, not folded into AnalyzeProjectHandler.** AnalyzeProjectHandler produces ProjectMap (a Domain entity); PublishProjectLanguageHandler translates `ProjectMap.PrimaryLanguage` into a typed concept publication. Mixing the two would reintroduce the pattern p0125c explicitly chose against — that publishing handlers and analyzing handlers are different concerns. -- alternatives: extend AnalyzeProjectHandler to publish project_language directly (rejected: 6 presets call AnalyzeCode and would all start publishing project_language; today only init-project consumes it, and the bootstrap skills' tightened activates_when assumes publication is init-project-scoped); compute project_language inline in BootstrapDispatch (rejected: bypasses IRunStateConcepts so other concept-aware filters can't see the value).
- [Implementation] **`project_language` synonym table is fixed code, not config.** 10 entries covering csharp/c#/.net/dotnet, typescript/javascript/node/node.js/nodejs/ts/js, python/py — everything else falls to `generic`. Case-insensitive lookup; whitespace-trimmed input. External config (YAML in skills repo) was rejected — premature config-ification for a stable internal translation. A 5th language is a deliberate vocabulary change, not a config knob.
- [Implementation] **Bootstrap skills' `activates_when` tightens from `project_language = "X"` to `pipeline_name = "init-project" AND project_language = "X"`.** The four bootstrap skills ship under `skills/coding/` (the same directory fix-bug / add-feature load), and `project_language` is a stable per-project concept. Without the conjunction, a Node project running fix-bug would see `node-bootstrap` appear in the ActivationSkillFilter output — wrong, that skill should only ever run during init-project. -- alternatives: keep narrow activates_when + rely on triage prompt to filter (rejected: relies on LLM judgment for what is a structural constraint).
- [Implementation] **`init-project` added to `pipeline_name` enum cross-repo (agent-smith-skills 2.5.0 → 2.6.0).** `PipelineNameInitializerHandler.SetEnum("pipeline_name", "init-project")` now succeeds instead of throwing the p0125c fence. The fence stays in place for `skill-manager` / `autonomous` / `fix-no-test` / `add-feature` — those presets still trigger the ArgumentException at the head of every chain until they have working skill sets to justify being unfenced.
- [Implementation] **`PipelineCommand.SkillRound` resolved via `PipelinePresets.GetSkillRoundCommandName(pipelineName)`.** Even though init-project resolves to `SkillRoundCommand` (the default), going through the preset-aware resolver keeps BootstrapDispatch consistent with how `StructuredTriageStrategy` emits SkillRound commands — and tracks any future per-preset divergence in one place. -- alternatives: hardcode `CommandNames.SkillRound` (rejected: parallel knowledge that drifts with future security/api preset additions).
- [TradeOff] **InitProject preset gains `WriteRunResult` step that wasn't there before.** Old preset was 4 steps (Init / CheckoutSource / BootstrapProject / InitCommit). New preset is 8 steps including WriteRunResult — operator now sees a `runs/<run-id>/result.md` for init-project runs. p0128a wired bootstrap-output rendering into WriteRunResultHandler; this slice lights it up. Cost: an extra file write per init-project run; benefit: same observability story as every other pipeline.
- [TradeOff] **PipelineType for init-project stays `PipelineType.Discussion`.** Conceptually init-project is now closer to Hierarchical (deterministic dispatch + single producer skill), but changing the type drives `TriageStrategySelector` routing — and InitProject doesn't have a Triage step in the new preset, so the type only affects classification labels. Changing it pre-emptively is a triage-strategy decision that belongs in p0131c-pre. -- alternatives: introduce a new PipelineType.Linear (rejected: scope creep for one preset's classification); flip to Structured now (rejected: makes the type definition lie about what the executor actually does).
- [TradeOff] **`autonomous` and `skill-manager` remain fenced (not in pipeline_name enum).** Both have unresolved real issues — autonomous has no skills with `activates_when` matching it; skill-manager has its own DiscoverSkills/Evaluate/Install handler chain that doesn't go through the SkillRound mechanism. Lifting their fence would be a parallel work-stream; this slice keeps them at the p0125c posture so the symptom (`SetEnum` throws when those presets run) stays visible to operators rather than silently allowing pipelines that don't actually do anything productive.

## p0131c-pre: Discussion-triage migration prerequisite

- [Architecture] **Output-side phase collapse via SinglePhaseCollapser instead of prompt-side adaptation.** mad-discussion + legal-analysis presets have no RunReviewPhase / RunFinalPhase steps, but the structured-triage prompt asks the LLM to assign skills to all three phases. Output-side collapse is one small helper invoked conditionally; prompt-side adaptation would require cross-repo prompt-template changes and a producer flag for prompt variants. -- alternatives: emit a single-phase prompt variant (rejected: cross-repo coordination cost); duplicate the strategy into DiscussionStructuredTriage (rejected: parallel-class drift over time).
- [Architecture] **`PipelinePresets.IsSinglePhase` derived from the preset's command list, not declared as separate metadata.** Adding a new preset automatically gets the right classification by inspecting whether RunReviewPhase / RunFinalPhase are part of its step list. The classification IS the structural property — keeping it where the structure lives prevents drift. -- alternatives: explicit single-phase pipeline-name list (rejected: parallel knowledge); pipeline-type enum value SinglePhase (rejected: semantic overlap with Discussion that creates ambiguity).
- [Implementation] **Selector signature extended `(PipelineType, string pipelineName)`.** Per-name routing is the actual decision in this slice (Discussion mad/legal → Structured vs Discussion autonomous → Legacy). One parameter is cheaper than wrapping or strategy-of-strategies. TriageHandler reads `ResolvedPipelineConfig.PipelineName` (already in context) and passes both. -- alternatives: type-only signature with name-routing inside StructuredTriage (rejected: blends strategy-selection with strategy-execution).
- [Implementation] **Collapser dedup is first-write-wins, not last-wins.** Spec sketched last-wins; implementation reversed it because Plan-phase Lead is the strongest commitment from the LLM and shouldn't be overridden by a Review/Final reappearance of the same skill. Plan-phase Analysts come first, then Review's Analysts, then Review's Reviewers (now Analysts), then Final's positions. The HashSet seed ensures each skill name appears at most once in the merged Analysts list. -- alternatives: error on duplicate (rejected: edge case); concatenate role tags (rejected: makes no sense for Lead/Filter slots).
- [Implementation] **Filter slot fallback chain Plan → Review → Final.** When the LLM puts a Filter on Review (and not Plan), the collapsed output should still get a filter. First non-null wins; a Plan-phase Filter beats a Review-phase one even if both are declared. Mirrors the Plan-Lead-first principle.
- [TradeOff] **autonomous stays on LegacyTriage despite being Discussion-type.** Catalog gap (no `pipeline_name = "autonomous"` skills shipping) means StructuredTriage's HasLoadedSkills check would Fail-loud on autonomous runs. Legacy returns Ok("no roles needed") and lets the pipeline continue past Triage to ConvergenceCheck → CompileDiscussion (still produces a no-op result for the operator, but no hard failure). When autonomous-* skills land, the selector adds autonomous to the structured-allowed set. The fence on `pipeline_name` enum stays in place for autonomous as a parallel signal that this preset isn't fully wired. -- alternatives: migrate autonomous now and let it Fail-loud (rejected: behavior regression); delete autonomous preset (rejected: out of scope).
- [TradeOff] **TriageHandlerSnapshotTests adapted to the new selector signature.** One existing test seeded the selector mock with `Select(It.IsAny<PipelineType>())`; now uses `Select(It.IsAny<PipelineType>(), It.IsAny<string>())`. Single-line change; no semantic shift. The test's snapshot-rendering coverage stays unchanged.

## p0131a: Skill loader + frontmatter cleanup (D7 slice 1/4)

- [Architecture] **SkillRoleMapping as a static helper in Contracts.Models.Skills, not a method on RoleSkillDefinition.** The mapping is the bridge between SKILL.md frontmatter (producer/investigator/judge/filter) and pipeline-side SkillRole enum (Lead/Analyst/Reviewer/Filter). Static helper keeps it callable from validator + filter handler + future runtime callers without forcing a non-null Role on every entity instance. -- alternatives: extension method on RoleSkillDefinition (rejected: same null-handling cost); switch in every caller (rejected: drift on 4 places).
- [Architecture] **SkillIndexEntry shape simplified to (Name, Description, Role, OutputSchema?, ActivatesWhen?).** Legacy ActivationCriteria-bag, RoleAssignments, OutputType retired together with the multi-role format. Rewriting consumers is mechanical; keeping a v2-vs-v1 split would force every consumer to handle both. -- alternatives: deprecate-and-add-v2 (rejected: drift trap; nobody pinned to legacy now post-p0127c).
- [Architecture] **TriageOutputValidator's rationale-key check goes vocabulary-only.** Pre-p0131a the validator checked rationale keys against (a) skill's Activation.Positive bag, (b) skill's RoleAssignments[role].Criteria bag, (c) the global vocabulary. Bags (a) and (b) retired with the multi-role format; the vocabulary fallback (added in p0124 for resilience) becomes the only validation. The skill-specific keys lived in legacy bags as priority signals to the prompt; with `activates_when` they're already first-class. -- alternatives: parse activates_when AST + check identifiers (rejected: validates the activation contract not the rationale; redundant with build-time `validate-concepts`).
- [Implementation] **ConceptVocabularyValidator becomes a no-op stub instead of being deleted.** The type is wired via DI in YamlSkillLoader's constructor; deleting it would cascade into a constructor-signature change + DI rebinding + 5 test files. Keeping it as a no-op (with a single LogTrace) keeps the load pipeline shape stable and gives future load-time concept checks a place to plug in. -- alternatives: delete the class + all references (rejected: pointless cascade for the size of the cleanup).
- [Implementation] **SkillBodyResolver shrunk from 82 to 19 lines.** RoleBodies branch retired (no per-role body in new format) + {{ref:X}} placeholder substitution retired (References field gone). The cache stays — body string interning per (skill, role) tuple is still cheap and removes per-call allocations. The one operator-facing skill that still has stale `{{ref:X}}` placeholders (architect-planner) has had them orphaned since p0127c shipped 2.0.0; they now render verbatim in the prompt. Not a regression — the placeholders were already non-functional.
- [TradeOff] **`_index/<category>.yaml` schema is breaking-changed.** Pre-p0131a YAML carried `roles_supported`/`activation`/`role_assignment`/`output_type` per skill. Post-p0131a it's `role`/`output_schema`/`activates_when`. Operators checking these files into git see a noisy diff on next run. Acceptable: the index is auto-generated and the operator-tooling around it is internal. -- alternatives: emit both shapes for a deprecation window (rejected: dead-code; the index is a generated artifact, not a stable contract).
- [TradeOff] **SkillMdParserExtendedTests.cs deleted entirely.** 210 lines of legacy-shape fixtures, 2 already skipped, the rest asserted rejection of legacy formats — those rejections happen via the missing-Role branch now, covered by RoleSkillDefinitionNewFormatFieldsTests.LegacyFormat_RejectedAtLoad. The `Parse_RoleAndRolesSupportedBothPresent_RejectsSkill` test was deleted from SkillMdParserNewFormatTests for the same reason: YamlDotNet's IgnoreUnmatchedProperties just drops the unknown `roles_supported` field; the rejection probe is gone.

## p0131b: BootstrapProjectHandler + CodeMapGenerator removal (D7 slice 2/4)

- [Architecture] **Cascading deletion: BootstrapProjectHandler took ~10 supporting services with it.** The entire chain was load-time-only — IProjectDetector, IRepoSnapshotCollector, IContextGenerator, IContextValidator, ICodingPrinciplesGenerator, ICodeMapGenerator, MetaFileBootstrapper, SkillYamlGenerator, ContextUserPromptBuilder, ILanguageDetector + 3 impls all lived in the BootstrapProject path. Once that path goes, they're orphans. Deleting them as a package keeps the diff coherent and avoids leaving dead-but-compilable types around. -- alternatives: keep them as dead code (rejected: code rot); split the cleanup (rejected: arbitrary slicing for one semantic cut).
- [Architecture] **CommandNames.BootstrapProject + LoadCodeMap constants kept; registered in RetiredCommands with migration hints.** Operators with custom pipeline presets in `agentsmith.yml` may still reference these step names; removing the constants would break YAML loading hard. The retired-commands path (existing pattern from p0123's ExtractFindings retirement) gives a clear boot-time error pointing operators to the right migration.
- [Architecture] **ContextKeys.CodeMap stays.** AnalyzeProjectHandler emits a YAML-ish text rendering of ProjectMap into ContextKeys.CodeMap for prompt-builders that consume the map as a single string (PlanExecution builders, SkillRoundHandlerBase, ProjectBriefBuilder). Retiring it now would force a parallel rewrite of those builders to consume the structured ProjectMap directly — out of scope. The text key continues to exist; only its writer changes (LoadCodeMapHandler retired; AnalyzeProjectHandler's existing emit path is now the single source).
- [Implementation] **TestHandler.ResolveTestCommand drops the DetectedProject branch.** Pre-p0131b TestHandler had a DetectedProject branch (populated by BootstrapProjectHandler) followed by a ProjectMap fallback. With BootstrapProjectHandler retired, DetectedProject is never populated. Falling through to ProjectMap is the natural and correct behavior; the dead branch is removed.
- [TradeOff] **InitProject preset already migrated in p0130c — no changes here.** The other 6 code-touching presets just lose the BootstrapProject + LoadCodeMap steps; the BootstrapGate (p0130a) handles missing-files failures structurally. After p0131b, the gate is the only mechanism that produces the "run init-project first" message — which is exactly what the gate was designed for. The cascading retirement of MetaFileBootstrapper / SkillYamlGenerator means there is no more silently-emitted `.agentsmith/skill.yaml` either; operators who want one have to write it explicitly.
- [TradeOff] **7 test files deleted entirely.** CodeMapGeneratorTests, ContextGeneratorTests, ContextValidatorTests, LoadCodeMapHandlerTests, ProjectDetectorTests, RepoSnapshotCollectorTests, LlmResponseHelperTests — total ~1100 lines. The deleted services were their only reason to exist. Migrating them would mean inventing replacement APIs and writing tests against ProjectAnalyzer / ProjectMap consumers — those already have their own coverage in AnalyzeProjectHandlerTests / ProjectMapPromptRendererTests.

## p0131c: LegacyTriageStrategy removal (D7 slice 3/4)

- [Architecture] **autonomous routes to StructuredTriage now, not retained on Legacy.** autonomous is `pipeline_name`-fenced (the enum doesn't include "autonomous", so PipelineNameInitializer throws ArgumentException at the head of every chain). End-to-end autonomous runs are already broken; the Legacy "no roles → Ok" branch was a silent no-op hiding the gap. StructuredTriage's HasLoadedSkills Fail-loud surfaces the gap explicitly. -- alternatives: NoopTriageStrategy for autonomous (rejected: special-casing one preset for behavior that's wrong upstream); leave Legacy in for autonomous (rejected: keeps a 135-line strategy alive for one consumer whose pipeline doesn't work today anyway).
- [Architecture] **ITriageStrategy interface kept in a one-strategy world.** Future routing decisions (provider-specific variants, replay-from-trace, different prompts per pipeline shape) will plug in via this seam without re-wiring TriageHandler / TriageStrategySelector. Removing the interface and inlining StructuredTriage saves ~10 lines today and re-creates the interface tomorrow when the first variant lands. -- alternatives: inline (rejected: short-term win, long-term churn).
- [Implementation] **TriageResponseParser deleted with LegacyTriageStrategy.** The parser was an internal helper for the legacy-discussion-prompt JSON shape (Lead + Participants + Reasoning). StructuredTriage uses ParseJson + TriageOutputDto in TriageOutputProducer with a different grammar (Phases dict, Confidence, Rationale). The parser had no other consumers. -- alternatives: keep as dead code (rejected: rot).

## p0132a: per-skill cost attribution (slice 1/3 of p0132)

- [Architecture] **Wrap-in-scope instead of full SkillCallRuntime migration.** Headline goal (PerSkillBreakdown surfaces in result.md) is achieved by BeginCall/EndCall alone; the runtime's additional machinery (LimitEnforcer / RetryCoordinator / LoopTraceCollector / OutcomeClassifier / PipelineConcurrencyGate) is valuable but orthogonal to per-skill cost visibility. The 441-line base class has two distinct paths (Discussion / Structured / Gate) that don't fit cleanly through the runtime's single-shot ExecuteAsync shape without a major restructure. Scope wrap is the minimal change that delivers operator value while keeping the runtime as a known-good future migration target. -- alternatives: full runtime migration in one slice (rejected: scope/risk for one-shot delivery); separate cost-tracker layer (rejected: BeginCall already provides this surface).
- [Implementation] **Phase mapping: PipelinePhase.Final → SkillExecutionPhase.Synthesize.** SkillExecutionPhase doesn't have a Final value; Synthesize is the closest semantic match (the Final phase IS where reviewers synthesize outputs). When PipelinePhase isn't set (legacy discussion-mode), default to Discuss. -- alternatives: add Final to SkillExecutionPhase (rejected: the enum is shaped around per-skill execution context, not pipeline lifecycle position); default to Investigate (rejected: Discuss fits mad/legal-style runs better).
- [TradeOff] **No LimitEnforcer attached to the scope.** SkillCallScope.BuildRecord falls back to elapsed-ms from StartedAt when no enforcer is attached. Cost record carries tokens (always populated via Track) + duration; ToolCallCount and LlmCallCount stay at 0 because no LimitEnforcer counts them in this path. When the full runtime migration lands, those counts populate; until then they read 0, which is honest about what the scope observed.
- [TradeOff] **Gate-path NOT wrapped in this slice.** gateRetryCoordinator.ExecuteAsync owns its own chat-loop with multiple GetResponseAsync calls in retry sequences. Wrapping it cleanly requires a coordinator-side hook (e.g. an `onResponse` callback like the RetryCoordinator already has in the runtime). Deferred — the Gate-path is shared across SecuritySkillRound + ApiSecuritySkillRound but the cost-tracker integration is the same shape as the non-Gate paths once the hook lands. p0132b can absorb this.

## p0132b: FilterRound + VerifyRound cost attribution (slice 2/3 of p0132)

- [Implementation] **Filter batches each get their own scope, not a single rolled-up filter-skill scope.** When a filter skill receives N observations exceeding the token-budget, TokenBudgetBatcher splits them and FilterBatchAsync is invoked per batch. Per-batch attribution surfaces the actual cost distribution (sometimes one batch dominates because its observations are verbose) instead of hiding it behind a rolled-up entry. -- alternatives: aggregate into one scope per filter call (rejected: hides per-batch cost variance).
- [Implementation] **Artifact-mode filter uses SkillExecutionPhase.Synthesize, not Filter.** Artifact-mode consolidates observations into a single output document — the closest enum match is Synthesize. List-mode (which actually filters) uses Filter. Same enum-mapping discipline as p0132a's MapPhase helper. -- alternatives: both modes use Filter (rejected: loses semantic distinction in PerSkillBreakdown).
- [TradeOff] **Gate-path scope still deferred.** gateRetryCoordinator owns its own chat-loop with retry semantics; cleanly wrapping requires a coordinator-side onResponse hook similar to the runtime's RetryCoordinator. The two scopes already in p0132a + the three added here cover the high-volume non-Gate paths.

## p0132c: Verifier tool access (slice 3/3 of p0132)

- [Architecture] **ResolveVerifyTools degrades to empty list when no sandbox is in context.** VerifyRoundHandler is consumed by RunVerifyPhase which runs only in FixBug + AddFeature today — both preset paths have a sandbox after CheckoutSource. The test suite exercises the handler with sandbox-less PipelineContexts; failing-loud there would force every existing test to seed a sandbox. Graceful degradation matches p0129a/b's design — verifiers can do both static and live work depending on what they're given. -- alternatives: fail-loud when sandbox missing (rejected: too rigid; static-mode verifiers are still valuable); inject sandbox factory + create one inline (rejected: sandbox lifecycle is the pipeline's job).
- [Architecture] **Tool-enabled chat-client reused across verifiers in a single RunVerifyPhase invocation.** Chat-client creation is cheap, but creating one per verifier would mean N FunctionInvokingChatClient wraps when one suffices — the underlying connection + function-invocation loop is stateless across calls. Same pattern the non-tool path already uses (per-round, not per-verifier).
- [TradeOff] **ProjectBuildCommandResolver deferred.** SKILL.md bodies instruct the LLM to discover the build/test command from `.agentsmith/context.yaml` (`stack.testing` etc) directly. The LLM has read_file in its read-only toolset, so it can resolve the command without a dedicated resolver service. Building one prematurely risks shaping it around assumptions that haven't been validated against operator runs. -- alternatives: ship ProjectBuildCommandResolver now (rejected: speculative design); hardcode commands per project_language (rejected: bypasses operator-config in agentsmith.yml).
- [TradeOff] **Post-verify Test command in FixBug preset stays.** During the rollout window, the dedicated Test command still runs after Verify so live-mode false-negatives in the verifier path (LLM decides not to invoke RunCommand, or RunCommand fails for an unrelated reason) get caught by the real test run. Operator-evidence-driven follow-up can drop the explicit Test step once verifiers prove they cover the case reliably.

## PR #93: Central path resolvers (post-p0132c follow-up)

- [Architecture] **`IAgentSmithPaths` for host-side paths, `ProjectMetaPaths` for repo-relative ones.** Two distinct domains needed centralisation: cache locations outside any project (skill catalog, project-map cache) and the `.agentsmith/X` convention inside a project's working tree. They live in separate types because their lifecycles are different — host paths resolve once at process start and never change; project-meta paths are textual constants that travel into PR diffs. Bundling them would obscure that distinction. -- alternatives: one `IPaths` umbrella interface (rejected: mixes runtime-resolved values with compile-time constants); leave .agentsmith/X as scattered string literals (rejected: 8 sites, future rename = 8-file change instead of 1).
- [Architecture] **`DefaultPaths` (static in Contracts) is the single source of cache-root resolution.** Both `AgentSmithPaths` (DI-friendly impl in Infrastructure.Core) and the YAML loader's pre-DI defaulting need the same XDG resolution rules. Putting them in a shared static helper in Contracts (which neither layer dominates) makes drift between the call sites impossible. -- alternatives: duplicate the logic with a consistency test (rejected: tests catch drift after the fact, the right shape prevents it); make `SkillsConfig.CacheDir` lazy-resolved through DI (rejected: deserialiser eagerness + every test fixture would need DI plumbing).
- [Implementation] **SkillsConfig becomes pure data; YAML loader fills `CacheDir` from `IAgentSmithPaths.SkillsCatalogRoot` post-deserialise.** The earlier shape (`CacheDir { get; set; } = ResolveDefaultCacheDir();` calling a static method on its own class) mixed static + instance members on one type and ran a side-effecting initialiser at construction time. Pure-data + loader-fills cleanly separates "what the operator said" from "what the runtime resolved", and keeps the type single-responsibility.
- [Implementation] **`ProjectCacheDir` keys by 8-byte SHA-256 of the remote URL.** Short enough to be tab-completable; wide enough that a single operator's repo set will never collide. The repo identity is `RemoteUrl` (not local path) so two checkouts of the same upstream share a cache entry — matches the "did the dep manifests change?" cache invalidation model.
- [TradeOff] **`AgentSmith.Contracts` gains a small amount of static utility code (`DefaultPaths`, `ProjectMetaPaths`).** Contracts is conventionally interfaces-only, but these two are pure data + pure path math — no I/O, no allocations, no providers. Moving them into Infrastructure would force every consumer (including the SkillsConfig YAML default) to take a runtime dependency on Infrastructure, which violates the layering. Keeping them in Contracts is the lesser of two evils. -- alternatives: dedicate a new `AgentSmith.Paths` project (rejected: one-class projects bloat the solution); inline both back into `AgentSmithPaths` and live with the duplication for SkillsConfig (rejected: that's what we just had).
- [TradeOff] **Tmp-path usages NOT centralised in this pass.** Eight call sites use `Path.Combine(Path.GetTempPath(), "agentsmith-...-{guid}")` for one-shot scratch dirs (sandbox workdir, tool runners, ZAP swagger handoff, draft skill staging). Each has its own lifetime + naming convention; an `IAgentSmithPaths.NewTempDir(prefix)` helper would save little while adding indirection. -- alternatives: add a TempDir method to IAgentSmithPaths (rejected: thin wrapper around `Path.GetTempPath()`, no shared invariant to enforce).

## p0133: Init-project label-trigger onboarding

- [Architecture] **Init-project triggered via existing pipeline_from_label across all four platforms — no new endpoint, no k8s-specific operator logic.** The label-trigger primitive is universal since p0083/p0084 (webhook) and p0108 (polling); `PipelineFromLabel` is a plain `Dictionary<string, string>` with no preset whitelist (PipelinePresets.cs already registers `init-project`). A separate onboarding mechanism would fragment the operator surface. -- alternatives: separate HTTP init endpoint (rejected: extra trigger path without operator benefit); CLI-only init (rejected: breaks for k8s operators without cluster shell); init-container in Helm chart (rejected: too invasive for what a pipeline already does).
- [Architecture] **InitCommitHandler reads TicketId directly via `pipeline.TryGet`, no FetchTicket added to InitProject preset.** The spec originally proposed adding FetchTicket to the preset, but `FetchTicketContextBuilder.cs:14` does `pipeline.Get<TicketId>` which throws `KeyNotFoundException` when the key is missing — so a Slack-modal / CLI init (no TicketId) would crash the preset. Making FetchTicket null-tolerant would touch a handler used by 5 other presets and risk hiding bugs there. TicketId is already published by `ExecutePipelineUseCase.cs:84` when `WebhookResult.TicketId` is set; `DoneStatus` is published via `InitialContext`; `TicketConfig` is already in `InitCommitContext`. The id-only path is sufficient for `UpdateStatusAsync` + `TransitionToAsync` / `CloseTicketAsync`. -- alternatives: add FetchTicket to InitProject preset + make it null-tolerant (rejected: 5-preset blast radius); add a new conditional `LoadTicketIfPresent` step (rejected: 95% duplicate handler for a 5-line conditional).
- [Architecture] **InitCommitHandler EXTENDED for ticket lifecycle, not REPLACED by CommitAndPRHandler.** InitProject needs commit message `chore: initialize .agentsmith/ directory` regardless of trigger source; CommitAndPRHandler hard-codes `fix: {Ticket.Title} (#{Ticket.Id})`. Forcing CommitAndPR into InitProject would either branch its commit-shape on pipeline name (conditional-handler smell that p0130a explicitly rejected for the BootstrapGate categorization) or surprise bug-fix operators with renamed PR semantics. Cleaner shape: one handler per commit-shape; both call the same finalize helper. -- alternatives: replace InitCommit with CommitAndPR + branch on pipeline name (rejected: conditional commit shape per pipeline = drift); duplicate the lifecycle code in both handlers (rejected: drift risk for a 15-line block).
- [Implementation] **`TicketLifecycle.FinalizeAsync` is a static helper, not an injected service.** No state, no DI dependencies beyond what the callers already have (`ITicketProviderFactory`, `TicketConfig`, ids/strings). A static method on a public class keeps the call sites readable and unit-testable through the existing `Mock<ITicketProviderFactory>` plumbing. -- alternatives: `ITicketLifecycle` interface + DI registration (rejected: ceremony for a function with no state); inline-duplicate in both handlers (rejected: drift).
- [Implementation] **`agent-smith:init` label string, parallel to `agent-smith:fix` / `agent-smith:feature` short forms.** Four labels for the operator to learn, not five. The string is configurable per project via `pipeline_from_label` — operators preferring `agent-smith:init-project` can override without code changes. -- alternatives: `agent-smith:init-project` (rejected: outlier in the short-form set); `agent-smith:bootstrap` (rejected: collides with the BootstrapGate's "bootstrap files" message — operator confusion).
- [Implementation] **Smoke test covers all four platforms in one file.** Routing is config-driven and the four webhook resolvers (`GitHubIssueWebhookHandler.cs:107`, `GitLabIssueWebhookHandler.cs:109`, `AzureDevOpsWorkItemWebhookHandler.cs:110`, `JiraAssigneeWebhookHandler.cs:146`) share a structurally identical dict-lookup shape — one platform proves the routing primitive, but four platforms catch drift if any one resolver ever diverges. The maintenance cost is low (single test file, one helper per platform's payload shape). -- alternatives: one platform only (rejected: drift between resolvers would silently break onboarding for users who only deploy that platform); per-platform smoke files (rejected: scattered, harder to maintain in lockstep).
- [TradeOff] **`agent-smith:init` placed FIRST in `pipeline_from_label` in the bundled examples.** Match order is dict-insertion order. Operators discovering the onboarding flow by reading the bundled config see init before fix / feature / scan — which is the order they need to run them in for a fresh repo. -- alternatives: alphabetical (rejected: hides the onboarding step); last (rejected: same problem); ordering ignored at runtime (rejected: that's a config-loader change).
- [TradeOff] **p0029 Slack-modal / chat-text init path stays in place, unchanged.** Audit (`notes/init-trigger-audit.md`) confirmed it's still wired end-to-end: `ChatIntentParser` → `InitProjectIntent` → `InitProjectIntentHandler` → `JobRequest { PipelineOverride = "init-project" }`. No ticket is published from this path; `InitCommitHandler`'s lifecycle branch reads `TryGet<TicketId>` which simply returns false. Both trigger paths coexist (label = universal; Slack = ChatOps comfort) without conflicting code or duplicated logic.

## p0135: Sandbox toolchain auto-detection

- [Architecture] **Resolution order: override → host-cache → remote-context-yaml → in-memory ProjectMap → generic.** The override is deterministic operator intent and wins outright. Host-cache (`project-map.json` under `IAgentSmithPaths.ProjectCacheDir(source.Url)`) is fast + local + present after the first `AnalyzeProjectHandler` run on a repo, so it precedes the REST roundtrip. Remote `.agentsmith/context.yaml` precedes in-memory `ProjectMap` because in-memory is structurally null at sandbox-create time (the pipeline loop hasn't run yet) — the slot stays for symmetry, not because it ever fires today. Generic fallback remains so init-project + scan pipelines keep working on repos that have no language signal yet. -- alternatives: cache after remote (rejected: local-and-fast first); drop remote-context-yaml (rejected: a fresh server pod with cold cache + a bootstrapped repo would be language-blind on every fix-bug); fail-loud on unknown language (rejected: init-project + scan pipelines legitimately run on the generic toolchain).
- [Architecture] **`ISourceProvider.TryReadFileAsync(path)` as a new interface method, not a separate helper service.** Each provider has a fundamentally different file-read API — AzDO `GitHttpClient.GetItemContentAsync`, Octokit `Repository.Content.GetAllContentsByRef`, GitLab REST raw-file endpoint, Local `File.ReadAllTextAsync`. A central helper would re-switch on provider type and own all four code paths. Returns `string?` (raw content) — parsing is the caller's job; the provider contract stays minimal. -- alternatives: `IFileReader<TProvider>` service family (rejected: ceremonial DI for what is already a provider concern); expose a full repo-content interface (rejected: scope creep, p0023 multi-repo can extend it later).
- [Implementation] **Resolver returns `string? language`, not a partial `ProjectMap`.** `ProjectMap` is a sealed record with seven required fields; constructing a partial one full of empty defaults leaks empty `Modules`/`TestProjects`/`Conventions` into a code path that only consumes `PrimaryLanguage`. The new resolver returns just the language string, and `SandboxSpecBuilder` gained a `Build(ProjectConfig, string? language)` overload — the existing `Build(ProjectConfig, ProjectMap?)` overload delegates to it with `projectMap?.PrimaryLanguage` so the in-memory branch keeps working. -- alternatives: synthesize partial ProjectMap (rejected: misleading shape); new `LanguageHint` record (rejected: viral type proliferation for a single string).
- [Implementation] **Default-branch resolution stays on the provider, not the resolver.** Each provider already exposes `GetDefaultBranchAsync` (with cache) for `CreatePullRequestAsync` — `TryReadFileAsync` reuses it internally. The resolver passes only the file path; the provider picks the branch. Keeps `SandboxLanguageResolver` agnostic of provider URL shapes and avoids a parallel default-branch fetch path. Layer 2b therefore reads from the default branch — at sandbox-create time the work-branch doesn't exist yet (CheckoutSource hasn't run) and the default branch is the contract surface init-project's PR merged into. -- alternatives: resolver computes the default branch (rejected: duplicates provider knowledge); pass branch as resolver-API parameter (rejected: leaks branch concerns into the resolver for a value the provider already has).
- [Implementation] **Resolution-layer names are a closed enum (`SandboxToolchainResolutionLayer`), not free-form log strings.** The pipeline-start log line names which layer hit; if layer names drift across the code-base, operator log-grep / dashboards break silently. The enum forces every call site to pick from the same set — `Override`, `HostCache`, `RemoteContextYaml`, `InMemoryProjectMap`, `GenericFallback`. -- alternatives: const strings (rejected: easier to typo, no compiler check); inline literals (rejected: three different spellings in six months).
- [Implementation] **`ISandboxLanguageResolver` interface extracted for test mocking.** Moq can't mock sealed concrete classes, and the three existing `PipelineExecutor*Tests` files need a stubbed resolver to set up their executor. Hand-rolling a stub class per test is more code than a tiny interface; un-sealing the resolver would invite subclassing as a real use case. The interface is internal-shape (one async method) and the cost is one extra file. -- alternatives: un-seal `SandboxLanguageResolver` (rejected: subclassing is not a real use case); hand-roll a stub (rejected: duplicates the contract in three test files).
- [TradeOff] **YAML parsing of `stack.lang` lives in `SandboxLanguageResolver`, using YamlDotNet's `UnderscoredNamingConvention`.** Application layer gains a YamlDotNet `PackageReference` (already in Infrastructure). The alternative was to parse via raw string-matching to avoid the dependency, but that breaks on quoting, comments, and nested key ordering — operators write context.yaml by hand and the format already uses YamlDotNet's snake-case convention elsewhere. YamlDotNet's strict deserializer raises on non-scalar `stack.lang` (e.g. a list), which the resolver catches and treats as "no language detected" — falls through to GenericFallback rather than throwing. -- alternatives: hand-rolled regex parser (rejected: brittle); pull the parse helper out of Infrastructure (rejected: Application calling into Infrastructure inverts the layering).
- [TradeOff] **Cache-stale tolerance: read `PrimaryLanguage` from `project-map.json` even when the on-disk cache key shows stale manifest hashes.** `PrimaryLanguage` is a near-immutable repository attribute — a .NET project doesn't become a Node project between runs. Manifest-hash staleness matters for module/test-project enumeration (AnalyzeProjectHandler re-runs the analyzer in that case) but not for toolchain image selection. -- alternatives: enforce cache-key match (rejected: produces unnecessary cache-misses on every `dotnet add package`); refresh the cache opportunistically (rejected: scope creep into AnalyzeProjectHandler's territory).
- [TradeOff] **`SandboxSpecBuilder.LanguageImages` dictionary extended with operator-facing aliases (`C#`, `.NET 8`, `TypeScript`, `Node.js`, `golang`).** Cache writes use lowercase canonical strings (`csharp`, `node`, `typescript`), but `stack.lang` in operator-edited `.agentsmith/context.yaml` uses the human-facing names. Adding both sides to the same `OrdinalIgnoreCase` map is one new row per alias and prevents resolver-callers from needing a normalisation step. -- alternatives: normalise inside the resolver (rejected: hides the mapping from the spec-builder which is the actual decision point); maintain two maps (rejected: drift risk).
- [TradeOff] **GitHub + Azure DevOps `TryReadFileAsync` unit tests deferred.** Both providers construct their underlying SDK client (`Octokit.GitHubClient`, `VssConnection.GetClient<GitHttpClient>()`) internally, with no injection seam. Unit-testing them at the same level as Local + GitLab would require a constructor-injection refactor (factory delegate or `IConnection` parameter) that touches every call site in Infrastructure — scope creep beyond p0135. Local (filesystem) and GitLab (`HttpClient` already injectable) cover the four canonical cases — `ExistingFile_ReturnsContent` / `FileNotFound_ReturnsNull` / `ServerError_Throws` / `AuthError_Throws` (Local replaces auth with `Canceled`). The Octokit/VssConnection wrappers are thin enough that the contract surface they implement is exercised by the resolver's RemoteContextYaml-hit test against the abstract interface. Logged as a follow-up so a future refactor doesn't pretend the testability gap was never noticed. -- alternatives: inject SDK clients via constructor delegates now (rejected: scope creep); wire in `WireMock` for HTTP-level testing (rejected: dependency churn for two test files); skip silently (rejected: gap should be visible in the log).

## p0135 follow-up: GitHub + Azure DevOps client-factory injection

- [Implementation] **`IGitHubClientFactory` + `IAzDoClientFactory` close the testability gap deferred in p0135.** Both providers used to hard-`new` their SDK clients (`new GitHubClient(...)`, `new VssConnection(...)`) inside private methods, so the four `TryReadFileAsync` cases were untestable at the unit level (logged at end of p0135 as "deferred until injection seam exists"). New factory interfaces live in `Infrastructure/Services/Providers/Source/`, registered as DI singletons. `SourceProviderFactory` resolves them and passes the per-provider instance into each provider's ctor. -- alternatives: `Func<>`-delegate parameter on the provider ctor (rejected: pretty, but every future telemetry/retry decorator would have to wrap a delegate instead of an interface — the interface is the conventional seam in this codebase); `internal virtual` + `InternalsVisibleTo` (rejected: un-seals the providers and pushes test concerns into the production type hierarchy).
- [Implementation] **`IGitHubClientFactory.Create` returns `IGitHubClient` (Octokit's interface), not `GitHubClient`.** Octokit's main type tree is interface-first (`IGitHubClient` → `IRepositoriesClient` → `IRepositoryContentsClient`) which Moq mocks cleanly. Returning the concrete `GitHubClient` would have forced tests to either `Mock<GitHubClient>(ProductHeaderValue)` (its properties aren't all virtual) or stand up a fake `IConnection` — both heavier than just typing the seam at the interface. Production `DefaultGitHubClientFactory` still constructs a real `GitHubClient` and returns it via the interface.
- [Implementation] **`IAzDoClientFactory` exposes three methods (sync git client, async git client, work-item client), not just `Create(VssConnection)`.** Each AzDo SDK client is obtained via a different `VssConnection.GetClient<T>()` call shape (sync vs async) and from different connection objects in the existing provider code (PR-creation path uses sync, comment-thread path uses async). Surfacing the three explicit methods means each call site keeps its existing shape and tests can mock the one method they need — instead of having to stub the full `VssConnection` (which is also a concrete class with a non-trivial ctor). -- alternatives: factory returns `VssConnection` (rejected: pushes the SDK boilerplate back into the provider AND makes mocking harder); collapse async into sync (rejected: would change CreatePullRequestAsync's call shape, scope creep).
- [Implementation] **AzDo test pins the exact 14-parameter `GetItemContentAsync` overload in a helper method.** The AzDo SDK ships four `GetItemContentAsync` overloads on `GitHttpClientBase` (`{string, Guid}` project × `{string, Guid}` repositoryId) plus four older 13-param variants on `GitCompatHttpClientBase` marked `[Obsolete]`. The production call uses named arguments — the compiler picks the non-obsolete 14-param `(string, string, ...)` overload. Centralising the Moq `Setup(c => c.GetItemContentAsync(It.IsAny<...>() × 14))` in `SetupGetItemContentAsync` keeps the four `[Fact]`s readable and makes a future SDK-version bump (param-count or nullability changes) a one-line fix. -- alternatives: inline Setup per test (rejected: 14-line block × 4 tests, signal-to-noise tanks); match on partial parameters (rejected: Moq's expression tree binds to a specific overload, partial match isn't possible).

## p0136: Per-project sandbox container resources via IOptions

- [Architecture] **Target the Sandbox path (ISandboxFactory / PodSpecBuilder / DockerContainerSpecBuilder) only — not the legacy IJobSpawner / KubernetesJobSpawner CLI-image path.** The CLI-image spawn path runs `agentsmith-cli:latest` jobs and is dev-tool-only; production spawns toolchain containers via ISandboxFactory (p0115/p0116/p0135). Configurability work on a path being phased out is wasted; the JobSpawner path keeps its current hardcoded values until it is removed or repurposed. -- alternatives: configure both paths in parallel (rejected: doubles surface area for a path that is going away); configure only KubernetesJobSpawner (rejected: opposite of the target architecture).
- [Architecture] **Resource override location: `projects.<name>.sandbox.resources` in agentsmith.yml.** SandboxConfig already exists under `projects.<name>.sandbox` with ToolchainImage; resources are semantically a peer concern (override behaviour of the same container) and live where operators already look for sandbox tuning. -- alternatives: `projects.<name>.resources` top-level (rejected: implies scope beyond the sandbox container, e.g. the server pod); `projects.<name>.runtime.resources` (rejected: introduces a 'runtime' block with only one inhabitant).
- [Architecture] **Per-project resolution returns a fully-populated `ResourceLimits`, never partial overrides.** An operator setting only `memory_limit` and leaving `cpu_limit` unset is ambiguous — does it inherit global `cpu_limit`, or fall back to the type default? Forcing the resolver to materialise all four fields makes the spawn-time value predictable: per-project block wins wholesale → otherwise global SandboxOptions defaults apply → otherwise `ResourceLimits.Default`. -- alternatives: per-field override-with-fallback (rejected: each field independently inherits, producing combinations operators don't expect); reject partial blocks at config load (rejected: harsher than needed — global defaults already make 'partial' a non-problem).
- [Architecture] **SandboxOptions lives in `AgentSmith.Application/Models/`, not in Server.** The resolver (`SandboxResourceResolver`) lives in Application/Services/Sandbox/ next to its peer `SandboxLanguageResolver`, and it injects `IOptions<SandboxOptions>`. Putting the options class in Server would invert the dependency direction (Server → Application is the rule; Application → Server is forbidden). The Server side wires the binding (`services.Configure<SandboxOptions>(config.GetSection("Sandbox"))`) but the type itself belongs in the layer that consumes it. -- alternatives: SandboxOptions in `AgentSmith.Server/Models/` (rejected: layering violation); SandboxOptions in `AgentSmith.Contracts/Models/Configuration/` (rejected: Contracts holds agentsmith.yml-derived schema; appsettings-derived IOptions belongs with the consumer).
- [Implementation] **`ResourceLimits` is a record with mutable properties + a parameterless ctor + a 4-arg ctor, NOT a positional record.** A positional record (`public sealed record ResourceLimits(string CpuRequest, ...)`) generates `init`-only properties and no parameterless ctor — YamlDotNet can't deserialize the `projects.<name>.sandbox.resources` block into one. Mutable `get; set;` properties with both ctors let YamlDotNet construct + populate AND let callers do `new ResourceLimits("250m", "1000m", "512Mi", "2Gi")` in code. Record-derived value-equality is preserved for test assertions like `.Should().Be(ResourceLimits.Default)`. -- alternatives: positional record + custom YamlDotNet INodeDeserializer (rejected: bespoke deserializer for one type when the convention works); plain class with overridden Equals/GetHashCode (rejected: re-implements record-equality boilerplate by hand).
- [Implementation] **Adopt `IOptions<T>` in this phase (first IOptions touchpoint in Server).** The existing `AddJobSpawnerOptions` read each env-var manually with `??` fallbacks — no appsettings binding, no validation. `services.Configure<T>` gives appsettings + env-vars (with `Section__Key` convention) + CLI binding for free, plus `IValidateOptions<T>` hooks. Sets the precedent for future Options classes. -- alternatives: keep the manual env-var pattern for consistency (rejected: consistency with the worse pattern is not a virtue); convert every Options POCO in Server first (rejected: scope creep across RedisOptions/SlackOptions/etc.).
- [Implementation] **Two Options classes — `SandboxOptions` (new) and `JobSpawnerOptions` (existing, converted in-place).** SandboxOptions belongs to the Sandbox path this phase actually configures; JobSpawnerOptions belongs to the legacy IJobSpawner path. Converting JobSpawnerOptions to IOptions<T> ships under the same umbrella as a pattern-cleanup — no functional change beyond binding semantics. `AddJobSpawnerOptions` keeps the legacy env-vars (K8S_NAMESPACE etc.) as `services.Configure<T>(opts => { ... })` defaults; a second `services.Configure<T>(config.GetSection("JobSpawner"))` runs after and wins for any key set in appsettings. -- alternatives: merge both into one Options class (rejected: bundles two different concerns); skip JobSpawnerOptions conversion (rejected: leaves the codebase with two competing Options patterns).
- [Implementation] **`SandboxSpec.Resources` is positional-required, no longer nullable with a `?? new ResourceLimits()` fallback.** `SandboxSpec` carries the resolver-output across the Application→Server seam; making Resources required forces every construction site (`SandboxSpecBuilder.Build`, test fixtures) to either go through the resolver or pass an explicit value. PodSpecBuilder and DockerContainerSpecBuilder both lose their null-coalescing fallback — if a future caller bypasses the resolver they get a compile error, not silent default behaviour. -- alternatives: keep `ResourceLimits?` and have the consumers fall back to Default (rejected: hides the bypass, exactly the situation the resolver is meant to prevent).
- [Implementation] **`PodSpecBuilder.BuildToolchainContainer` now emits BOTH Requests and Limits.** Pre-p0136 the toolchain container only had Limits set — meaning in K8s namespaces with a ResourceQuota that mandates `requests.cpu` / `requests.memory`, pod creation failed before the workload started. The new code emits the four explicit quantities from `ResourceLimits`. The `agent-loader` initContainer keeps its hardcoded values (p0135 follow-up rationale: loader extracts the bundled .NET single-file binary, not operator-tunable). -- alternatives: infer requests = limits at PodSpecBuilder level (rejected: collapses Burstable QoS, removes operator control over headroom).
- [Implementation] **`DockerContainerSpecBuilder` maps K8s quantities onto Docker HostConfig via `NanoCPUs`/`Memory`/`MemoryReservation`.** CpuLimit → NanoCPUs (hard CPU cap), MemoryLimit → Memory (hard memory cap), MemoryRequest → MemoryReservation (soft floor). Docker has no first-class request/limit split for CPU — `CpuShares` is a weight, not a request, so CpuRequest is intentionally not mapped (a `MemoryRequest`-equivalent for CPU does not exist in the Docker API). The two existing helpers were renamed (`ParseMemory` → `ParseMemoryToBytes`, new `ParseCpuToNanoCpus`) and the latter handles both bare-number-cores (`"2"`, `"1.5"`) and millicore (`"500m"`) inputs. -- alternatives: pull in cgroup-v2 CPU.weight as a CpuRequest equivalent (rejected: Docker DotNet doesn't surface it cleanly); skip Docker entirely (rejected: scope creep — Docker is a first-class sandbox backend).
- [Tooling] **`JobSpawnerOptions` extracted to its own file in Server/Models/.** Pre-phase it lived at the bottom of `KubernetesJobSpawner.cs` — a violation of the project's "one type per file" coding-principles rule that was overdue. Since this phase reworks the consumers of the class anyway, splitting it now is a one-file-add zero-risk cleanup that brings the layout in line with `SandboxOptions` (and every other Options-style class). -- alternatives: leave it where it was (rejected: coding-principles violation called out in CLAUDE.md is non-negotiable); split as a separate cleanup phase (rejected: ceremony for a five-line move that costs nothing to do under the same commit).

## p0138: Triage prompt rewrite + coding-catalog gap fix

- [Architecture] **Root cause is a stale prompt, not a missing validator branch.** `triage-structured-system.md` referenced `activation.positive`, `role_assignment[role].positive/negative`, and `roles_supported` — three SkillMdFrontmatter fields p0131a removed. The LLM had no defined key-source for rationale tokens and imitated the example, citing invented keys. Tightening the validator without fixing the prompt would produce 'failed harder' — the prompt itself must encode the post-p0131a constraints. -- alternatives: relax TriageOutputValidator to warn on vocab violations (rejected: the role-slot constraint is semantic, fail-loud is correct; vocab-as-contract is worth keeping); add a stricter retry message (rejected: retries should converge, not contradict the first attempt — the first message should already say the right thing).
- [Architecture] **Coding catalog gets 4 planner skills + 1 filter skill — consistency with the existing investigator/judge pairs trumps an architect-planner-covers-backend exception.** Every domain category (architect, backend-developer, frontend-developer, dba, devops, security-reviewer, tester) ends up with a {investigator, judge, planner} triple plus the verifier family. An exception 'architect-planner handles backend' is inferrable from descriptions but not from the catalog's structural shape — the LLM (and future readers) are more likely to pick the right Lead when the naming convention is fully regular. -- alternatives: 3 planners + architect-planner description tweak (rejected: structural irregularity bites the next-largest reader); collapse all planners into one generic planner (rejected: existing planners have non-overlapping output shapes — architect emits 'pattern + reason + files', dba emits 'schema + migration + integrity', security-reviewer emits 'threat + control + verification').
- [Implementation] **Catalog version 2.0.1 → 2.1.0, not 2.7.0 → 2.8.0 as the spec said.** The spec's 2.7.0 figure was carried over from a stale read of context.yaml; the actual catalog had been re-versioned through release-please at v2.0.0 (BREAKING change for the SKILL.md format flip) + v2.0.1 (validator fixes). 2.0.1 → 2.1.0 is the conventional minor bump for additive features. release-please will materialise the bump automatically from the `feat:` commit on merge to skills-repo main; manual CHANGELOG.md / version.txt edits are intentionally not in this PR. -- alternatives: 2.0.1 → 2.0.2 patch bump (rejected: 5 new skills are features, not patches); 2.0.1 → 3.0.0 major (rejected: additions are backwards-compatible, agent-smith pin stays in the >=2.0.0 range).
- [Implementation] **Description field follows the addyosmani/agent-skills convention of doubling as an activation hint.** Their format is minimal (`name` + `description`) with descriptions starting 'Use when …' as routing guidance. We keep our richer frontmatter (role + output_schema + activates_when are load-bearing for the pipeline), but every new planner's description opens with 'Lead when X. Analyst/reviewer when Y.' so the LLM has the role-selection heuristic at a glance instead of inferring from the body. Filter opens with 'Use in Final phase to …'. Existing 3 planners stay unchanged this phase. -- alternatives: rewrite all existing planner descriptions in lockstep (rejected: scope creep + churn on already-working skills); skip the convention (rejected: the addyosmani comparison surfaced it as a concrete cheap improvement).
- [Implementation] **`BuildTokens` signature changes from `(TriageInput)` to `(TriageInput, ConceptVocabulary)`.** The vocabulary loads in `ProduceAsync` (line 50-53) and gets passed to `CallAndValidateAsync` for validator use, but pre-p0138 was never injected into the prompt template — the user prompt couldn't show the LLM the legal key set. Adding a parameter to `BuildTokens` (rather than threading vocabulary into `TriageInput`) keeps `TriageInput` as the ticket-and-skills shape and uses the parameter list to surface 'BuildTokens depends on both input + vocabulary'. -- alternatives: add `Vocabulary` field to `TriageInput` (rejected: bloats the record with a value already in scope at the call site); render vocabulary inside `RenderSkills` (rejected: conflates two distinct content blocks).
- [TradeOff] **Catalog smoke test deferred — architecture + canonical-example tests cover the failure modes that bit us.** The spec listed `YamlSkillLoaderCatalogTests.CodingCatalog_v2_8_0_LoadsAllPlannersAndFilter` which would load real v2.1.0 SKILL.md files via fixture. The two tests landed (`TriagePromptArchitectureTests`×6 + `TriagePromptCanonicalExampleTests`×1) cover the higher-value failure modes (stale field references reintroduced, role-slot mapping documentation lost, canonical example violates its own constraints). The catalog-load test would just re-verify YamlSkillLoader works on the standard format — already covered by existing `YamlSkillLoaderTests`. Logged as a deferred-to-followup item in the phase's done section. -- alternatives: write the catalog test inline (rejected: time-cost > value-add given the existing coverage); skip both new test classes (rejected: the architecture test is the only regression-guard against the bug class that just bit us).

## fix: CLI api-scan source→sandbox handoff (hotfix)

- [Architecture] **`SandboxSpec` gains an optional `InitialSourcePath` — host-filesystem dir for InProcessSandbox to use as `/work` instead of creating a fresh empty temp dir.** Reason: api-security-scan's `TryCheckoutSourceHandler` clones host-side via IHostSourceCloner and publishes the path on `ContextKeys.SourcePath`. Pre-fix, the InProcessSandboxFactory ignored this and minted a separate empty temp dir as `/work` — handlers reading `/work/.agentsmith/context.yaml` reported `False/False` even though the cloned source contained the bootstrap files. The field is honored only by InProcessSandboxFactory; container-based factories (Docker/Kubernetes) ignore it because they have their own in-sandbox source-loading mechanism (CheckoutSourceHandler runs `git clone` directly into the sandbox volume). -- alternatives: TryCheckoutSourceHandler copies/symlinks into sandbox /work after sandbox creation (rejected: requires ISandbox to expose its workDir or a recursive file-copy via ISandboxFileWriter — both invasive); recreate sandbox after TryCheckoutSource (rejected: breaks `await using` lifetime + double-creation cost).
- [Architecture] **Sandbox creation in `PipelineExecutor.RunAsync` is now lazy — built only when the first sandbox-requiring command in a batch comes up, instead of upfront at loop entry.** Reason: TryCheckoutSourceHandler is a sandbox-free, host-side command that must run BEFORE the sandbox is built (it publishes the source path the sandbox needs). Pre-fix's upfront construction made the SourcePath read in `TryCreateSandboxAsync` always see an empty context. Lazy-init defers construction past TryCheckoutSource; the sandbox is built at the first sandbox-requiring command's turn, with the published path already in scope. -- alternatives: split RunAsync into pre-sandbox + post-sandbox phases (rejected: more code surface for the same effect); Lazy<Task<ISandbox>> wrapper (rejected: adds indirection that buys nothing).
- [Implementation] **`TryCheckoutSource` removed from `SandboxRequiringCommands` HashSet.** It was a latent miscategorisation — TryCheckoutSourceHandler's ctor takes no `ISandbox` and writes only to host-filesystem via IHostSourceCloner. Listing it forced upfront sandbox creation even though the handler never used it. Removal is semantically correct AND load-bearing for the lazy-init flow (sandbox creation now defers past TryCheckoutSource). -- alternatives: keep it in the list and special-case (rejected: list = "uses sandbox at runtime", special case is a lie about that contract).
- [Implementation] **`BootstrapCheck` added to `SandboxRequiringCommands` HashSet.** Pre-existing latent dependency that was invisible under upfront sandbox creation — BootstrapCheckHandler always saw a ready ISandbox because some later command in the same preset triggered the upfront creation. With lazy init, an early BootstrapCheck would run before any later sandbox-requiring command and trip the handler's `requires an active sandbox` guard. Adding it to the trigger list closes the gap. -- alternatives: have BootstrapCheck silently no-op when sandbox is absent (rejected: returning False/False on missing sandbox would mask config errors); leave it implicit (rejected: the trigger list is the contract, implicit deps will rot the next time someone reorders a preset).

## p0139: Named catalogs for agents/repos/trackers/pipeline_triggers

- [Architecture] **Four top-level catalogs, not three — trackers carry their own connection metadata.** `trackers:` is the third extracted block alongside `agents:` and `repos:`. Pre-p0139 every project re-inlined the same tracker org/url/auth/open_states/done_status. Extracting it removes that duplication symmetrically with agents+repos; doing trackers inconsistently (agents+repos extracted, trackers inlined) would have re-introduced the duplication problem we set out to solve. The fourth catalog `pipeline_triggers:` (label→pipeline) lives at top level because the same 4-5 entries were repeated on every project's trigger block. -- alternatives: agents+repos only with trackers staying inline (rejected: inconsistent and reintroduces the same duplication problem); per-project pipeline_triggers override (rejected for v1 — global only until a real cross-cutting need shows up).
- [Architecture] **Projects reference catalog entries BY NAME only — inline definitions explicitly not supported.** A dual-mode loader (name OR inline) would double the validation surface and let duplication creep back in unnoticed. The catalog IS the entry point; a project that wants a 'unique agent for just this project' adds a one-off entry to `agents:` and references it by name. -- alternatives: dual-mode loader (rejected: scope creep + validation pain); inline-only with a `shared:` aliasing layer (rejected: aliases are a worse spelling of catalog references).
- [Architecture] **`AgentSmithConfig.Projects` exposes `IReadOnlyDictionary<string, ResolvedProject>` — raw YAML DTOs stay loader-internal.** Consumers always see fully-bound records (Repo, Tracker, Agent already resolved by the time anything outside the loader sees the config). The Raw* DTOs (`RawAgentSmithConfig`, `RawRepoEntry`, `RawTrackerEntry`, `RawProjectEntry`) live in `AgentSmith.Infrastructure.Core/Services/Configuration/` and are only used during the YamlDotNet deserialize step. -- alternatives: expose raw config + a separate `ResolveProject(name)` method (rejected: every consumer would have to call resolve; smallest call-site diff is to expose the resolved view directly); single bidirectional ProjectConfig with optional name-string AND optional inline blocks (rejected: dual-mode complexity).
- [Architecture] **Resolver lives in Infrastructure.Core, not Application (spec called for Application).** ConfigCatalogResolver consumes raw infrastructure DTOs (`RawRepoEntry` etc.). Putting it in Application would either invert the layer dependency (Application → Infrastructure.Core is forbidden) or force the Raw* DTOs into Contracts (where they don't belong — they're a YAML-binding implementation detail). The resolver naturally lives next to the loader. AgentSmithConfigValidator stays in Application — it operates on the already-resolved public types in Contracts. -- alternatives: move Raw* to Contracts (rejected: pollutes Contracts with YAML-shape internals); have the resolver take dictionaries piecemeal (rejected: arg-list explosion).
- [Architecture] **Catalog dicts on `AgentSmithConfig` are `Dictionary<string, X>`, not `IReadOnlyDictionary<string, X>`.** Pragmatic relaxation: test setups frequently mutate `config.Projects["x"] = ...` post-construction. Making them concrete dictionaries cuts ~500 lines of test-helper churn. Production code paths construct AgentSmithConfig once (via the resolver) and don't mutate the catalogs afterwards — the relaxation is purely an in-test ergonomic. The immutability invariant lives in convention, not in the type system. -- alternatives: IReadOnlyDictionary + test-only mutable wrapper (rejected: doubles the type surface for tests); strict immutability + builder pattern (rejected: every test would need a 5-line builder dance).
- [Architecture] **`ResolvedProject` (and other new records) have init-only properties with default values, not `required` modifiers.** Initially used `required`; backed off because tests construct ResolvedProject with just the fields each test cares about (e.g. only a Pipeline + Polling for the Normalizer tests). The resolver always populates everything; tests bypass the resolver and need the cheap default-construction path. The `Repo` accessor (transitional single-repo shim) has both a getter (asserts Count==1) and an `init` setter (initializes `Repos = new[] { value }`) so test code reads `Repo = ...` naturally. -- alternatives: required fields + test factory helpers (rejected: 50+ test files would need migration to a factory pattern); positional records (rejected: 14-property positional ctor is unreadable at the call site).
- [Architecture] **Webhook handlers retain their per-handler pipeline_from_label iteration; only the polling path's `PipelineResolver` consults the global fallback.** Refactoring all five webhook handlers (GitHub Issue / GitLab Issue / AzureDevOps WorkItem / Jira Assignee / GitHub Comment etc.) to route through `PipelineResolver` would have added ~40 more call sites to migrate. For p0139 the polling path proves the design; webhook handlers fall back to `trigger.DefaultPipeline` per their existing logic — which is the pre-p0139 behavior. Documented as a known follow-up; closes when a project actually needs label routing via the global map on webhook events. -- alternatives: refactor webhooks too (rejected: scope blow-up); make pipeline_triggers a no-op until webhooks honor it (rejected: polling is a first-class trigger surface and benefits today).
- [Implementation] **`RepoType` enum has 4 values including `Local`, not 3 as the spec said.** Spec's 3-value enum (GitHub/GitLab/AzureDevOps) dropped Local. But the CLI uses `LocalSourceProvider` to operate against `--source-path /tmp/foo` and the api-security passive-mode flow uses `type: Local`. Dropping Local would break those flows. Adding Local to the enum is a one-row addition with no downstream cost; the catalog accepts `type: Local, path: ./repo`. -- alternatives: keep enum at 3 + special-case Local outside the catalog (rejected: forces CLI code to construct a synthetic non-catalog RepoConnection); drop LocalSourceProvider entirely (rejected: breaks dev workflows).
- [Implementation] **`AzureRepos` → `AzureDevOps` is a hard rename.** Pre-p0139 SourceConfig.Type allowed the string `"AzureRepos"` to select AzureReposSourceProvider. The new enum has `AzureDevOps` for both repos and trackers. The dogfood and example YAML are rewritten; operators migrating old configs by hand must change the string. No legacy-alias fallback in the enum parser. -- alternatives: accept `AzureRepos` as alias (rejected: prolongs naming inconsistency for no real cost — migration is one-time).
- [Implementation] **`WebhookTriggerConfig.PipelineFromLabel` is now nullable (`Dictionary<string, string>?`).** A null map means "use global pipeline_triggers"; a non-null populated map means "use my entries"; a non-null empty map is equivalent to null (uses global). Loaders that previously accessed `.Count` need null-safe patterns (`?.Count ?? 0`). All call sites were updated. -- alternatives: keep non-nullable and use Count==0 as the "use global" signal (rejected: empty map and "not set" should be distinguishable in YAML; null is the natural representation of "not set").
- [Implementation] **`ProjectConfigNormalizer` operates on `RawProjectEntry`, not on the public `ResolvedProject`.** The normalizer applies the legacy single-pipeline → Pipelines-list shim BEFORE the resolver runs. Operating on raw entries keeps the shim contained to the loader path; the resolver consumes already-normalized raw and produces ResolvedProject with the final Pipelines list. -- alternatives: normalize after resolution (rejected: ResolvedProject is init-only; mutation post-construction is forbidden); duplicate the shim in resolver (rejected: doubles the legacy-handling code path).
- [Implementation] **Resolver splits into 4 small classes (`RepoCatalogBuilder`, `TrackerCatalogBuilder`, `ResolvedProjectBuilder`, `ConfigCatalogResolver` as orchestrator).** Each stays well under the 120-line class limit. The orchestrator builds catalogs first, then projects (so project resolution can see the materialized catalogs). Errors are collected into a `List<string>` passed through; only the orchestrator throws (aggregated `ConfigurationException`). -- alternatives: one fat resolver class (rejected: violates the 120-line limit); per-project resolver as a static helper (rejected: harder to substitute in tests).
- [Implementation] **`SourceConfigOverrider` returns `ResolvedProject` instead of mutating.** Pre-p0139 the overrider mutated `project.Source.Type = ...` on a mutable SourceConfig. RepoConnection is an immutable record; the rewrite uses `repo with { ... }` to build a modified copy and returns `project with { Repos = new[] { modified } }`. The interface signature changed accordingly (was `void Apply(ProjectConfig, PipelineContext)`, now `ResolvedProject Apply(ResolvedProject, PipelineContext)`). -- alternatives: keep RepoConnection mutable (rejected: contradicts the "records with init properties" coding principle); use `ref` parameters (rejected: not idiomatic C#).
- [Implementation] **`AgentSmithConfig.Empty()` static factory replaces the inline empty-config initialization.** The DI default registration uses `AgentSmithConfig.Empty()` rather than spelling out all five catalog dicts. Default values on the record's init properties make this a literal one-line factory. -- alternatives: inline expansion at the DI registration site (rejected: 5 lines of empty-dict gymnastics every time); accept null catalogs (rejected: every consumer would need null-checks).
- [TradeOff] **Validator implements only 2 of the 4 spec-listed checks.** The spec listed: (a) unresolved references, (b) duplicate keys, (c) pipeline_triggers values → known pipelines, (d) trigger-block kind vs tracker type. (a) is already done by the resolver — defense-in-depth in the validator would be dead code. (b) is a no-op because YAML dictionaries can't have duplicate keys (YamlDotNet rejects them at parse time). Validator implements (c) and (d) — the genuine cross-cutting checks the resolver can't see. -- alternatives: implement (a)+(b) anyway for completeness (rejected: dead code is worse than absent code).
- [TradeOff] **Validator wired into Server startup only — CLI runs the resolver but not the validator.** CLI invocations are interactive and short-lived; an invalid config fails fast at the resolver step with the same precise error message. Server is long-running and benefits from the broader sanity checks (trigger/tracker type alignment) at startup before requests start arriving. Adding validator to CLI is one more line if a user case shows up. -- alternatives: validator runs in both composition roots (rejected: marginal CLI benefit for an extra line of wiring); validator runs lazily on first config-read (rejected: defeats fail-fast).
- [Tooling] **Bulk-migration of ~70 consumer call sites done via `perl -i -pe` regex pass + targeted Edit fixups.** Type-name renames (`SourceConfig` → `RepoConnection`, `TicketConfig` → `TrackerConnection`, `ProjectConfig` → `ResolvedProject`) and property renames (`.Source` → `.Repo`, `.Tickets` → `.Tracker`) were almost all mechanical. Two perl passes plus ~10 manual Edits drove the production-code error count from 450 to 0. Test fixtures (10-15 files) needed similar treatment plus type-string-to-enum migrations (`Type = "GitHub"` → `Type = RepoType.GitHub`) which the perl handled via case-insensitive non-greedy matching inside `new RepoConnection { ... }` blocks. -- alternatives: hand-edit every call site (rejected: time-cost without quality gain); rely entirely on a subagent (rejected: dispatched once and stalled at 600s without progress; bulk-perl is faster and more predictable).

## p0140a: Resolver foundation slice of multi-repo + project pipelines

- [Architecture] **p0140 sliced into a/b/c/d/e — implemented a first.** Codebase exploration after p0139 landed revealed the spec's "4 webhook handlers + 4 pollers" model didn't match reality (11 webhook handlers in `src/AgentSmith.Server/Services/Webhooks/`, pollers wired per-project not per-tracker, 18 `.Repo` shim consumers across 8 files, no metrics infrastructure). Honest sizing for the umbrella spec was 4-7 sessions of focused work. Slicing it lets each piece land as a reviewable single-purpose PR. The umbrella p0140 spec stays in planned/ as the design-intent reference, with a SUPERSEDED note pointing at the slices. -- alternatives: implement the umbrella in one PR (rejected: would have produced 6 half-implemented changes overlapping in one branch); restart the planning from scratch (rejected: the umbrella's design decisions are good, only the implementation needed slicing).
- [Architecture] **`ProjectResolver` is project-only; pipeline-within-project still delegates to `PipelineResolver.Resolve`.** The new resolver picks WHICH project owns an incoming ticket; once that's decided, the existing pipeline-from-label / default-pipeline / global-fallback chain (touched in p0139) keeps deciding WHICH pipeline of that project runs. Coupling both decisions into a single new resolver would double the test surface for no benefit — pipeline routing is already correctly handled today. `ProjectResolver.Resolve` returns `ProjectMatch(ProjectName, PipelineName, Kind)` tuples; the `Kind` field carries the trigger-block name that matched (github/gitlab/azuredevops/jira) for diagnostics. -- alternatives: coupled (project + pipeline) resolver (rejected: doubles test surface, two responsibilities); replace PipelineResolver (rejected: actively-used in polling path, no benefit to rewrite).
- [Architecture] **Adding `ITicketProvider.SupportsComments` in the foundation slice, not in the consumer slice.** The interface change has zero behaviour cost (default-true means today's 4 providers inherit it unchanged) but adding it in p0140b alongside the 11 webhook-handler migrations would crowd the PR. Doing it in p0140a keeps the noise minimal: one line on the interface, no provider implementations need touching, the future Email provider (p0141) overrides to false, and the consumer slice can focus on handler structure. -- alternatives: bundle into p0140b (rejected: noise crowds the slice that already touches 11 files); skip until p0141 (rejected: p0140b's zero-match path needs the capability check at the handler call site).
- [Architecture] **`IncomingTicketEnvelope` is the new platform-neutral input — `WebhookResult` is not extended.** Today's WebhookResult is the handler's OUTPUT (already-resolved project + pipeline). The envelope is the INPUT to ProjectResolver. Same data flow but a separate type because: (a) the envelope has fields WebhookResult lacks (labels, area-path, source-repo-url, to-address); (b) WebhookResult carries handler-internal state (TriggerInput, InitialContext, DialogueAnswer) the resolver shouldn't see; (c) a single envelope type lets the resolver be tested independently of any handler. -- alternatives: extend WebhookResult with the new fields (rejected: conflates input + output shape); per-handler envelope builders without a typed shape (rejected: can't test the resolver against a typed input).
- [Architecture] **`PipelineDefinition.AgentName` (string catalog-ref) is added alongside the existing `Agent: AgentConfig?` field — not replacing it.** AgentName is the original YAML reference (e.g. `agent: claude-fast`); Agent is the catalog-resolved AgentConfig that `PipelineConfigResolver.Resolve` consumes. ConfigCatalogResolver populates both at load-time. Keeping AgentName for diagnostics + validator error messages ("pipeline X declares agent Y which is not in catalog") makes failures readable without grepping the YAML. -- alternatives: only AgentName + look up at resolve-time (rejected: PipelineConfigResolver would need agents-catalog injection — invasive change); only Agent + lose the original reference (rejected: error messages reference the catalog key, not the resolved AgentConfig hash).
- [Implementation] **`RawPipelineEntry` introduced to separate YAML-binding from the resolved `PipelineDefinition`.** Pre-p0140a, RawProjectEntry.Pipelines was typed as `List<PipelineDefinition>` — which has `AgentConfig? Agent` — so YAML like `pipelines: [{name: foo, agent: bar}]` couldn't bind ("bar" is a string, AgentConfig is an object). No project actually used the pipelines feature so it didn't break anything, but the type was structurally wrong. RawPipelineEntry has `string? Agent` (catalog ref); ResolvedProjectBuilder maps Raw → Resolved with the catalog lookup. -- alternatives: change PipelineDefinition.Agent to `string?` (rejected: PipelineConfigResolver consumes it as AgentConfig today); custom YamlTypeConverter to accept either string or object (rejected: deferred — only needed for the `- pipeline-name` shorthand form which p0140a doesn't ship; object form `{name: ..., agent?: ..., skills_path?: ...}` is sufficient for now).
- [Implementation] **`AreaPathNormalizer` is `internal static` — lives in Triggers next to ProjectResolver.** ADO area paths come with either `\` (native) or `/` (YAML-safe) separators, plus the YAML quoted-string double-escape pitfall (`"...\\B..."` silently becomes `...\B...` in the deserialised string). Normalising to backslash form on both sides of the comparison is the cheapest correct path. Prefix matching enforces a real path-segment boundary so `ContosoMain\Billing` doesn't match the sibling `ContosoMain\BillingOther`. -- alternatives: leave the operator's form as-is and compare verbatim (rejected: `Contoso/Billing` would never match `Contoso\Billing`); make the comparator pluggable (rejected: premature; one comparator is what's needed).
- [Implementation] **`ProjectResolution` is REQUIRED on every declared trigger block at the schema level — validated at startup, not at first-webhook.** With multi-tenant trackers being the realistic majority (one ADO project hosting 60 repos, one Jira install used by 8 teams), the webhook handler can't route without an explicit declaration. Catching the missing field at config-load surfaces the mistake before any traffic arrives. The validator emits a typed error per missing trigger; the loader writes it as a runtime-nullable property so the validator can produce a clean message instead of a null-deref. -- alternatives: required at the C# record level (rejected: bypasses the typed validator message — operators see a null-ref stack trace instead); optional with a default of tag-by-project-name (rejected: silent magic).
- [Implementation] **`ResolutionStrategy` enum has 4 values including `ToAddress` (Email, p0141).** Adding the enum value now — even though no Email provider exists yet — lets p0141 land without revisiting this enum's serialization shape later. The schema enum lists all four; the resolver's switch returns false for ToAddress when the envelope has no ToAddress field (today's behaviour for every non-Email webhook handler). -- alternatives: 3-value enum, add ToAddress in p0141 (rejected: enum-value addition is a serialization-compat hazard once configs have been written; better to declare the surface now).
- [Implementation] **Validator's "pipelines references unknown agent" is defence-in-depth, not the primary check.** ResolvedProjectBuilder's ResolvePipelines already fails fast at load when AgentName doesn't resolve, AND emits a precise `pipeline 'X' references agent 'Y' which is not defined in agents: catalog` error. The validator only catches the case where AgentName is populated but Agent is null (test fixtures, partial loads). The validator test for this case constructs a deliberately-broken ResolvedProject without going through the resolver. -- alternatives: skip the validator check (rejected: the validator runs at server startup independently of the loader; defense-in-depth catches programmatic config construction); make the validator the primary (rejected: duplicates the resolver's logic).
- [TradeOff] **No production caller goes through ProjectResolver in p0140a.** The resolver exists, is DI-registered, has 7 strategy tests + 3 normalizer tests — but no webhook handler invokes it. The first consumer migration lands in p0140b. This is the cost of slicing the foundation out: a feature that's not yet active, with full test coverage, ahead of the migration that turns it on. The benefit is that p0140b can focus exclusively on handler structure changes without re-litigating the data model. -- alternatives: ship the resolver only when the first consumer is also ready (rejected: collapses the foundation+migration into one large PR — the original problem); ship the consumer migration with a mocked resolver (rejected: defeats the test surface).
- [TradeOff] **Pipelines list YAML shorthand (string entry) deferred.** The schema's pipelineEntry $def declares oneOf string-or-object, but the YAML loader only accepts the object form today (RawProjectEntry.Pipelines is `List<RawPipelineEntry>`, and YamlDotNet without a custom converter can't bind a scalar to a class). A custom IYamlTypeConverter for RawPipelineEntry would add ~40 lines for a convenience that no production project currently uses (the dogfood config uses singular `pipeline: fix-bug`). When a project actually needs `pipelines: [foo, bar]` shorthand, add the converter then. -- alternatives: ship the converter now (rejected: ~40 lines + a test surface for an unused feature); reject the schema's oneOf shape (rejected: spec wants both forms eventually; keeping the schema accurate to intent has value).

## p0140b: Webhook migration + spawner (multi-repo fan-out at the enqueue layer)

- [Architecture] **Pre-implementation spec audit refined every gap before code touched.** The original p0140b spec had at least seven internal contradictions (e.g., "11 handlers" while only 8 ticket-event handlers exist; "Enqueue via IRedisJobQueue" while ClaimSpawnAsync is needed for dedup; "ResolveAsync" on a synchronous resolver; multi-repo end-to-end claim while consumer side still throws on project.Repo). The audit produced a coherent spec, then implementation followed. Without the audit, each gap would have surfaced mid-implementation as a context-thrashing question. -- alternatives: implement spec-literal and accept the regressions / open questions (rejected: spec-first methodology demands the spec be the contract; gaps surface design problems, not implementation problems).
- [Architecture] **`ClaimSpawnAsync` is the new claim-region method; `ClaimAsync(single)` becomes a 1-element wrapper.** The naive "loop ClaimAsync N times" approach silently degrades multi-repo to single-repo because the ticket lifecycle transition (Pending → Enqueued) is ticket-level. The second repo sees lifecycle=Enqueued and returns AlreadyClaimed. ClaimSpawnAsync does one pre-check, acquires one lock on `agentsmith:claim-lock:{platform}:{ticket-id}`, transitions lifecycle once, and enqueues N PipelineRequests inside the locked region. All dedup protections survive multi-repo fan-out. -- alternatives: direct `IRedisJobQueue.EnqueueAsync` per spec text (rejected: loses Redis claim-lock + ClaimPreChecker + lifecycle transition); per-(platform, ticket, repo) lifecycle (rejected: ticket-level state on the tracker can't carry per-repo status without modeling work in the tracker — out of scope).
- [Architecture] **`SpawnRegionExecutor` extracted from `TicketClaimService` to honor the 120-line class limit.** Adding ClaimSpawnAsync inline blew TicketClaimService to 148 lines; the post-lock work (lifecycle read + transition + N enqueues + per-request logging) factored cleanly into its own ~75-line helper. TicketClaimService now owns pre-check + lock orchestration + scope-logging; SpawnRegionExecutor owns the inside-the-lock sequence. Both classes stay under the limit. -- alternatives: split ClaimSpawnAsync logic across multiple helpers in TicketClaimService (rejected: arbitrary cuts, each still over the method-line limit); inline everything (rejected: violates the 120-line class limit).
- [Architecture] **New interface `IEnvelopeProjectResolver` rather than renaming the existing Server-side `IProjectResolver`.** The Server's existing IProjectResolver (ticket-existence API lookup, used by IntentEngine for slack-intent disambiguation) has an unrelated responsibility and a different return shape. Reusing the name would require renaming the old one — touches IntentEngine + tests + DI for no real benefit. The "Envelope" prefix makes the two visibly separate at the call site. -- alternatives: rename old IProjectResolver to ITicketProjectLookup (rejected: scope creep into IntentEngine surface); skip the interface and inject concrete ProjectResolver (rejected: breaks the SOLID DI rule that every injected service has an interface).
- [Architecture] **`WebhookSpawnDispatcher` concentrates per-match-loop + zero-match handling.** Eight migrated ticket-event handlers all need the same post-resolver work: select trigger by Kind, apply status filter, call spawn use case OR (zero match) emit structured log + opt-in tracker comment. Duplicating this in 8 handlers would: (a) blow each handler past the 50-line target; (b) make the zero-match policy drift across platforms. The dispatcher is a sealed instance class (not static) so the ITicketProviderFactory + ILogger injection is clean. -- alternatives: helper static methods on each handler (rejected: 8 copies of the same loop; one platform changes and the others silently diverge); base class (rejected: violates "composition over inheritance — base classes max 30 lines, no business logic").
- [Architecture] **`WebhookRequestProcessor.RouteToClaimServiceAsync` deleted, not deprecated.** After the 8 ticket-event handlers migrate, no handler returns ProjectName/TicketId on its WebhookResult — the structured-ticket routing branch is fully dead code. Deprecation invites confusion ("which path actually runs?"). The DialogueAnswer branch (PR /approve|/reject) and TriggerInput legacy branch stay untouched — they serve different webhook shapes. -- alternatives: keep with `[Obsolete]` (rejected: nothing to migrate from since all callers are deleted); leave as untested dead code (rejected: dead code rots and confuses readers).
- [Architecture] **Multi-repo is enqueue-only in p0140b; consumer-side multi-repo is p0140d.** The 12 builder/handler sites that still read `project.Repo` would throw on a multi-repo project (Repos.Count > 1). Migrating all 12 in p0140b would re-litigate the `.Repo` shim removal — the entire purpose of p0140d. The multi-repo smoke test asserts only enqueue count (3 PipelineRequests for a 3-repo project), not full execution. Today's deployed projects are all single-repo, so consumer-side behavior is invisible in the wild. -- alternatives: do both in one slice (rejected: PR explodes to ~50 files; reviewability dies); leave multi-repo broken end-to-end and assert nothing (rejected: enqueue-level multi-repo is the core deliverable of p0140b).
- [Implementation] **Backward-compat default-trigger-label fallbacks deleted (GitHub `agent-smith`, ADO `security-review`).** p0140a's validator now requires `project_resolution` on every declared trigger. A project without a trigger block won't match through the resolver, so the old "no trigger → fall back to hardcoded default label" branch is unreachable. Deleting it shrinks the handlers by ~15 lines each and removes the silent-magic that hid misconfigurations. Dogfood + example configs use explicit triggers everywhere; no real-world flow relies on the fallback. -- alternatives: keep with a deprecation log (rejected: it's already unreachable post-p0140a); fail loud at config-load if a trigger block is absent (deferred: p0140a's validator already requires project_resolution; a missing trigger block is the operator's explicit "no triggering on this platform" choice).
- [Implementation] **`ITicketClaimService` registered as Singleton (was Scoped).** WebhookSpawnDispatcher → ISpawnPipelineRunsUseCase (Transient) → ITicketClaimService (was Scoped) failed `ValidateScopes` because a Singleton can't consume a Scoped service. TicketClaimService is stateless (its deps IRedisClaimLock + ITicketStatusTransitionerFactory + IRedisJobQueue are all singletons); making it Singleton keeps the dependency chain valid without changing behaviour. Caught by `ServerDi_BuildUnderDevelopmentEnvWithValidateOnBuild_AllSingletonsResolve_NoLifetimeViolations`. -- alternatives: Scope the dispatcher + all 8 handlers (rejected: cascading lifetime change for one wiring fix); keep dispatcher Scoped (rejected: handlers resolved as IEnumerable<IWebhookHandler> at request time means we'd build the dispatcher per request despite being stateless — unnecessary work).
- [Implementation] **PR/MR-dialogue handlers (5 handlers) stay out of scope.** GitHubPrCommentWebhookHandler, GitHubPrLabelWebhookHandler, AzureDevOpsPrCommentWebhookHandler, GitLabMrCommentWebhookHandler, GitLabMrLabelWebhookHandler route `/approve` and `/reject` answers to in-flight pipelines via WebhookDialogueRouter. They don't start new pipelines and have no project-resolution to perform. The original spec's "11 handlers" count came from including some of these; the audit corrected it to 8 ticket-event handlers. -- alternatives: migrate them too for uniformity (rejected: scope creep; they have no resolver to call); rename the dialogue path (rejected: not what p0140 is about).
- [TradeOff] **Writeback comment reads `RepoConnection` from the per-handler context record, not from `PipelineContext.CurrentRepo`.** The CommitAndPRContext + InitCommitContext already carry RepoConnection (the builders pass `project.Repo`). The header text change is a one-line edit per handler. Moving the read to `PipelineContext.CurrentRepo` would couple this to p0140d. -- alternatives: add ContextKeys.CurrentRepo in p0140b just for the writeback (rejected: would either need to half-migrate the 12 sites or have two ways to read the run's repo).

## p0140c: Poller restructure (per-tracker)

- [Architecture] **One `TrackerPoller` replaces the four per-platform pollers, not four per-tracker wrappers.** Post-p0140c the loop is identical across platforms — pull open tickets, build envelope (Labels-only — Ticket lacks AreaPath/SourceRepoUrl), resolve, per-match spawn. The only platform-specific value is the `Platform` string on the envelope, which comes from `tracker.Type.ToString()`. Preserving 4 wrapper files would be ceremony — each wrapper ~10 lines delegating to a shared helper. One class, `tracker.Type` carries the platform identity. -- alternatives: 4 per-platform wrappers around a TrackerPollLoop helper (rejected: 4 files of duplicated plumbing); template-method base class (rejected: violates 'no fat base classes — composition over inheritance'); preserve the 4 files per spec text (rejected: the spec's '4 pollers change ctor' was a holdover from the pre-collapse model, not a design intent).
- [Architecture] **`PollerHostedService` no longer touches `ITicketClaimService` — pollers own the spawn path.** Pre-p0140c: poller→ClaimRequest→PollerHostedService→ClaimService→Enqueue. Post-p0140c: each poller calls `ISpawnPipelineRunsUseCase.ExecuteAsync` directly per matched project, which goes through `ITicketClaimService.ClaimSpawnAsync` (p0140b). PollerHostedService shrinks to a parallel-loop driver + `PollResult` summary logger. Dedup machinery (Redis lock + lifecycle transition) is preserved end-to-end. -- alternatives: keep PollerHostedService as the ClaimAsync caller (rejected: forces it to know about resolver+spawn semantics; concentration of spawn knowledge in SpawnPipelineRunsUseCase is the right boundary); keep pollers yielding ClaimRequests but route through SpawnPipelineRunsUseCase from the host (rejected: per-match spawning is a per-ticket decision, not a per-cycle decision).
- [Architecture] **Claim-lock key stays `agentsmith:claim-lock:{platform}:{ticket-id}` (NOT `{tracker-name}:{ticket-id}`).** A ticket id is unique within a platform. Switching the lock key to per-tracker would split webhook-driven and poller-driven claims into separate buckets — but they SHOULD race for the same lock so a webhook-arrived ticket isn't double-spawned by the next poll cycle. -- alternatives: per-tracker lock key (rejected: webhook + poller race for the same ticket would no longer be detected); per-(project, ticket) lock key (rejected: a ticket spawning N projects via ambiguous-match would self-block); leave today's pre-p0140b shape (chosen: ClaimSpawnAsync from p0140b uses this exact key shape, no change required).
- [Architecture] **`PollResult` is a count summary, not a list of ClaimRequests or ClaimResults.** Pollers handle their own dispatching during PollAsync; the host only needs visibility into per-cycle counts (polled / matched / spawned / status-filtered / zero-matched). Returning richer per-ticket data would force the host to know about per-ticket semantics it doesn't need. -- alternatives: return ClaimResult list (rejected: leaks claim-region details out of the poller); return void + emit counts via metrics (deferred: metrics are p0140e; PollResult is the bridge for now).
- [Architecture] **`TrackerConnection.Polling` is a new property; the old `ResolvedProject.Polling` stays as a deprecated input.** Operators with project-level polling get a structured warning at startup via `PollingConfigDeprecationWarner` but the server starts cleanly. The project-level block is parsed (still binds on RawProjectEntry → ResolvedProject) but the per-tracker pollers don't consult it. Cleanup is a follow-up phase once operators have migrated. -- alternatives: hard remove `ResolvedProject.Polling` (rejected: breaks every operator config in one release; the spec's slicing model favours soft-migration windows); silent ignore without warning (rejected: operators won't notice the deprecation until polling silently stops working).
- [Implementation] **`ZeroMatchComment` binding bug from p0140a fixed alongside `Polling` binding.** Audit revealed `RawTrackerEntry` had no `ZeroMatchComment` field and `TrackerCatalogBuilder.TryBuild` didn't project it onto `TrackerConnection` — so the p0140a property never bound from YAML. Both fields added in the same edit. The webhook spawn dispatcher's zero-match comment path was effectively dead-code before this fix. -- alternatives: ship the p0140c Polling fix alone and file p0140a-followup (rejected: same file, same edit, same review window — would be artificial to split).
- [Implementation] **`PollerFactory.Build` becomes per-tracker but stays a static helper, not a DI registration.** Today's PollerFactory.Build is invoked from `PollerLeaderHostedService.RunPollerAsync` per leader-acquisition cycle, with the freshly-loaded config snapshot. Per-tracker enumeration is the trivial swap (foreach over `config.Trackers.Values where Polling.Enabled`). Pollers remain non-DI services because their config (TrackerConnection + AgentSmithConfig snapshot) is per-cycle, not per-process. -- alternatives: register pollers via DI scopes (rejected: scope lifetime doesn't match leader-tenure; current factory pattern works); inject IConfigurationLoader into pollers + reload per-poll (rejected: poll cycles would see config drift mid-cycle; per-leader-tenure snapshot is the right cadence).
- [Implementation] **Polling path supports only tag-strategy project resolution (Labels-only envelope).** The Ticket entity has Labels but no AreaPath or SourceRepoUrl. `TrackerPoller.BuildEnvelope` populates only `Labels` + `TicketId` + `Platform`. Operators configuring `project_resolution: area-path` on an ADO project served by polling will see zero matches (logged as `polling-zero-match`). Adding AreaPath to Ticket would touch all 4 provider implementations + tests — deferred until an operator actually hits this. To be documented in p0140e operator docs. -- alternatives: extend Ticket with optional AreaPath (rejected: invasive for an unproven need); fail-loud at startup if polling-enabled tracker serves a non-tag-strategy project (rejected: false-positive risk if the project doesn't actually use polling for area-path).
- [TradeOff] **Project-level `Polling` removal deferred.** The deprecation warner ships, the project-level field stays bindable, the per-tracker pollers ignore it. Hard removal is a follow-up phase once operator configs are migrated. The dogfood config (config/agentsmith.yml) has no project-level Polling.Enabled=true today, so dogfood is clean.
