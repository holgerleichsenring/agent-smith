# yaml-language-server: $schema=../decision.schema.json
# p0073: Class Size Enforcement
phase: p0073
decisions:
  - category: Implementation
    chose: CI gate starts non-blocking, flip to blocking after Tier 1+2
    reason: allows incremental adoption without breaking builds.
  - category: Scope
    chose: Pure refactoring only (extract, rename, split
    reason: no behavior changes) — ensures no regressions from size enforcement work.
