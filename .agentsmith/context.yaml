# yaml-language-server: $schema=context.schema.json
meta:
  project: agent-smith
  version: 1.0.0
  type: [agent, pipeline]
  purpose: "Self-hosted AI orchestration framework: code, legal, security, workflows."

stack:
  runtime: .NET 8
  lang: C#
  infra: [Docker, K8s, Redis]
  testing: [xUnit, Moq, FluentAssertions]
  sdks: [Anthropic, OpenAI, Google-Gemini, Octokit, LibGit2Sharp, YamlDotNet]

arch:
  style: [CleanArch]
  patterns: [Command/Handler, Pipeline, Factory, Strategy, Adapter]
  layers:
    - Domain        # entities, value objects — no deps
    - Contracts   # interfaces, DTOs, config models
    - Application   # handlers, pipeline executor, use cases
    - Infrastructure # AI providers, git, tickets, Redis bus
    - Host          # CLI entry point, DI wiring
    - Dispatcher    # Slack gateway, job spawning, intent routing

behavior:
  pipelines:
    fix-bug:
      trigger: [cli, slack, webhook]
      steps: [FetchTicket, CheckoutSource, BootstrapProject, LoadCodeMap,
              LoadDomainRules, LoadContext, AnalyzeCode, Triage, GeneratePlan,
              Approve, AgenticExecute, Test, WriteRunResult, CommitAndPR]
      error: stop-on-first-failure
    security-scan:
      trigger: [cli, slack, webhook]
      steps: [CheckoutSource, BootstrapProject, LoadDomainRules, AnalyzeCode,
              Triage, ConvergenceCheck, CompileDiscussion, DeliverOutput]
    mad-discussion:
      trigger: [cli, slack]
      steps: [FetchTicket, CheckoutSource, BootstrapProject, LoadContext,
              Triage, ConvergenceCheck, CompileDiscussion, WriteRunResult, CommitAndPR]
    legal-analysis:
      trigger: [cli, slack, inbox-polling]
      steps: [AcquireSource, BootstrapDocument, LoadDomainRules,
              Triage, ConvergenceCheck, CompileDiscussion, DeliverOutput]

quality:
  lang: english-only
  limits: { method-lines: 20, class-lines: 120, types-per-file: 1 }
  principles: [SOLID, DRY, GuardClauses, FailFast]
  csharp: [file-scoped-ns, sealed-default, primary-constructors, records-for-DTOs]
  naming:
    classes: PascalCase
    booleans: Is/Has/Can-prefix
    async: Async-suffix
    fields: _camelCase
    interfaces: I-prefix
  testing: { style: AAA, naming: "{Method}_{Scenario}_{Expected}" }

integrations:
  AI:     { type: outbound, providers: [Claude, OpenAI, Gemini, Ollama] }
  Git:    { type: bidirectional, protocol: SSH/HTTPS, does: "LibGit2Sharp clone/branch/commit/push" }
  GitHub: { type: bidirectional, does: "Issues + PRs via Octokit" }
  GitLab: { type: bidirectional, does: "Issues + MRs via REST" }
  Azure:  { type: bidirectional, does: "Work items + Repos via DevOps SDK" }
  Jira:   { type: inbound, does: "Tickets via REST v3" }
  Slack:  { type: bidirectional, does: "Chat commands + progress via Events API" }
  Redis:  { type: bidirectional, does: "Job queue + progress pub/sub" }

state:
  done:
    p01: "Solution structure, domain entities, contracts, YAML config loader"
    p02: "Command/Handler pattern: 9 context records, 9 handler stubs, CommandExecutor"
    p03: "Providers: AzureDevOps+GitHub tickets, Local+GitHub source, Claude agentic loop"
    p04: "Pipeline execution: IntentParser, PipelineExecutor, ProcessTicketUseCase, DI wiring"
    p05: "CLI (System.CommandLine), Dockerfile, docker-compose, DI integration test"
    p06: "Resilience: Polly retry with exponential backoff + jitter"
    p07: "Prompt caching: CacheConfig, TokenUsageTracker, system prompt optimization"
    p08: "Context compaction: ClaudeContextCompactor, FileReadTracker deduplication"
    p09: "Model registry: per-task model selection, ScoutAgent for codebase discovery"
    p10: "Production container: headless mode, Docker hardening, health checks"
    p11: "Multi-provider: OpenAI GPT-4.1 + Gemini 2.5 agent providers"
    p12: "Cost tracking: RunCostSummary, PricingConfig, per-phase token tracking"
    p13: "Ticket writeback: UpdateStatus, Close, pipeline integration after PR"
    p14: "Webhook trigger: GitHub Action (issues.labeled), WebhookListener (--server)"
    p15: "Azure Repos source provider: clone, branch, PR via DevOps API"
    p16: "Jira ticket provider: REST v3, ADF parsing"
    p17: "GitLab provider: tickets + source (MRs, clone, branch)"
    p18: "Chat gateway: Redis message bus, Dispatcher, Slack adapter, conversation state"
    p19: "K8s manifests, Dispatcher Dockerfile, Kustomize overlays, docker-compose redis"
    p19a: "Docker job spawner: DockerJobSpawner, self-contained docker-compose"
    p20: "Intent engine (regex+Haiku), help command, error UX, agentic Slack updates"
    p21: "Code quality: folder restructuring (6 projects), sealed, constants, boolean naming, 161 tests"
    p22: "CCS auto-bootstrap: ProjectDetector, ContextGenerator, language-agnostic pipeline"
    p24: "Code map generation: CodeMapGenerator, LoadCodeMapHandler, prompt integration"
    p27: "Structured command UI: Slack modals, slash commands, cached ticket search, pipeline override"
    p26: "Coding principles detection: RepoSnapshot, LLM-based style/arch/methodology, ModelRegistry DI fix"
    p28: ".agentsmith/ directory: unified meta-file home, LoadContext/WriteRunResult, coding-principles generation, context injection"
    p29: "Init Project command: Slack modal + chat, JobRequest generalization, init-project pipeline"
    p30: "Systemic fixes: PR URL propagation, orphan job detection, optional parameter elimination (288 tests)"
    p31: "OrphanJobDetector Redis scan: GetAllAsync for untracked stale states after dispatcher restart (290 tests)"
    p32: "Architecture cleanup: ILlmClient abstraction, ILlmClientFactory per-project, ILlmIntentParser, blocked commands (339 tests)"
    p33: "Run cost in result.md: AgentExecutionResult, YAML frontmatter with token/cost/duration data (344 tests)"
    p34: "Multi-skill architecture: cascading commands, role-based triage, SkillRound/ConvergenceCheck, execution trail, auto-detection (407 tests)"
    p35: "Simplified Slack commands: Fix Ticket+Pipeline dropdown → Fix Bug, Fix Bug (no tests), Add Feature with implicit pipeline (413 tests)"
    p36: "GenerateTests & GenerateDocs commands, pipeline resilience, OrphanJobDetector liveness checks, PLATFORM env fix (427 tests)"
    p37: "Strategy Pattern refactoring, remove PipelineConfig, command/handler decomposition"
    p38: "MAD Discussion pipeline: CompileDiscussionHandler, SkillsPath, mad-discussion preset"
    p40a: "Contracts extensions, Infrastructure.Core, ProviderRegistry (473 tests)"
    p41: "Decision Log: IDecisionLogger, tell-don't-ask, decisions in result.md (466 tests)"
    p42: "Legal analysis pipeline: AcquireSource, BootstrapDocument, DeliverOutput, InboxPolling, legal skills (473 tests)"
    p43a: "CI/CD: GitHub Action, Docker Hub publishing, multi-arch, Dockerfile optimization"
    p43b: "Security pipeline: CLI subcommands, IPrDiffProvider, IOutputStrategy, security skills (478 tests)"
    p43c: "SARIF output: SarifOutputStrategy, MarkdownOutputStrategy, IPrCommentProvider (488 tests)"
    p43d: "Ollama local models: OllamaAgentProvider, OllamaAgenticLoop, capability check, IOutputStrategy DI, IPrCommentProvider (492 tests)"
    p43e: "Webhook expansion: IWebhookHandler dispatch, GitHub/GitLab/AzDO signature verification (503 tests)"
    p44: "Rename ProcessTicketUseCase → ExecutePipelineUseCase, rebrand to AI orchestration framework (511 tests)"
    p45: "API security scan: Nuclei + swagger.json analysis, OWASP API Top 10 skills, LoadSwagger/SpawnNuclei handlers (517 tests)"
    p46: "CLI refactor: PipelineCommand, PipelineRequest, Program.cs split (456→18), factory registries, LlmIntentParser, verb-per-pipeline, tell-don't-ask cleanup (517 tests)"
    p47: "Spectral OpenAPI linter + api-design-auditor rewrite with 6-category schema analysis (534 tests)"
    p48: "Swagger context compression — 85% token reduction in skill prompts (545 tests)"
    p49: "IToolRunner abstraction — Docker, Podman, Process tool runners with auto-detection (553 tests)"
    p50: "Multi-output strategy: --output console,summary,markdown,sarif + --output-dir + SummaryOutputStrategy (561 tests)"
    p51: "ToolRunner clean architecture: {work} placeholders, no hardcoded paths, OutputDir resolved once (567 tests)"
    p52: "Single executable release: self-contained binaries for 5 platforms, GitHub Releases, Release-Please, Docker entrypoint with gosu, config discovery (567 tests)"
    p53: "Documentation site at docs.agent-smith.org — MkDocs Material, GitHub Pages, 28 pages (567 tests)"
    p54: "Security scan expansion: StaticPatternScan (91 patterns), GitHistoryScan, DependencyAudit, 4 new LLM skills, 9 security specialists (567 tests)"
    p55: "Security findings compression: CompressSecurityFindingsHandler, category grouping, skill-specific slices, ~74% token reduction (567 tests)"
    p56: "Security scan polish: SecretProviderRegistry (18 mappings), mandatory FP filter, severity logs, SARIF output, ExtractFindingsHandler, docs update (567 tests)"
    p58: "Interactive dialogue: DialogQuestion/DialogAnswer contracts, RedisDialogueTransport, ConsoleDialogueTransport (CLI), SlackAdapter Block Kit (5 QuestionTypes), ask_human tool, AskCommandHandler, dialogue trail in result.md (749 tests)"
    p59: "PR comment webhook: CommentIntentParser, GitHubPrCommentWebhookHandler (Scenario A+B), GitHubPrCommentReplyService, IConversationLookup, dialogue answer routing, require_member + allowed_pipelines guards (749 tests)"
    p60: "Security enhancements: ZapSpawner (docker cp), SecurityTrendHandler (git-based + CLI subcommand), SecuritySnapshotWriter, SpawnFixHandler (auto-fix with confirm_before_fix dialogue), dast-analyst + dast-fp-filter skills (749 tests)"
  active: {}
  planned:
    # --- next up (feature roadmap) ---
    p57a: "Skill standard: SKILL.md format migration → .agentsmith/phases/planned/p57a-skill-standard.md"
    p57b: "Skill manager: autonomous skill discovery + installation (needs p57a + p58) → .agentsmith/phases/planned/p57b-skill-manager.md"
    p61: "Project knowledge base: Karpathy pattern, wiki compilation, QueryIntent (needs p58 + p60) → .agentsmith/phases/planned/p61-project-knowledge-base.md"
    p57c: "Autonomous pipeline: agent writes tickets (needs p61) → .agentsmith/phases/planned/p57c-autonomous-pipeline.md"
    p62: "Docs update: new pages for all features → .agentsmith/phases/planned/p62-docs-update.md"
    # --- backlog (new features) ---
    p23: "Multi-repo support → .agentsmith/phases/planned/p23-multi-repo-update.md"
    p25: "PR review iteration → .agentsmith/phases/planned/p25-pr-review.md"
    # --- deferred (platform expansion, no new features) ---
    p58b: "Teams integration: TeamsAdapter, Adaptive Cards, JWT → .agentsmith/phases/planned/p58b-teams-integration.md"
    p59b: "GitLab MR comment webhook → .agentsmith/phases/planned/p59b-gitlab-pr-comments.md"
    p59c: "Azure DevOps PR comment webhook → .agentsmith/phases/planned/p59c-azdo-pr-comments.md"
    # --- deferred (refactoring, no new features) ---
    p40b: "Git provider extraction (refactoring) → .agentsmith/phases/planned/p40b-git-provider-extraction.md"
    p40c: "AI provider extraction (refactoring) → .agentsmith/phases/planned/p40c-ai-provider-extraction.md"
    p40d: "Storage providers & LoadAttachments (refactoring) → .agentsmith/phases/planned/p40d-storage-providers-attachments.md"
