// 2026-09-25-8e51d: the tab names, in a file of their own so the tablist and the pane can both
// name them without one importing the other.
export type DialogPaneTab = "proposal" | "scope" | "filed" | "approved";
