import { ConversationsView } from "@/components/dialog/ConversationsView";

// 2026-09-21-f237b: the second view of the Work it out surface — every conversation the caller
// holds, with room to read one. A peer route rather than a rail entry: the rail names surfaces.
export default function ConversationsPage() {
  return <ConversationsView />;
}
