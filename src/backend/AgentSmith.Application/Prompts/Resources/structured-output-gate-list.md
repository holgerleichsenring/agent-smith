Review all observations. Respond with JSON: {"confirmed": [...], "rejected": [...]}. Each item follows the framework SkillObservation schema: {"concern": "security", "description": "", "suggestion": "", "severity": "high|medium|low|info", "confidence": 0-100, "file": "", "start_line": 0, "api_path": "METHOD /path", "schema_name": "SchemaName", "evidence_mode": "confirmed|potential|analyzed_from_source", "category": "secrets|injection|auth|...", "rationale": ""}. Preserve api_path / schema_name / file / start_line from contributor observations when present. Severity values: high, medium, low, info — never "critical" / "warning" / "severe" (map: critical→high, warning→medium). Output minified JSON on a single line — no whitespace between tokens, no indentation, no newlines.
