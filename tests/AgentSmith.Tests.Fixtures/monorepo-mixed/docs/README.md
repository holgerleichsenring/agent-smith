# monorepo-mixed/docs

Documentation sub-tree for the p0161 mixed-stack fixture. Validates that a
markdown-only context spawns its own sandbox with the generic-fallback image
(no language-specific toolchain) and is iterated alongside csharp/typescript
contexts in the same coordinator run.
