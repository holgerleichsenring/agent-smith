// Minimal fixture target — exercises CheckoutSource / SetupRegistryAuth /
// AnalyzeCode handler paths under a real DI graph + InProcessSandbox or
// DockerSandbox.

namespace Sample;

public static class Program
{
    public static int Main() => 0;
}
