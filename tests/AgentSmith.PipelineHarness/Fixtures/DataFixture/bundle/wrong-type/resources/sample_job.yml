resources:
  jobs:
    sample_job:
      name: sample_job
      max_concurrent_runs: "not a number"
      tasks:
        - task_key: main
          notebook_task:
            notebook_path: ../src/main.py
