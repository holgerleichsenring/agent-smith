{
  "name": "agent-smith-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build:tokens": "node ../tools/build-tokens.mjs",
    "prebuild": "npm run build:tokens",
    "predev": "npm run build:tokens",
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
