apiVersion: v1
kind: Service
metadata:
  name: agentsmith-server
  namespace: agentsmith
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8081
      nodePort: 30080
      protocol: TCP
